
=== IL Node Coverage ===

IL Premises: 824/878 attempted (93.85%), 807/878 succeeded (91.91%)
108 rule premises
315 if-premises: succeeded 286/315 (90.79%), failed 130/315 (41.27%), neither 12/315 (3.81%), total 416/630 (66.03%)

def $UINT64_MAX:
      clause 0:

def $UINT64_MAX_SQRT:
      clause 0:

def $GENESIS_SLOT:
      clause 0:

def $GENESIS_EPOCH:
      clause 0:

def $FAR_FUTURE_EPOCH:
      clause 0:

def $BASE_REWARDS_PER_EPOCH:
      clause 0:

def $DEPOSIT_CONTRACT_TREE_DEPTH:
      clause 0:

def $JUSTIFICATION_BITS_LENGTH:
      clause 0:

def $BLS_WITHDRAWAL_PREFIX:
      clause 0:

def $ETH1_ADDRESS_WITHDRAWAL_PREFIX:
      clause 0:

def $DOMAIN_BEACON_PROPOSER:
      clause 0:

def $DOMAIN_BEACON_ATTESTER:
      clause 0:

def $DOMAIN_RANDAO:
      clause 0:

def $DOMAIN_DEPOSIT:
      clause 0:

def $DOMAIN_VOLUNTARY_EXIT:
      clause 0:

def $DOMAIN_SELECTION_PROOF:
      clause 0:

def $DOMAIN_AGGREGATE_AND_PROOF:
      clause 0:

def $DOMAIN_APPLICATION_MASK:
      clause 0:

def $INTERVALS_PER_SLOT:
      clause 0:

def $BASIS_POINTS:
      clause 0:

def $NODE_ID_BITS:
      clause 0:

def $MAX_CONCURRENT_REQUESTS:
      clause 0:

def $TARGET_AGGREGATORS_PER_COMMITTEE_CONST:
      clause 0:

def $ETH_TO_GWEI:
      clause 0:

def $SAFETY_DECAY:
      clause 0:

def $TIMELY_SOURCE_FLAG_INDEX:
      clause 0:

def $TIMELY_TARGET_FLAG_INDEX:
      clause 0:

def $TIMELY_HEAD_FLAG_INDEX:
      clause 0:

def $TIMELY_SOURCE_WEIGHT:
      clause 0:

def $TIMELY_TARGET_WEIGHT:
      clause 0:

def $TIMELY_HEAD_WEIGHT:
      clause 0:

def $SYNC_REWARD_WEIGHT:
      clause 0:

def $PROPOSER_WEIGHT:
      clause 0:

def $WEIGHT_DENOMINATOR:
      clause 0:

def $DOMAIN_SYNC_COMMITTEE:
      clause 0:

def $DOMAIN_SYNC_COMMITTEE_SELECTION_PROOF:
      clause 0:

def $DOMAIN_CONTRIBUTION_AND_PROOF:
      clause 0:

def $PARTICIPATION_FLAG_WEIGHTS:
      clause 0:

def $G2_POINT_AT_INFINITY:
      clause 0:

def $TARGET_AGGREGATORS_PER_SYNC_SUBCOMMITTEE:
      clause 0:

def $SYNC_COMMITTEE_SUBNET_COUNT:
      clause 0:

def $MAX_REQUEST_LIGHT_CLIENT_UPDATES:
      clause 0:

def $SAFE_SLOTS_TO_IMPORT_OPTIMISTICALLY:
      clause 0:

def $DOMAIN_BLS_TO_EXECUTION_CHANGE:
      clause 0:

def $MAX_COMMITTEES_PER_SLOT:
      clause 0:

def $TARGET_COMMITTEE_SIZE:
      clause 0:

def $MAX_VALIDATORS_PER_COMMITTEE:
      clause 0:

def $SHUFFLE_ROUND_COUNT:
      clause 0:

def $HYSTERESIS_QUOTIENT:
      clause 0:

def $HYSTERESIS_DOWNWARD_MULTIPLIER:
      clause 0:

def $HYSTERESIS_UPWARD_MULTIPLIER:
      clause 0:

def $MIN_DEPOSIT_AMOUNT:
      clause 0:

def $MAX_EFFECTIVE_BALANCE:
      clause 0:

def $EFFECTIVE_BALANCE_INCREMENT:
      clause 0:

def $MIN_ATTESTATION_INCLUSION_DELAY:
      clause 0:

def $SLOTS_PER_EPOCH:
      clause 0:

def $MIN_SEED_LOOKAHEAD:
      clause 0:

def $MAX_SEED_LOOKAHEAD:
      clause 0:

def $MIN_EPOCHS_TO_INACTIVITY_PENALTY:
      clause 0:

def $EPOCHS_PER_ETH1_VOTING_PERIOD:
      clause 0:

def $SLOTS_PER_HISTORICAL_ROOT:
      clause 0:

def $EPOCHS_PER_HISTORICAL_VECTOR:
      clause 0:

def $EPOCHS_PER_SLASHINGS_VECTOR:
      clause 0:

def $HISTORICAL_ROOTS_LIMIT:
      clause 0:

def $VALIDATOR_REGISTRY_LIMIT:
      clause 0:

def $BASE_REWARD_FACTOR:
      clause 0:

def $WHISTLEBLOWER_REWARD_QUOTIENT:
      clause 0:

def $PROPOSER_REWARD_QUOTIENT:
      clause 0:

def $INACTIVITY_PENALTY_QUOTIENT:
      clause 0:

def $MIN_SLASHING_PENALTY_QUOTIENT:
      clause 0:

def $PROPORTIONAL_SLASHING_MULTIPLIER:
      clause 0:

def $MAX_PROPOSER_SLASHINGS:
      clause 0:

def $MAX_ATTESTER_SLASHINGS:
      clause 0:

def $MAX_ATTESTATIONS:
      clause 0:

def $MAX_DEPOSITS:
      clause 0:

def $MAX_VOLUNTARY_EXITS:
      clause 0:

def $INACTIVITY_PENALTY_QUOTIENT_ALTAIR:
      clause 0:

def $MIN_SLASHING_PENALTY_QUOTIENT_ALTAIR:
      clause 0:

def $PROPORTIONAL_SLASHING_MULTIPLIER_ALTAIR:
      clause 0:

def $SYNC_COMMITTEE_SIZE:
      clause 0:

def $EPOCHS_PER_SYNC_COMMITTEE_PERIOD:
      clause 0:

def $MIN_SYNC_COMMITTEE_PARTICIPANTS:
      clause 0:

def $UPDATE_TIMEOUT:
      clause 0:

def $INACTIVITY_PENALTY_QUOTIENT_BELLATRIX:
      clause 0:

def $MIN_SLASHING_PENALTY_QUOTIENT_BELLATRIX:
      clause 0:

def $PROPORTIONAL_SLASHING_MULTIPLIER_BELLATRIX:
      clause 0:

def $MAX_BYTES_PER_TRANSACTION:
      clause 0:

def $MAX_TRANSACTIONS_PER_PAYLOAD:
      clause 0:

def $BYTES_PER_LOGS_BLOOM:
      clause 0:

def $MAX_EXTRA_DATA_BYTES:
      clause 0:

def $MAX_BLS_TO_EXECUTION_CHANGES:
      clause 0:

def $MAX_WITHDRAWALS_PER_PAYLOAD:
      clause 0:

def $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP:
      clause 0:

def $MIN_GENESIS_ACTIVE_VALIDATOR_COUNT:
      clause 0:

def $MIN_GENESIS_TIME:
      clause 0:

def $GENESIS_FORK_VERSION:
      clause 0:

def $GENESIS_DELAY:
      clause 0:

def $SECONDS_PER_SLOT:
      clause 0:

def $SLOT_DURATION_MS:
      clause 0:

def $SECONDS_PER_ETH1_BLOCK:
      clause 0:

def $MIN_VALIDATOR_WITHDRAWABILITY_DELAY:
      clause 0:

def $SHARD_COMMITTEE_PERIOD:
      clause 0:

def $ETH1_FOLLOW_DISTANCE:
      clause 0:

def $EJECTION_BALANCE:
      clause 0:

def $MIN_PER_EPOCH_CHURN_LIMIT:
      clause 0:

def $CHURN_LIMIT_QUOTIENT:
      clause 0:

def $PROPOSER_SCORE_BOOST:
      clause 0:

def $REORG_HEAD_WEIGHT_THRESHOLD:
      clause 0:

def $REORG_PARENT_WEIGHT_THRESHOLD:
      clause 0:

def $REORG_MAX_EPOCHS_SINCE_FINALIZATION:
      clause 0:

def $PROPOSER_REORG_CUTOFF_BPS:
      clause 0:

def $MAX_PAYLOAD_SIZE:
      clause 0:

def $MAX_REQUEST_BLOCKS:
      clause 0:

def $EPOCHS_PER_SUBNET_SUBSCRIPTION:
      clause 0:

def $MIN_EPOCHS_FOR_BLOCK_REQUESTS:
      clause 0:

def $ATTESTATION_PROPAGATION_SLOT_RANGE:
      clause 0:

def $MAXIMUM_GOSSIP_CLOCK_DISPARITY:
      clause 0:

def $MESSAGE_DOMAIN_INVALID_SNAPPY:
      clause 0:

def $MESSAGE_DOMAIN_VALID_SNAPPY:
      clause 0:

def $SUBNETS_PER_NODE:
      clause 0:

def $ATTESTATION_SUBNET_COUNT:
      clause 0:

def $ATTESTATION_SUBNET_EXTRA_BITS:
      clause 0:

def $ATTESTATION_SUBNET_PREFIX_BITS:
      clause 0:

def $ATTESTATION_DUE_BPS:
      clause 0:

def $AGGREGATE_DUE_BPS:
      clause 0:

def $INACTIVITY_SCORE_BIAS:
      clause 0:

def $INACTIVITY_SCORE_RECOVERY_RATE:
      clause 0:

def $ALTAIR_FORK_VERSION:
      clause 0:

def $ALTAIR_FORK_EPOCH:
      clause 0:

def $SYNC_MESSAGE_DUE_BPS:
      clause 0:

def $CONTRIBUTION_DUE_BPS:
      clause 0:

def $TERMINAL_TOTAL_DIFFICULTY:
      clause 0:

def $TERMINAL_BLOCK_HASH:
      clause 0:

def $TERMINAL_BLOCK_HASH_ACTIVATION_EPOCH:
      clause 0:

def $BELLATRIX_FORK_VERSION:
      clause 0:

def $BELLATRIX_FORK_EPOCH:
      clause 0:

def $CAPELLA_FORK_VERSION:
      clause 0:

def $CAPELLA_FORK_EPOCH:
      clause 0:

def $ZERO_ROOT:
      clause 0:

def $MAX_RANDOM_BYTE:
      clause 0:

def $VERSIONED_HASH_VERSION_KZG:
      clause 0:

def $MAX_BLOB_COMMITMENTS_PER_BLOCK:
      clause 0:

def $MAX_BLOBS_PER_BLOCK:
      clause 0:

def $MAX_PER_EPOCH_ACTIVATION_CHURN_LIMIT:
      clause 0:

def $bls_verify:

def $bls_fast_aggregate_verify:

def $eth_aggregate_pubkeys:

def $hash_:

def $debug_print_:

def $debug_print_label_:

def $hash_tree_root_beaconBlockHeader:

def $hash_tree_root_beaconBlockBody:

def $hash_tree_root_depositData:

def $hash_tree_root_forkdata:

def $hash_tree_root_beaconState:

def $hash_tree_root_withdrawals:

def $hash_tree_root_beaconBlock:

def $hash_tree_root_roots:

def $hash_tree_root_tx:

def $compute_signing_root_epoch:

def $compute_signing_root_voluntary_exit:

def $compute_signing_root_bls_to_execution_change:

def $compute_signing_root_beaconBlockHeader:

def $compute_signing_root_attestationData:

def $compute_signing_root_depositMessage:

def $compute_signing_root_block_root:

def $compute_signing_root_beaconBlock:

def $is_valid_merkle_branch:

def $integer_square_root:

def $count_occurrences_:

def $pow2:

def $to_set_:

def $sum_:

def $repeat_:

def $sort_:

def $set_intersection_:

def $set_or_append_list_:

def $range:

def $enumerate_:

def $bytes_to_uint64:

def $uint_to_bytes:

def $xor:

def $first_28_bytes:

def $get_first_byte:

def $strip_first_byte:

def $concat_domain:

def $make_withdrawal_credentials_eth1:

def $make_versioned_hash:

def $uint8_to_bytes_le:

def $uint32_to_bytes_le:

def $uint64_to_bytes_le:

def $bytes32_prefix8:

def $bytes8_to_uint64_le:

def $shr_uint64:

def $and_uint64:

def $bytes32_to_bytes1_list:

def $bytes1_to_uint64:

def $concat_bytes:

def $bytes32_to_bytes:

def $bytes4_to_bytes:

def $extract_execution_address:

def $filter_list_:
      clause 0:
   0:  360/156483     -- if X*{X <- X*} matches []
       360          []
      clause 1:
   1: 156483/####     -- if X*{X <- X*} matches _ :: _
   2: 156483         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
   3: 28093/128390     -- if ($cond(X_h) = true)
      28093          X_h :: $filter_list_<X>(X_t*{X_t <- X_t*}, $cond)
      clause 2:
   4: 128390/####     -- if X*{X <- X*} matches _ :: _
   5: 128390         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
   6: 128390/####     -- if ($cond(X_h) = false)
      128390          $filter_list_<X>(X_t*{X_t <- X_t*}, $cond)

def $filter_list_2_:
      clause 0:
   7: 23408/7384929     -- if X*{X <- X*} matches []
      23408          []
      clause 1:
   8: 7384929/####     -- if X*{X <- X*} matches _ :: _
   9: 7384929         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
  10: 5396617/1988312     -- if ($cond(X_h, Y) = true)
      5396617          X_h :: $filter_list_2_<X, Y>(X_t*{X_t <- X_t*}, Y, $cond)
      clause 2:
  11: 1988312/####     -- if X*{X <- X*} matches _ :: _
  12: 1988312         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
  13: 1988312/####     -- if ($cond(X_h, Y) = false)
      1988312          $filter_list_2_<X, Y>(X_t*{X_t <- X_t*}, Y, $cond)

def $fold_:
      clause 0:
  14: 9697/868783     -- if Y*{Y <- Y*} matches []
      9697          X
      clause 1:
  15: 868783/####     -- if Y*{Y <- Y*} matches _ :: _
  16: 868783         -- let Y_h :: Y_t*{Y_t <- Y_t*} = Y*{Y <- Y*}
      868783          $fold_<X, Y>($fold_func, $fold_func(X, Y_h), Y_t*{Y_t <- Y_t*})

def $has_flag:
      clause 0:
  17: ####/1256     -- if (n >= 8)
      ####          false
      clause 1:
  18:  147/1109     -- if (((participationFlags / $pow2(n)) \ 2) = 1)
       147          true
      clause 2:
  19: 1109         -- otherwise
      1109          false

def $add_flag:
      clause 0:
  20: ####/1016     -- if (n >= 8)
      ####          participationFlags
      clause 1:
  21: ####/1016     -- if (((participationFlags / $pow2(n)) \ 2) = 1)
      ####          participationFlags
      clause 2:
  22: 1016         -- otherwise
      1016          (participationFlags + $pow2(n))

def $max_uint64:
      clause 0:
  23: 406149/399110     -- if (x >= y)
      406149          x
      clause 1:
  24: 399110         -- otherwise
      399110          y

def $min_uint64:
      clause 0:
  25:  638/1512     -- if (x <= y)
       638          x
      clause 1:
  26: 1512         -- otherwise
      1512          y

def $eth_fast_aggregate_verify:
      clause 0:
  27:  535/  18     -- if (|blsPubkey*{blsPubkey <- blsPubkey*}| = 0)
  28:  535/####     -- if (blsSignature = $G2_POINT_AT_INFINITY)
       535          true
      clause 1:
  29:   18         -- otherwise
        18          $bls_fast_aggregate_verify(blsPubkey*{blsPubkey <- blsPubkey*}, bytes32_msg, blsSignature)

def $ee_verify_and_notify_new_payload:

def $index_of_pubkey:
      clause 0:
  30: 19256958/####     -- if (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*} matches _ :: _
  31: 19256958         -- let (vid_h, blsPubkey_h) :: (vid_t, blsPubkey_t)*{blsPubkey_t <- blsPubkey_t*, vid_t <- vid_t*} = (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*}
  32: 144004/19112954     -- if (blsPubkey_h = blsPubkey_committee)
      144004          vid_h
      clause 1:
  33: 19112954/####     -- if (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*} matches _ :: _
  34: 19112954         -- let (vid_h, blsPubkey_h) :: (vid_t, blsPubkey_t)*{blsPubkey_t <- blsPubkey_t*, vid_t <- vid_t*} = (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*}
  35: 19112954         -- otherwise
      19112954          $index_of_pubkey((vid_t, blsPubkey_t)*{blsPubkey_t <- blsPubkey_t*, vid_t <- vid_t*}, blsPubkey_committee)

def $kzg_commitment_to_versioned_hash:
      clause 0:
  36: ####         -- let bytes32_hash = $hash_<kzgCommitment>(kzgCommitment_in)
  37: ####         -- let bytes31_tail = $strip_first_byte(bytes32_hash)
  38: ####         -- let bytes1_version = $VERSIONED_HASH_VERSION_KZG
  39: ####         -- let bytes32_result = $make_versioned_hash(bytes1_version, bytes31_tail)
  40: ####         -- let versionedHash_result = bytes32_result
      ####          versionedHash_result

def $compute_epoch_at_slot:
      clause 0:

def $compute_start_slot_at_epoch:
      clause 0:

def $get_current_epoch:
      clause 0:

def $get_previous_epoch:
      clause 0:
  41: ####/5648     -- if ($get_current_epoch(state) = $GENESIS_EPOCH)
      ####          $GENESIS_EPOCH
      clause 1:
  42: 5648         -- otherwise
      5648          ($get_current_epoch(state) - 1)

def $compute_time_at_slot:
      clause 0:

def $get_prev_slot:
      clause 0:
  43: ####/ 553     -- if (slot = 0)
      ####          0
      clause 1:
  44:  553         -- otherwise
       553          (slot - 1)

def $compute_fork_data_root:
      clause 0:

def $compute_domain:
      clause 0:
  45: 1602/  39     -- if version'?{version' <- version'?} matches (_)
  46: 1602         -- let ?(version) = version'?{version' <- version'?}
  47: 1602/####     -- if root'?{root' <- root'?} matches (_)
  48: 1602         -- let ?(root) = root'?{root' <- root'?}
      1602          $concat_domain(domainType, $first_28_bytes($compute_fork_data_root(version, root)))
      clause 1:
  49:   39/####     -- if version?{version <- version?} matches ()
  50:   20/  19     -- if root?{root <- root?} matches ()
        20          $concat_domain(domainType, $first_28_bytes($compute_fork_data_root($GENESIS_FORK_VERSION, $ZERO_ROOT)))
      clause 2:
  51: ####/  19     -- if version'?{version' <- version'?} matches (_)
  52: ####         -- let ?(version) = version'?{version' <- version'?}
  53: ####/####     -- if root?{root <- root?} matches ()
      ####          $concat_domain(domainType, $first_28_bytes($compute_fork_data_root(version, $ZERO_ROOT)))
      clause 3:
  54:   19/####     -- if version?{version <- version?} matches ()
  55:   19/####     -- if root'?{root' <- root'?} matches (_)
  56:   19         -- let ?(root) = root'?{root' <- root'?}
        19          $concat_domain(domainType, $first_28_bytes($compute_fork_data_root($GENESIS_FORK_VERSION, root)))

def $get_domain:
      clause 0:
  57:  562/1584     -- if epoch?{epoch <- epoch?} matches ()
  58:  562         -- let epoch_cur = $get_current_epoch(state)
  59: ####/ 562     -- if (epoch_cur < state.FORK.EPOCH)
      ####          $compute_domain(domainType, ?(state.FORK.PREVIOUS_VERSION), ?(state.GENESIS_VALIDATORS_ROOT))
      clause 1:
  60:  562/1584     -- if epoch?{epoch <- epoch?} matches ()
  61:  562         -- let epoch_cur = $get_current_epoch(state)
  62:  562         -- otherwise
       562          $compute_domain(domainType, ?(state.FORK.CURRENT_VERSION), ?(state.GENESIS_VALIDATORS_ROOT))
      clause 2:
  63: 1584/####     -- if epoch'?{epoch' <- epoch'?} matches (_)
  64: 1584         -- let ?(epoch) = epoch'?{epoch' <- epoch'?}
  65: ####/1584     -- if (epoch < state.FORK.EPOCH)
      ####          $compute_domain(domainType, ?(state.FORK.PREVIOUS_VERSION), ?(state.GENESIS_VALIDATORS_ROOT))
      clause 3:
  66: 1584/####     -- if epoch'?{epoch' <- epoch'?} matches (_)
  67: 1584         -- let ?(epoch) = epoch'?{epoch' <- epoch'?}
  68: 1584         -- otherwise
      1584          $compute_domain(domainType, ?(state.FORK.CURRENT_VERSION), ?(state.GENESIS_VALIDATORS_ROOT))

def $verify_block_signature:
      clause 0:
  69: ####         -- let validator_proposer = state.VALIDATORS[signedBeaconBlock.MESSAGE.PROPOSER_INDEX]
  70: ####         -- let root_signing = $compute_signing_root_beaconBlock(signedBeaconBlock.MESSAGE, $get_domain(state, $DOMAIN_BEACON_PROPOSER, ?()))
  71: ####/####     -- if $bls_verify(validator_proposer.PUBKEY, root_signing, signedBeaconBlock.SIGNATURE)
      ####          true
      clause 1:
  72: ####         -- otherwise
      ####          false

def $compute_activation_exit_epoch:
      clause 0:

def $is_active_validator:
      clause 0:
  73: 345911/3828     -- if (validator.ACTIVATION_EPOCH <= epoch)
  74: 342725/3186     -- if (epoch < validator.EXIT_EPOCH)
      342725          true
      clause 1:
  75: 7014         -- otherwise
      7014          false

def $is_eligible_for_activation_queue:
      clause 0:
  76:  112/11520     -- if (validator.ACTIVATION_ELIGIBILITY_EPOCH = $FAR_FUTURE_EPOCH)
  77:  112/####     -- if (validator.EFFECTIVE_BALANCE = $MAX_EFFECTIVE_BALANCE)
       112          true
      clause 1:
  78: 11520         -- otherwise
      11520          false

def $is_eligible_for_activation:
      clause 0:
  79: 11520/ 112     -- if (validator.ACTIVATION_ELIGIBILITY_EPOCH <= state.FINALIZED_CHECKPOINT.EPOCH)
  80:  368/11152     -- if (validator.ACTIVATION_EPOCH = $FAR_FUTURE_EPOCH)
       368          true
      clause 1:
  81: 11264         -- otherwise
      11264          false

def $is_slashable_validator:
      clause 0:
  82:  224/  12     -- if ~validator.SLASHED
  83:  224/####     -- if (validator.ACTIVATION_EPOCH <= epoch)
  84:  224/####     -- if (epoch < validator.WITHDRAWABLE_EPOCH)
       224          true
      clause 1:
  85:   12         -- otherwise
        12          false

def $has_eth1_withdrawal_credential:
      clause 0:

def $is_fully_withdrawable_validator:
      clause 0:
  86:  146/147882     -- if ($has_eth1_withdrawal_credential(validator) = true)
  87:   29/ 117     -- if (validator.WITHDRAWABLE_EPOCH <= epoch)
  88:   28/   1     -- if (balance > 0)
        28          true
      clause 1:
  89: 148000         -- otherwise
      148000          false

def $is_partially_withdrawable_validator:
      clause 0:
  90:  118/147882     -- if ($has_eth1_withdrawal_credential(validator) = true)
  91:   79/  39     -- if (validator.EFFECTIVE_BALANCE = $MAX_EFFECTIVE_BALANCE)
  92:   78/   1     -- if (balance > $MAX_EFFECTIVE_BALANCE)
        78          true
      clause 1:
  93: 147922         -- otherwise
      147922          false

def $is_active_validator_enumerated:
      clause 0:

def $get_active_validator_indices:
      clause 0:
  94: 10493         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
  95: 10493         -- let (vid_active, _validator)*{_validator <- _validator*, vid_active <- vid_active*} = $filter_list_2_<(validatorIndex, validator), epoch>((vid, validator)*{validator <- validator*, vid <- vid*}, epoch, $is_active_validator_enumerated)
      10493          vid_active*{vid_active <- vid_active*}

def $get_validator_churn_limit:
      clause 0:
  96:  396         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, $get_current_epoch(state))
       396          $max_uint64($MIN_PER_EPOCH_CHURN_LIMIT, (|vid_active*{vid_active <- vid_active*}| / $CHURN_LIMIT_QUOTIENT))

def $get_validator_from_deposit:
      clause 0:
  97:  206         -- let balance_effective = $min_uint64((uint64_amount - (uint64_amount \ $EFFECTIVE_BALANCE_INCREMENT)), $MAX_EFFECTIVE_BALANCE)
       206          {PUBKEY blsPubkey, WITHDRAWAL_CREDENTIALS bytes32_withdrawal, EFFECTIVE_BALANCE balance_effective, SLASHED false, ACTIVATION_ELIGIBILITY_EPOCH $FAR_FUTURE_EPOCH, ACTIVATION_EPOCH $FAR_FUTURE_EPOCH, EXIT_EPOCH $FAR_FUTURE_EPOCH, WITHDRAWABLE_EPOCH $FAR_FUTURE_EPOCH}

relation AddValidatorToRegistry:
      rule :
  98:  206         -- let vid_new = |state.VALIDATORS|
  99:  206         -- let validator = $get_validator_from_deposit(blsPubkey, bytes32_wc, uint64_amount)
 100:  206         -- let validator_updated*{validator_updated <- validator_updated*} = $set_or_append_list_<validator>(state.VALIDATORS, vid_new, validator)
 101:  206         -- let balance_updated*{balance_updated <- balance_updated*} = $set_or_append_list_<gwei>(state.BALANCES, vid_new, uint64_amount)
 102:  206         -- let participationFlags_prev_updated*{participationFlags_prev_updated <- participationFlags_prev_updated*} = $set_or_append_list_<participationFlags>(state.PREVIOUS_EPOCH_PARTICIPATION, vid_new, 0)
 103:  206         -- let participationFlags_cur_updated*{participationFlags_cur_updated <- participationFlags_cur_updated*} = $set_or_append_list_<participationFlags>(state.CURRENT_EPOCH_PARTICIPATION, vid_new, 0)
 104:  206         -- let uint64_inactivity_updated*{uint64_inactivity_updated <- uint64_inactivity_updated*} = $set_or_append_list_<uint64>(state.INACTIVITY_SCORES, vid_new, 0)
       206          state .VALIDATORS++{ blsPubkey bytes32_wc uint64_amount }~> state[VALIDATORS = validator_updated*{validator_updated <- validator_updated*}][BALANCES = balance_updated*{balance_updated <- balance_updated*}][PREVIOUS_EPOCH_PARTICIPATION = participationFlags_prev_updated*{participationFlags_prev_updated <- participationFlags_prev_updated*}][CURRENT_EPOCH_PARTICIPATION = participationFlags_cur_updated*{participationFlags_cur_updated <- participationFlags_cur_updated*}][INACTIVITY_SCORES = uint64_inactivity_updated*{uint64_inactivity_updated <- uint64_inactivity_updated*}]

def $is_eligible_validator_enumerated:
      clause 0:

def $get_eligible_validator_indices:
      clause 0:
 105:   44         -- let epoch_prev = $get_previous_epoch(state)
 106:   44         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 107:   44         -- let (vid_eligible, _validator)*{_validator <- _validator*, vid_eligible <- vid_eligible*} = $filter_list_2_<(validatorIndex, validator), epoch>((vid, validator)*{validator <- validator*, vid <- vid*}, epoch_prev, $is_eligible_validator_enumerated)
        44          vid_eligible*{vid_eligible <- vid_eligible*}

def $has_flag_for_index:
      clause 0:

def $is_unslashed:
      clause 0:
 108: 1083581/####     -- if (vid < |state.VALIDATORS|)
 109: 1083581         -- let validator = state.VALIDATORS[vid]
 110: 875669/207912     -- if (validator.SLASHED = false)
      875669          true
      clause 1:
 111: 207912         -- otherwise
      207912          false

def $get_unslashed_participating_indices:
      clause 0:
 112:   27/5213     -- if ($get_current_epoch(state) = epoch)
 113:   27         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch)
 114:   27         -- let vid_participating*{vid_participating <- vid_participating*} = $filter_list_2_<validatorIndex, (participationFlags*, validatorIndex)>(vid_active*{vid_active <- vid_active*}, (state.CURRENT_EPOCH_PARTICIPATION, vid_flag), $has_flag_for_index)
 115:   27         -- let vid_unslashed*{vid_unslashed <- vid_unslashed*} = $filter_list_2_<validatorIndex, beaconState>(vid_participating*{vid_participating <- vid_participating*}, state, $is_unslashed)
        27          $to_set_<validatorIndex>(vid_unslashed*{vid_unslashed <- vid_unslashed*})
      clause 1:
 116: 5213/####     -- if ($get_previous_epoch(state) = epoch)
 117: 5213         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch)
 118: 5213         -- let vid_participating*{vid_participating <- vid_participating*} = $filter_list_2_<validatorIndex, (participationFlags*, nat)>(vid_active*{vid_active <- vid_active*}, (state.PREVIOUS_EPOCH_PARTICIPATION, vid_flag), $has_flag_for_index)
 119: 5213         -- let vid_unslashed*{vid_unslashed <- vid_unslashed*} = $filter_list_2_<validatorIndex, beaconState>(vid_participating*{vid_participating <- vid_participating*}, state, $is_unslashed)
      5213          $to_set_<validatorIndex>(vid_unslashed*{vid_unslashed <- vid_unslashed*})

def $get_validator_activation_churn_limit:
      clause 0:
 120:   36         -- let uint64_churn_limit = $get_validator_churn_limit(state)
 121:   36         -- let uint64_activation_churn_limit = $min_uint64($MAX_PER_EPOCH_ACTIVATION_CHURN_LIMIT, uint64_churn_limit)
        36          uint64_activation_churn_limit

def $get_block_root_at_slot:
      clause 0:
 122: 2804/####     -- if (slot < state.SLOT)
 123: 2804/####     -- if (state.SLOT <= (slot + $SLOTS_PER_HISTORICAL_ROOT))
 124: 2804         -- let root = state.BLOCK_ROOTS[(slot \ $SLOTS_PER_HISTORICAL_ROOT)]
      2804          root

def $get_block_root:
      clause 0:

def $get_randao_mix:
      clause 0:

def $get_finality_delay:
      clause 0:
 125: 5128/####     -- if ($get_previous_epoch(state) >= state.FINALIZED_CHECKPOINT.EPOCH)
      5128          ($get_previous_epoch(state) - state.FINALIZED_CHECKPOINT.EPOCH)

def $is_in_inactivity_leak:
      clause 0:

def $choose_index:
      clause 0:
 126: 410819/407562     -- if (nat_bit = 1)
      410819          uint64_flip
      clause 1:
 127: 407562         -- otherwise
      407562          i

def $apply_shuffle_round:
      clause 0:
 128: 840330         -- let bytes_seed = $bytes32_to_bytes(seed)
 129: 840330         -- let bytes_round8 = $uint8_to_bytes_le((uint64_round \ 256))
 130: 840330         -- let bytes32_round_hash = $hash_<bytes>($concat_bytes(bytes_seed, bytes_round8))
 131: 840330         -- let bytes8_prefix = $bytes32_prefix8(bytes32_round_hash)
 132: 840330         -- let uint64_pivot_raw = $bytes8_to_uint64_le(bytes8_prefix)
 133: 840330         -- let uint64_pivot = (uint64_pivot_raw \ i_count)
 134: 840330         -- let uint64_flip = (((uint64_pivot + i_count) - i) \ i_count)
 135: 840330         -- let uint64_position = $max_uint64(i, uint64_flip)
 136: 840330         -- let bytes_bucket = $uint32_to_bytes_le((uint64_position / 256))
 137: 840330         -- let bytes32_source = $hash_<bytes>($concat_bytes($concat_bytes(bytes_seed, bytes_round8), bytes_bucket))
 138: 840330         -- let uint64_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_source)[((uint64_position \ 256) / 8)])
 139: 840330         -- let nat_bit = ($and_uint64($shr_uint64(uint64_byte, (uint64_position \ 8)), 1) \ 2)
 140: 840330         -- let i_out = $choose_index(i, uint64_flip, nat_bit)
      840330          i_out

def $compute_shuffled_index:
      clause 0:
 141: 9337         -- let uint64_round*{uint64_round <- uint64_round*} = $range($SHUFFLE_ROUND_COUNT)
 142: 9337         -- let uint64_round_first = uint64_round*{uint64_round <- uint64_round*}[0]
 143: 9337         -- let uint64_round_last = uint64_round*{uint64_round <- uint64_round*}[($SHUFFLE_ROUND_COUNT - 1)]
 144: 9337         -- let i_shuffled = $fold_<uint64, (bytes32, uint64, uint64)>($apply_shuffle_round, i, (seed, i_count, uint64_round)*{uint64_round <- uint64_round*})
      9337          i_shuffled

def $compute_proposer_index_loop:
      clause 0:
 145: 2959         -- let vid_candidate = vid*{vid <- vid*}[$compute_shuffled_index((i \ uint64_total), uint64_total, seed)]
 146: 2959         -- let bytes32_h = $hash_<bytes>($concat_bytes($bytes32_to_bytes(seed), $uint64_to_bytes_le((i / 32))))
 147: 2959         -- let uint64_random_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_h)[(i \ 32)])
 148: 2959         -- let balance_effective = state.VALIDATORS[vid_candidate].EFFECTIVE_BALANCE
 149: 2959/####     -- if (balance_effective <= ($UINT64_MAX / $MAX_RANDOM_BYTE))
 150: 2868/  91     -- if ((balance_effective * $MAX_RANDOM_BYTE) >= ($MAX_EFFECTIVE_BALANCE * uint64_random_byte))
      2868          vid_candidate
      clause 1:
 151:   91         -- otherwise
        91          $compute_proposer_index_loop(state, vid*{vid <- vid*}, seed, uint64_total, (i + 1))

def $compute_proposer_index:
      clause 0:
 152: 2868/####     -- if (|vid*{vid <- vid*}| > 0)
      2868          $compute_proposer_index_loop(state, vid*{vid <- vid*}, seed, |vid*{vid <- vid*}|, 0)

def $get_seed:
      clause 0:
 153: 3739         -- let bytes32_mix = $get_randao_mix(state, (((epoch + $EPOCHS_PER_HISTORICAL_VECTOR) - $MIN_SEED_LOOKAHEAD) - 1))
      3739          $hash_<bytes>($concat_bytes($concat_bytes($bytes4_to_bytes(domainType), $uint64_to_bytes_le(epoch)), $bytes32_to_bytes(bytes32_mix)))

def $get_beacon_proposer_index:
      clause 0:
 154: 2868         -- let epoch_cur = $get_current_epoch(state)
 155: 2868         -- let seed = $hash_<bytes>($concat_bytes($bytes32_to_bytes($get_seed(state, epoch_cur, $DOMAIN_BEACON_PROPOSER)), $uint64_to_bytes_le(state.SLOT)))
 156: 2868         -- let vid*{vid <- vid*} = $get_active_validator_indices(state, epoch_cur)
      2868          $compute_proposer_index(state, vid*{vid <- vid*}, seed)

relation IncreaseBalance:
      rule :
 157: 52987/####     -- if (vid < |state.BALANCES|)
 158: 52987         -- let balance = state.BALANCES[vid]
      52987          state .BALANCES[ vid ]+ delta ~> state[BALANCES[vid] = (balance + delta)]

relation DecreaseBalance:
      rule zero:
 159: 312765/####     -- if (vid < |state.BALANCES|)
 160: 312765         -- let balance = state.BALANCES[vid]
 161:   61/312704     -- if (delta > balance)
        61          state .BALANCES[ vid ]- delta ~> state[BALANCES[vid] = 0]
      rule remains:
 162: 312704/####     -- if (vid < |state.BALANCES|)
 163: 312704         -- let balance = state.BALANCES[vid]
 164: 312704/####     -- if (delta <= balance)
      312704          state .BALANCES[ vid ]- delta ~> state[BALANCES[vid] = (balance - delta)]

def $exit_epoch_not_far_future:
      clause 0:
 165: 28093/256780     -- if (validator.EXIT_EPOCH =/= $FAR_FUTURE_EPOCH)
      28093          true
      clause 1:
 166: 256780         -- otherwise
      256780          false

def $exit_epoch_eq:
      clause 0:
 167:  839/54508     -- if (validator.EXIT_EPOCH = epoch)
       839          true
      clause 1:
 168: 54508         -- otherwise
      54508          false

def $fold_max_epoch:
      clause 0:

def $select_epoch_exit_queue:
      clause 0:
 169:   80/ 280     -- if (uint64_churn >= uint64_limit)
        80          (epoch_exit_queue_0 + 1)
      clause 1:
 170:  280         -- otherwise
       280          epoch_exit_queue_0

relation InitiateValidatorExit:
      rule skip:
 171:  462/####     -- if (vid < |state.VALIDATORS|)
 172:  462         -- let validator = state.VALIDATORS[vid]
 173:  102/ 360     -- if (validator.EXIT_EPOCH =/= $FAR_FUTURE_EPOCH)
       102          state .VALIDATORS[ vid ]~> state
      rule main:
 174:  360/####     -- if (vid < |state.VALIDATORS|)
 175:  360         -- let validator = state.VALIDATORS[vid]
 176:  360/####     -- if (validator.EXIT_EPOCH = $FAR_FUTURE_EPOCH)
 177:  360         -- let validator_exit*{validator_exit <- validator_exit*} = $filter_list_<validator>(state.VALIDATORS, $exit_epoch_not_far_future)
 178: ####         -- (let epoch_exit = validator_exit.EXIT_EPOCH)*{epoch_exit <- epoch_exit*, validator_exit <- validator_exit*}
 179:  360         -- let epoch_exit_queue_0 = $fold_<epoch, epoch>($fold_max_epoch, 0, epoch_exit*{epoch_exit <- epoch_exit*} ++ [$compute_activation_exit_epoch($get_current_epoch(state))])
 180:  360         -- let validator_exit_queue*{validator_exit_queue <- validator_exit_queue*} = $filter_list_2_<validator, epoch>(validator_exit*{validator_exit <- validator_exit*}, epoch_exit_queue_0, $exit_epoch_eq)
 181:  360         -- let uint64_exit_queue_churn = |validator_exit_queue*{validator_exit_queue <- validator_exit_queue*}|
 182:  360         -- let uint64_churn_limit = $get_validator_churn_limit(state)
 183:  360         -- let epoch_exit_queue = $select_epoch_exit_queue(epoch_exit_queue_0, uint64_exit_queue_churn, uint64_churn_limit)
 184:  360/####     -- if (epoch_exit_queue <= ($UINT64_MAX - $MIN_VALIDATOR_WITHDRAWABILITY_DELAY))
 185:  360         -- let epoch_withdrawable = (epoch_exit_queue + $MIN_VALIDATOR_WITHDRAWABILITY_DELAY)
       360          state .VALIDATORS[ vid ]~> state[VALIDATORS[vid] = validator[EXIT_EPOCH = epoch_exit_queue][WITHDRAWABLE_EPOCH = epoch_withdrawable]]

relation SlashValidator:
      rule no_whistleblower:
 186:  224/####     -- if validatorIndex?{validatorIndex <- validatorIndex?} matches ()
 187:  224/####     -- if (vid_slashed < |state.VALIDATORS|)
 188:  224         -- let epoch = $get_current_epoch(state)
 189:  224/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid_slashed ]~> state_init
 190:  224         -- let validator = state_init.VALIDATORS[vid_slashed]
 191:  224         -- let epoch_withdrawable = $max_uint64(validator.WITHDRAWABLE_EPOCH, (epoch + $EPOCHS_PER_SLASHINGS_VECTOR))
 192:  224         -- let state_1 = state_init[VALIDATORS[vid_slashed] = validator[SLASHED = true][WITHDRAWABLE_EPOCH = epoch_withdrawable]]
 193:  224         -- let state_2 = state_1[SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] = (state_1.SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] + validator.EFFECTIVE_BALANCE)]
 194:  224         -- let penalty_slashing = (validator.EFFECTIVE_BALANCE / $MIN_SLASHING_PENALTY_QUOTIENT_BELLATRIX)
 195:  224/####     -- rel DecreaseBalance: state_2 .BALANCES[ vid_slashed ]- penalty_slashing ~> state_penalized
 196:  224         -- let vid_proposer = $get_beacon_proposer_index(state_penalized)
 197:  224         -- let reward_whistleblower = (validator.EFFECTIVE_BALANCE / $WHISTLEBLOWER_REWARD_QUOTIENT)
 198:  224         -- let gwei_prop_num = (reward_whistleblower * $PROPOSER_WEIGHT)
 199:  224         -- let reward_proposer = (gwei_prop_num / $WEIGHT_DENOMINATOR)
 200:  224/####     -- rel IncreaseBalance: state_penalized .BALANCES[ vid_proposer ]+ reward_proposer ~> state_rewarded_proposer
 201:  224         -- let reward_whistleblower_rest = (reward_whistleblower - reward_proposer)
 202:  224/####     -- rel IncreaseBalance: state_rewarded_proposer .BALANCES[ vid_proposer ]+ reward_whistleblower_rest ~> state_out
       224          state SLASH[ vid_slashed ]BY validatorIndex?{validatorIndex <- validatorIndex?} ~> state_out
      rule whistleblower:
 203: ####/####     -- if validatorIndex?{validatorIndex <- validatorIndex?} matches (_)
 204: ####         -- let ?(vid_whistleblower) = validatorIndex?{validatorIndex <- validatorIndex?}
 205: ####/####     -- if (vid_slashed < |state.VALIDATORS|)
 206: ####         -- let epoch = $get_current_epoch(state)
 207: ####/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid_slashed ]~> state_init
 208: ####         -- let validator = state_init.VALIDATORS[vid_slashed]
 209: ####         -- let epoch_withdrawable = $max_uint64(validator.WITHDRAWABLE_EPOCH, (epoch + $EPOCHS_PER_SLASHINGS_VECTOR))
 210: ####         -- let state_1 = state_init[VALIDATORS[vid_slashed] = validator[SLASHED = true][WITHDRAWABLE_EPOCH = epoch_withdrawable]]
 211: ####         -- let state_2 = state_1[SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] = (state_1.SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] + validator.EFFECTIVE_BALANCE)]
 212: ####         -- let penalty_slashing = (validator.EFFECTIVE_BALANCE / $MIN_SLASHING_PENALTY_QUOTIENT_BELLATRIX)
 213: ####/####     -- rel DecreaseBalance: state_2 .BALANCES[ vid_slashed ]- penalty_slashing ~> state_penalized
 214: ####         -- let vid_proposer = $get_beacon_proposer_index(state_penalized)
 215: ####         -- let reward_whistleblower = (validator.EFFECTIVE_BALANCE / $WHISTLEBLOWER_REWARD_QUOTIENT)
 216: ####         -- let gwei_prop_num = (reward_whistleblower * $PROPOSER_WEIGHT)
 217: ####         -- let reward_proposer = (gwei_prop_num / $WEIGHT_DENOMINATOR)
 218: ####/####     -- rel IncreaseBalance: state_penalized .BALANCES[ vid_proposer ]+ reward_proposer ~> state_rewarded_proposer
 219: ####         -- let reward_whistleblower_rest = (reward_whistleblower - reward_proposer)
 220: ####/####     -- rel IncreaseBalance: state_rewarded_proposer .BALANCES[ vid_whistleblower ]+ reward_whistleblower_rest ~> state_out
      ####          state SLASH[ vid_slashed ]BY validatorIndex?{validatorIndex <- validatorIndex?} ~> state_out

relation ApplyDeposit:
      rule new_pubkey:
 221:  209         -- let validator*{validator <- validator*} = state.VALIDATORS
 222: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 223:  206/   3     -- if ~blsPubkey <- blsPubkey_existing*{blsPubkey_existing <- blsPubkey_existing*}
 224:  206         -- let depositMessage = {PUBKEY blsPubkey, WITHDRAWAL_CREDENTIALS bytes32_wc, AMOUNT gwei_amount}
 225:  206         -- let domain = $compute_domain($DOMAIN_DEPOSIT, ?(), ?())
 226:  206/####     -- if $bls_verify(blsPubkey, $compute_signing_root_depositMessage(depositMessage, domain), blsSignature)
 227:  206/####     -- rel AddValidatorToRegistry: state .VALIDATORS++{ blsPubkey bytes32_wc gwei_amount }~> state_out
       206          state .VALIDATORS++{ blsPubkey bytes32_wc gwei_amount }SIGNED blsSignature ~> state_out
      rule new_pubkey_invalid_sig:
 228:    3         -- let validator*{validator <- validator*} = state.VALIDATORS
 229: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 230: ####/   3     -- if ~blsPubkey <- blsPubkey_existing*{blsPubkey_existing <- blsPubkey_existing*}
 231: ####         -- let depositMessage = {PUBKEY blsPubkey, WITHDRAWAL_CREDENTIALS bytes32_wc, AMOUNT gwei_amount}
 232: ####         -- let domain = $compute_domain($DOMAIN_DEPOSIT, ?(), ?())
 233: ####/####     -- if ~$bls_verify(blsPubkey, $compute_signing_root_depositMessage(depositMessage, domain), blsSignature)
      ####          state .VALIDATORS++{ blsPubkey bytes32_wc gwei_amount }SIGNED blsSignature ~> state
      rule existing_pubkey:
 234:    3         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 235: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 236:    3/####     -- if blsPubkey <- blsPubkey_existing*{blsPubkey_existing <- blsPubkey_existing*}
 237:    3         -- let vid_found = $index_of_pubkey((vid, blsPubkey_existing)*{blsPubkey_existing <- blsPubkey_existing*, vid <- vid*}, blsPubkey)
 238:    3/####     -- rel IncreaseBalance: state .BALANCES[ vid_found ]+ gwei_amount ~> state_out
         3          state .VALIDATORS++{ blsPubkey bytes32_wc gwei_amount }SIGNED blsSignature ~> state_out

def $compute_committee:
      clause 0:
 239:  872         -- let i_start = ((|vid*{vid <- vid*}| * i) / count)
 240:  872         -- let i_end = ((|vid*{vid <- vid*}| * (i + 1)) / count)
 241:  872         -- let n*{n <- n*} = $range((i_end - i_start))
 242: ####         -- (let i_range = (n + i_start))*{i_range <- i_range*, n <- n*}
 243: ####         -- (let i_shuffled = $compute_shuffled_index(i_range, |vid*{vid <- vid*}|, seed))*{i_range <- i_range*, i_shuffled <- i_shuffled*}
 244: ####         -- (let vid_committee = vid*{vid <- vid*}[i_shuffled])*{i_shuffled <- i_shuffled*, vid_committee <- vid_committee*}
      ####          vid_committee*{vid_committee <- vid_committee*}

def $get_committee_count_per_slot:
      clause 0:

def $get_beacon_committee:
      clause 0:
 245: 1406         -- let epoch = $compute_epoch_at_slot(slot)
 246: 1406         -- let count_committees_per_slot = $get_committee_count_per_slot(state, epoch)
      1406          $compute_committee($get_active_validator_indices(state, epoch), $get_seed(state, epoch, $DOMAIN_BEACON_ATTESTER), (((slot \ $SLOTS_PER_EPOCH) * count_committees_per_slot) + committeeIndex), (count_committees_per_slot * $SLOTS_PER_EPOCH))

def $bit_is_one_at_index:
      clause 0:
 247: 14575/####     -- if (i < |bit*{bit <- bit*}|)
      14575          bit*{bit <- bit*}[i]

def $get_attesting_indices:
      clause 0:
 248: 1406         -- let vid_committee*{vid_committee <- vid_committee*} = $get_beacon_committee(state, attestation.DATA.SLOT, attestation.DATA.INDEX)
 249: 1406         -- let (i, vid)*{i <- i*, vid <- vid*} = $enumerate_<validatorIndex>(vid_committee*{vid_committee <- vid_committee*})
 250: 1406         -- let (_nat, vid_attesting)*{_nat <- _nat*, vid_attesting <- vid_attesting*} = $filter_list_2_<(nat, validatorIndex), boolean*>((i, vid)*{i <- i*, vid <- vid*}, attestation.AGGREGATION_BITS, $bit_is_one_at_index)
 251: 1406         -- let vid_set*{vid_set <- vid_set*} = $to_set_<validatorIndex>(vid_attesting*{vid_attesting <- vid_attesting*})
      1406          vid_set*{vid_set <- vid_set*}

def $is_slashable_attestation_data:
      clause 0:

def $is_valid_indexed_attestation:
      clause 0:
 252: ####/1456     -- if (|indexedAttestation.ATTESTING_INDICES| = 0)
      ####          false
      clause 1:
 253: ####/1456     -- if ($sort_<validatorIndex>($to_set_<validatorIndex>(indexedAttestation.ATTESTING_INDICES)) =/= indexedAttestation.ATTESTING_INDICES)
      ####          false
      clause 2:
 254: 1456         -- let vid*{vid <- vid*} = indexedAttestation.ATTESTING_INDICES
 255: 1456/####     -- if (|indexedAttestation.ATTESTING_INDICES| > 0)
 256: 1456/####     -- if ($sort_<validatorIndex>($to_set_<validatorIndex>(indexedAttestation.ATTESTING_INDICES)) = indexedAttestation.ATTESTING_INDICES)
 257: ####         -- (let blsPubkey = state.VALIDATORS[vid].PUBKEY)*{blsPubkey <- blsPubkey*, vid <- vid*}
 258: 1456         -- let domain = $get_domain(state, $DOMAIN_BEACON_ATTESTER, ?(indexedAttestation.DATA.TARGET.EPOCH))
 259: 1456         -- let root_signing = $compute_signing_root_attestationData(indexedAttestation.DATA, domain)
      1456          $bls_fast_aggregate_verify(blsPubkey*{blsPubkey <- blsPubkey*}, root_signing, indexedAttestation.SIGNATURE)

relation SlashIfEligible:
      rule true:
 260:   86/####     -- if (vid < |state.VALIDATORS|)
 261:   76/  10     -- if $is_slashable_validator(state.VALIDATORS[vid], $get_current_epoch(state))
 262:   76/####     -- rel SlashValidator: state SLASH[ vid ]BY ?() ~> state_slashed
        76          state SLASH?[ vid ]~> state_slashed #SLASHED true
      rule false:
 263:   10/####     -- if (vid < |state.VALIDATORS|)
 264:   10/####     -- if ~$is_slashable_validator(state.VALIDATORS[vid], $get_current_epoch(state))
        10          state SLASH?[ vid ]~> state #SLASHED false

relation SlashIfEligible_over_validators:
      rule nil:
 265:   24/  86     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        24          state SLASH?[ validatorIndex*{validatorIndex <- validatorIndex*} ]~> state #SLASHED false
      rule cons:
 266:   86/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 267:   86         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 268:   86/####     -- rel SlashIfEligible: state SLASH?[ vid_h ]~> state_h #SLASHED bool_h
 269:   86/####     -- rel SlashIfEligible_over_validators: state_h SLASH?[ vid_t*{vid_t <- vid_t*} ]~> state_t #SLASHED bool_t
        86          state SLASH?[ validatorIndex*{validatorIndex <- validatorIndex*} ]~> state_t #SLASHED (bool_h \/ bool_t)

def $source_flag_list:
      clause 0:
 270:  309/ 563     -- if (uint64_inclusion_delay <= $integer_square_root($SLOTS_PER_EPOCH))
       309          [$TIMELY_SOURCE_FLAG_INDEX]
      clause 1:
 271:  563         -- otherwise
       563          []

def $target_flag_list:
      clause 0:
      clause 1:
 272: ####         -- otherwise
      ####          []

def $head_flag_list:
      clause 0:
 273:  257/ 615     -- if (uint64_inclusion_delay = $MIN_ATTESTATION_INCLUSION_DELAY)
       257          [$TIMELY_HEAD_FLAG_INDEX]
      clause 1:
 274:  615         -- otherwise
       615          []

def $get_attestation_participation_flag_indices:
      clause 0:
 275:  523/ 883     -- if (attestationData.TARGET.EPOCH = $get_current_epoch(state))
 276:  523         -- let checkpoint_justified = state.CURRENT_JUSTIFIED_CHECKPOINT
 277:  523/####     -- if (attestationData.SOURCE.EPOCH = checkpoint_justified.EPOCH)
 278:  523/####     -- if (attestationData.SOURCE.ROOT = checkpoint_justified.ROOT)
 279:  523         -- let root_target = $get_block_root(state, attestationData.TARGET.EPOCH)
 280:  523         -- let bool_target_root_matches = (attestationData.TARGET.ROOT = root_target)
 281:  523         -- let root_head = $get_block_root_at_slot(state, attestationData.SLOT)
 282:  523         -- let bool_head_root_matches = (attestationData.BEACON_BLOCK_ROOT = root_head)
 283:  523         -- let flag_source*{flag_source <- flag_source*} = $source_flag_list(uint64_inclusion_delay)
 284:  523         -- let flag_target*{flag_target <- flag_target*} = $target_flag_list(bool_target_root_matches)
 285:  523         -- let flag_head*{flag_head <- flag_head*} = $head_flag_list((bool_target_root_matches /\ bool_head_root_matches), uint64_inclusion_delay)
 286:  523         -- let flag_indices*{flag_indices <- flag_indices*} = flag_source*{flag_source <- flag_source*} ++ flag_target*{flag_target <- flag_target*} ++ flag_head*{flag_head <- flag_head*}
       523          flag_indices*{flag_indices <- flag_indices*}
      clause 1:
 287:  883/####     -- if (attestationData.TARGET.EPOCH =/= $get_current_epoch(state))
 288:  883         -- let checkpoint_justified = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 289:  883/####     -- if (attestationData.SOURCE.EPOCH = checkpoint_justified.EPOCH)
 290:  883/####     -- if (attestationData.SOURCE.ROOT = checkpoint_justified.ROOT)
 291:  883         -- let root_target = $get_block_root(state, attestationData.TARGET.EPOCH)
 292:  883         -- let bool_target_root_matches = (attestationData.TARGET.ROOT = root_target)
 293:  883         -- let root_head = $get_block_root_at_slot(state, attestationData.SLOT)
 294:  883         -- let bool_head_root_matches = (attestationData.BEACON_BLOCK_ROOT = root_head)
 295:  883         -- let flag_source*{flag_source <- flag_source*} = $source_flag_list(uint64_inclusion_delay)
 296:  883         -- let flag_target*{flag_target <- flag_target*} = $target_flag_list(bool_target_root_matches)
 297:  883         -- let flag_head*{flag_head <- flag_head*} = $head_flag_list((bool_target_root_matches /\ bool_head_root_matches), uint64_inclusion_delay)
 298:  883         -- let flag_indices*{flag_indices <- flag_indices*} = flag_source*{flag_source <- flag_source*} ++ flag_target*{flag_target <- flag_target*} ++ flag_head*{flag_head <- flag_head*}
       883          flag_indices*{flag_indices <- flag_indices*}

def $next_sync_committee_indices_loop:
      clause 0:
 299:    1/ 512     -- if (|vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}| >= $SYNC_COMMITTEE_SIZE)
         1          vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}
      clause 1:
 300:  512/####     -- if (|vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}| < $SYNC_COMMITTEE_SIZE)
 301:  512         -- let vid_candidate = vid_active*{vid_active <- vid_active*}[$compute_shuffled_index((vid \ count_active), count_active, seed)]
 302:  512         -- let bytes32_h = $hash_<bytes>($concat_bytes($bytes32_to_bytes(seed), $uint_to_bytes((vid / 32))))
 303:  512         -- let uint64_random_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_h)[(vid \ 32)])
 304:  512         -- let balance_effective = state.VALIDATORS[vid_candidate].EFFECTIVE_BALANCE
 305:  512/####     -- if (balance_effective <= ($UINT64_MAX / $MAX_RANDOM_BYTE))
 306:  512/####     -- if ((balance_effective * $MAX_RANDOM_BYTE) >= ($MAX_EFFECTIVE_BALANCE * uint64_random_byte))
       512          $next_sync_committee_indices_loop(state, vid_active*{vid_active <- vid_active*}, seed, count_active, (vid + 1), vid_sync_committee*{vid_sync_committee <- vid_sync_committee*} ++ [vid_candidate])
      clause 2:
 307: ####/####     -- if (|vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}| < $SYNC_COMMITTEE_SIZE)
 308: ####         -- let vid_candidate = vid_active*{vid_active <- vid_active*}[$compute_shuffled_index((vid \ count_active), count_active, seed)]
 309: ####         -- let bytes32_h = $hash_<bytes>($concat_bytes($bytes32_to_bytes(seed), $uint_to_bytes((vid / 32))))
 310: ####         -- let uint64_random_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_h)[(vid \ 32)])
 311: ####         -- let balance_effective = state.VALIDATORS[vid_candidate].EFFECTIVE_BALANCE
 312: ####/####     -- if (balance_effective <= ($UINT64_MAX / $MAX_RANDOM_BYTE))
 313: ####/####     -- if ((balance_effective * $MAX_RANDOM_BYTE) < ($MAX_EFFECTIVE_BALANCE * uint64_random_byte))
      ####          $next_sync_committee_indices_loop(state, vid_active*{vid_active <- vid_active*}, seed, count_active, (vid + 1), vid_sync_committee*{vid_sync_committee <- vid_sync_committee*})

def $get_next_sync_committee_indices:
      clause 0:
 314:    1         -- let epoch_next = ($get_current_epoch(state) + 1)
 315:    1         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch_next)
 316:    1         -- let count_active = |vid_active*{vid_active <- vid_active*}|
 317:    1         -- let seed = $get_seed(state, epoch_next, $DOMAIN_SYNC_COMMITTEE)
 318:    1         -- let vid_out*{vid_out <- vid_out*} = $next_sync_committee_indices_loop(state, vid_active*{vid_active <- vid_active*}, seed, count_active, 0, [])
         1          vid_out*{vid_out <- vid_out*}

def $get_next_sync_committee:
      clause 0:
 319:    1         -- let vid*{vid <- vid*} = $get_next_sync_committee_indices(state)
 320: ####         -- (let blsPubkey = state.VALIDATORS[vid].PUBKEY)*{blsPubkey <- blsPubkey*, vid <- vid*}
 321:    1         -- let blsPubkey_aggregate = $eth_aggregate_pubkeys(blsPubkey*{blsPubkey <- blsPubkey*})
         1          {PUBKEYS blsPubkey*{blsPubkey <- blsPubkey*}, AGGREGATE_PUBKEY blsPubkey_aggregate}

def $get_indexed_attestation:
      clause 0:
 322: 1406         -- let vid_attesting*{vid_attesting <- vid_attesting*} = $get_attesting_indices(state, attestation)
      1406          {ATTESTING_INDICES $sort_<validatorIndex>(vid_attesting*{vid_attesting <- vid_attesting*}), DATA attestation.DATA, SIGNATURE attestation.SIGNATURE}

def $get_total_balance:
      clause 0:
 323: ####         -- (let balance = state.VALIDATORS[vid].EFFECTIVE_BALANCE)*{balance <- balance*, vid <- vid*}
      ####          $max_uint64($EFFECTIVE_BALANCE_INCREMENT, $sum_<gwei>(balance*{balance <- balance*}))

def $get_total_active_balance:
      clause 0:
 324: 1450         -- let epoch_cur = $get_current_epoch(state)
 325: 1450         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch_cur)
 326: 1450         -- let vid_set*{vid_set <- vid_set*} = $to_set_<validatorIndex>(vid_active*{vid_active <- vid_active*})
 327: 1450         -- let gwei_total = $get_total_balance(state, vid_set*{vid_set <- vid_set*})
      1450          gwei_total

def $get_base_reward_per_increment:
      clause 0:

def $get_base_reward:
      clause 0:
 328: 15604/####     -- if (vid < |state.VALIDATORS|)
 329: 15604         -- let uint64_increments = (state.VALIDATORS[vid].EFFECTIVE_BALANCE / $EFFECTIVE_BALANCE_INCREMENT)
 330: 15604         -- let reward = (uint64_increments * $get_base_reward_per_increment(state))
      15604          reward

relation ApplyEffectiveBalance:
      rule main:
 331: 11632         -- let uint64_hyst_inc = ($EFFECTIVE_BALANCE_INCREMENT / $HYSTERESIS_QUOTIENT)
 332: 11632         -- let uint64_down_th = (uint64_hyst_inc * $HYSTERESIS_DOWNWARD_MULTIPLIER)
 333: 11632         -- let uint64_up_th = (uint64_hyst_inc * $HYSTERESIS_UPWARD_MULTIPLIER)
 334: 11632/####     -- if (balance <= ($UINT64_MAX - uint64_down_th))
 335: 11632/####     -- if (validator.EFFECTIVE_BALANCE <= ($UINT64_MAX - uint64_up_th))
 336: 2245/9387     -- if (((balance + uint64_down_th) < validator.EFFECTIVE_BALANCE) \/ ((validator.EFFECTIVE_BALANCE + uint64_up_th) < balance))
      2245          validator .EFFECTIVE_BALANCE balance ~> validator[EFFECTIVE_BALANCE = $min_uint64((balance - (balance \ $EFFECTIVE_BALANCE_INCREMENT)), $MAX_EFFECTIVE_BALANCE)]
      rule skip:
 337: 9387         -- otherwise
      9387          validator .EFFECTIVE_BALANCE _gwei ~> validator

def $inact_penalty_one:
      clause 0:
 338: 9198/ 898     -- if vid <- vid_eligible*{vid_eligible <- vid_eligible*}
 339: 4309/4889     -- if ~vid <- vid_matching*{vid_matching <- vid_matching*}
 340: 4309/####     -- if (vid < |state.VALIDATORS|)
 341: 4309         -- let balance_effective = state.VALIDATORS[vid].EFFECTIVE_BALANCE
 342: 4309/####     -- if (vid < |state.INACTIVITY_SCORES|)
 343: 4309         -- let uint64_score = state.INACTIVITY_SCORES[vid]
 344: 4309         -- let penalty_numerator = (balance_effective * uint64_score)
 345: 4309/####     -- if (penalty_numerator <= $UINT64_MAX)
 346: 4309         -- let penalty_denominator = ($INACTIVITY_SCORE_BIAS * $INACTIVITY_PENALTY_QUOTIENT_BELLATRIX)
 347: 4309         -- let penalty = (penalty_numerator / penalty_denominator)
      4309          penalty
      clause 1:
 348: 5787         -- otherwise
      5787          0

def $get_inactivity_penalty_deltas:
      clause 0:
 349:   30         -- let reward*{reward <- reward*} = $repeat_<gwei>(0, |state.VALIDATORS|)
 350:   30         -- let vid_eligible*{vid_eligible <- vid_eligible*} = $get_eligible_validator_indices(state)
 351:   30         -- let epoch_prev = $get_previous_epoch(state)
 352:   30         -- let vid_matching*{vid_matching <- vid_matching*} = $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, epoch_prev)
 353:   30         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 354: ####         -- (let penalty = $inact_penalty_one(vid, state, vid_matching*{vid_matching <- vid_matching*}, vid_eligible*{vid_eligible <- vid_eligible*}))*{penalty <- penalty*, vid <- vid*}
      ####          (reward*{reward <- reward*}, penalty*{penalty <- penalty*})

def $flag_reward_one_eligible:
      clause 0:
 355: 20958/2370     -- if vid <- vid_eligible*{vid_eligible <- vid_eligible*}
 356: 9176/11782     -- if vid <- vid_unslashed*{vid_unslashed <- vid_unslashed*}
 357: 8417/ 759     -- if ~$is_in_inactivity_leak(state)
 358: 8417         -- let reward_base = $get_base_reward(state, vid)
 359: 8417         -- let reward_numerator = ((reward_base * uint64_weight) * uint64_up_incs)
 360: 8417         -- let reward = (reward_numerator / (uint64_active_incs * $WEIGHT_DENOMINATOR))
      8417          reward
      clause 1:
 361: 14911         -- otherwise
      14911          0

def $flag_penalty_one_eligible:
      clause 0:
 362: 27594/2694     -- if vid <- vid_eligible*{vid_eligible <- vid_eligible*}
 363: 16890/10704     -- if ~vid <- vid_unslashed*{vid_unslashed <- vid_unslashed*}
 364: 10437/6453     -- if (flag_index =/= $TIMELY_HEAD_FLAG_INDEX)
 365: 10437         -- let reward_base = $get_base_reward(state, vid)
 366: 10437         -- let penalty = ((reward_base * uint64_weight) / $WEIGHT_DENOMINATOR)
      10437          penalty
      clause 1:
 367: 19851         -- otherwise
      19851          0

def $get_flag_index_deltas:
      clause 0:
 368:   90         -- let vid_eligible*{vid_eligible <- vid_eligible*} = $to_set_<validatorIndex>($get_eligible_validator_indices(state))
 369:   90         -- let epoch_prev = $get_previous_epoch(state)
 370:   90         -- let vid_unslashed*{vid_unslashed <- vid_unslashed*} = $get_unslashed_participating_indices(state, flag_index, epoch_prev)
 371:   90         -- let uint64_weight = $PARTICIPATION_FLAG_WEIGHTS[flag_index]
 372:   90         -- let balance_unslashed_participating = $get_total_balance(state, vid_unslashed*{vid_unslashed <- vid_unslashed*})
 373:   90         -- let uint64_unslashed_increments = (balance_unslashed_participating / $EFFECTIVE_BALANCE_INCREMENT)
 374:   90         -- let uint64_active_increments = ($get_total_active_balance(state) / $EFFECTIVE_BALANCE_INCREMENT)
 375:   90         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 376: ####         -- (let reward = $flag_reward_one_eligible(vid, state, vid_unslashed*{vid_unslashed <- vid_unslashed*}, vid_eligible*{vid_eligible <- vid_eligible*}, uint64_weight, uint64_unslashed_increments, uint64_active_increments))*{reward <- reward*, vid <- vid*}
 377: ####         -- (let penalty = $flag_penalty_one_eligible(vid, state, vid_unslashed*{vid_unslashed <- vid_unslashed*}, vid_eligible*{vid_eligible <- vid_eligible*}, uint64_weight, flag_index))*{penalty <- penalty*, vid <- vid*}
      ####          (reward*{reward <- reward*}, penalty*{penalty <- penalty*})

relation ApplyRewardAndPenalty:
      rule :
 378: 36734/####     -- rel IncreaseBalance: state .BALANCES[ vid ]+ reward ~> state_r
 379: 36734/####     -- rel DecreaseBalance: state_r .BALANCES[ vid ]- penalty ~> state_out
      36734          state .BALANCES[ vid ]+ reward - penalty ~> state_out

relation ApplyRewardsAndPenalties:
      rule nil:
 380:   82/36734     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
 381:   82/####     -- if gwei*{gwei <- gwei*} matches []
 382:   82/####     -- if gwei'*{gwei' <- gwei'*} matches []
        82          state .BALANCES[ validatorIndex*{validatorIndex <- validatorIndex*} ]+ gwei*{gwei <- gwei*} - gwei'*{gwei' <- gwei'*} ~> state
      rule cons:
 383: 36734/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 384: 36734         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 385: 36734/####     -- if gwei*{gwei <- gwei*} matches _ :: _
 386: 36734         -- let reward_h :: reward_t*{reward_t <- reward_t*} = gwei*{gwei <- gwei*}
 387: 36734/####     -- if gwei'*{gwei' <- gwei'*} matches _ :: _
 388: 36734         -- let penalty_h :: penalty_t*{penalty_t <- penalty_t*} = gwei'*{gwei' <- gwei'*}
 389: 36734/####     -- rel ApplyRewardAndPenalty: state .BALANCES[ vid_h ]+ reward_h - penalty_h ~> state_h
 390: 36734/####     -- rel ApplyRewardsAndPenalties: state_h .BALANCES[ vid_t*{vid_t <- vid_t*} ]+ reward_t*{reward_t <- reward_t*} - penalty_t*{penalty_t <- penalty_t*} ~> state_out
      36734          state .BALANCES[ validatorIndex*{validatorIndex <- validatorIndex*} ]+ gwei*{gwei <- gwei*} - gwei'*{gwei' <- gwei'*} ~> state_out

relation ApplyRewardsAndPenalties_over_deltas:
      rule nil:
 391:   30/ 120     -- if gwei*{gwei <- gwei*}*{gwei* <- gwei**} matches []
 392:   30/####     -- if gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**} matches []
        30          state .BALANCES[ _validatorIndex*{_validatorIndex <- _validatorIndex*} ]+ gwei*{gwei <- gwei*}*{gwei* <- gwei**} - gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**} ~>* state
      rule cons:
 393:  120/####     -- if gwei*{gwei <- gwei*}*{gwei* <- gwei**} matches _ :: _
 394:  120         -- let reward*{reward <- reward*} :: reward_t*{reward_t <- reward_t*}*{reward_t* <- reward_t**} = gwei*{gwei <- gwei*}*{gwei* <- gwei**}
 395:  120/####     -- if gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**} matches _ :: _
 396:  120         -- let penalty*{penalty <- penalty*} :: penalty_t*{penalty_t <- penalty_t*}*{penalty_t* <- penalty_t**} = gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**}
 397:  120/####     -- rel ApplyRewardsAndPenalties: state .BALANCES[ vid*{vid <- vid*} ]+ reward*{reward <- reward*} - penalty*{penalty <- penalty*} ~> state_t
 398:  120/####     -- rel ApplyRewardsAndPenalties_over_deltas: state_t .BALANCES[ vid*{vid <- vid*} ]+ reward_t*{reward_t <- reward_t*}*{reward_t* <- reward_t**} - penalty_t*{penalty_t <- penalty_t*}*{penalty_t* <- penalty_t**} ~>* state_out
       120          state .BALANCES[ vid*{vid <- vid*} ]+ gwei*{gwei <- gwei*}*{gwei* <- gwei**} - gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**} ~>* state_out

relation ApplyInactivity:
      rule unslashed_participating_no_leak:
 399: 4889/4309     -- if vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 400: 4399/ 490     -- if ~$is_in_inactivity_leak(state)
 401: 4399/####     -- if (vid < |state.INACTIVITY_SCORES|)
      4399          state .INACTIVITY_SCORES[ vid ]~> state[INACTIVITY_SCORES[vid] = $max_uint64(((state.INACTIVITY_SCORES[vid] - 1) - $INACTIVITY_SCORE_RECOVERY_RATE), 0)]
      rule unslashed_participating_leak:
 402:  490/4309     -- if vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 403:  490/####     -- if $is_in_inactivity_leak(state)
 404:  490/####     -- if (vid < |state.INACTIVITY_SCORES|)
       490          state .INACTIVITY_SCORES[ vid ]~> state[INACTIVITY_SCORES[vid] = $max_uint64((state.INACTIVITY_SCORES[vid] - 1), 0)]
      rule inactive_no_leak:
 405: 4309/####     -- if ~vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 406: 1843/2466     -- if ~$is_in_inactivity_leak(state)
 407: 1843         -- let n_leak_penalty = ($INACTIVITY_SCORE_BIAS - $INACTIVITY_SCORE_RECOVERY_RATE)
 408: 1843/####     -- if (vid < |state.INACTIVITY_SCORES|)
      1843          state .INACTIVITY_SCORES[ vid ]~> state[INACTIVITY_SCORES[vid] = $max_uint64((state.INACTIVITY_SCORES[vid] + n_leak_penalty), 0)]
      rule inactive_leak:
 409: 2466/####     -- if ~vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 410: 2466/####     -- if $is_in_inactivity_leak(state)
 411: 2466/####     -- if (vid < |state.INACTIVITY_SCORES|)
      2466          state .INACTIVITY_SCORES[ vid ]~> state[INACTIVITY_SCORES[vid] = $max_uint64((state.INACTIVITY_SCORES[vid] + $INACTIVITY_SCORE_BIAS), 0)]

relation ApplyInactivity_over_vids:
      rule nil:
 412:   30/9198     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        30          state .INACTIVITY_SCORES[ validatorIndex*{validatorIndex <- validatorIndex*} ]~>* state
      rule cons:
 413: 9198/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 414: 9198         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 415: 9198/####     -- rel ApplyInactivity: state .INACTIVITY_SCORES[ vid_h ]~> state_1
 416: 9198/####     -- rel ApplyInactivity_over_vids: state_1 .INACTIVITY_SCORES[ vid_t*{vid_t <- vid_t*} ]~>* state_updated
      9198          state .INACTIVITY_SCORES[ validatorIndex*{validatorIndex <- validatorIndex*} ]~>* state_updated

def $is_due_slashing_en:
      clause 0:
 417: 1086/22178     -- if (validator.SLASHED = true)
 418: ####/1086     -- if ((epoch + ($EPOCHS_PER_SLASHINGS_VECTOR / 2)) = validator.WITHDRAWABLE_EPOCH)
      ####          true
      clause 1:
 419: 23264         -- otherwise
      23264          false

relation DecreaseBalances:
      rule nil:
 420:   36/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
 421:   36/####     -- if gwei*{gwei <- gwei*} matches []
        36          state .BALANCES[ validatorIndex*{validatorIndex <- validatorIndex*} ]- gwei*{gwei <- gwei*} ~> state
      rule cons:
 422: ####/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 423: ####         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 424: ####/####     -- if gwei*{gwei <- gwei*} matches _ :: _
 425: ####         -- let penalty_h :: penalty_t*{penalty_t <- penalty_t*} = gwei*{gwei <- gwei*}
 426: ####/####     -- rel DecreaseBalance: state .BALANCES[ vid_h ]- penalty_h ~> state_h
 427: ####/####     -- rel DecreaseBalances: state_h .BALANCES[ vid_t*{vid_t <- vid_t*} ]- penalty_t*{penalty_t <- penalty_t*} ~> state_t
      ####          state .BALANCES[ validatorIndex*{validatorIndex <- validatorIndex*} ]- gwei*{gwei <- gwei*} ~> state_t

relation WeighJustificationAndFinalization:
      rule justify_neither_finalize_prev:
 428:   27/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 429:   11/  16     -- if ~((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 430:    6/   5     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 431:    6         -- let bit*{bit <- bit*} = [false, state.JUSTIFICATION_BITS[0], state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 432:    6         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 433:    6         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 434:    6         -- let epoch_cur = $get_current_epoch(state)
 435: ####/   6     -- if ((((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) \/ ((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
      ####          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][FINALIZED_CHECKPOINT = checkpoint_prev][JUSTIFICATION_BITS = bit*{bit <- bit*}]
      rule justify_neither_finalize_neither:
 436:   27/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 437:   11/  16     -- if ~((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 438:    6/   5     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 439:    6         -- let bit*{bit <- bit*} = [false, state.JUSTIFICATION_BITS[0], state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 440:    6         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 441:    6         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 442:    6         -- let epoch_cur = $get_current_epoch(state)
 443:    6/####     -- if (~(((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) /\ ~((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
         6          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][JUSTIFICATION_BITS = bit*{bit <- bit*}]
      rule justify_prev_finalize_prev:
 444:   21/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 445:   16/   5     -- if ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 446:   10/   6     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 447:   10         -- let bit*{bit <- bit*} = [false, true, state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 448:   10         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 449:   10         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 450:   10         -- let epoch_prev = $get_previous_epoch(state)
 451:   10         -- let epoch_cur = $get_current_epoch(state)
 452:    6/   4     -- if ((((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) \/ ((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
         6          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][CURRENT_JUSTIFIED_CHECKPOINT = {EPOCH epoch_prev, ROOT $get_block_root(state, epoch_prev)}][FINALIZED_CHECKPOINT = checkpoint_prev][JUSTIFICATION_BITS = bit*{bit <- bit*}]
      rule justify_prev_finalize_neither:
 453:   15/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 454:   10/   5     -- if ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 455:    4/   6     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 456:    4         -- let bit*{bit <- bit*} = [false, true, state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 457:    4         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 458:    4         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 459:    4         -- let epoch_prev = $get_previous_epoch(state)
 460:    4         -- let epoch_cur = $get_current_epoch(state)
 461:    4/####     -- if (~(((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) /\ ~((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
         4          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][CURRENT_JUSTIFIED_CHECKPOINT = {EPOCH epoch_prev, ROOT $get_block_root(state, epoch_prev)}][JUSTIFICATION_BITS = bit*{bit <- bit*}]
      rule justify_cur_finalize_cur:
 462:   11/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 463:   11/####     -- if ((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 464:   11         -- let bit*{bit <- bit*} = [true, (state.JUSTIFICATION_BITS[0] \/ ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))), state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 465:   11         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 466:   11         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 467:   11         -- let epoch_cur = $get_current_epoch(state)
 468:    6/   5     -- if ((((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 2))) \/ ((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 1))))
         6          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][CURRENT_JUSTIFIED_CHECKPOINT = {EPOCH epoch_cur, ROOT $get_block_root(state, epoch_cur)}][FINALIZED_CHECKPOINT = checkpoint_cur][JUSTIFICATION_BITS = bit*{bit <- bit*}]
      rule justify_cur_finalize_prev:
 469:    5/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 470:    5/####     -- if ((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 471:    5         -- let bit*{bit <- bit*} = [true, (state.JUSTIFICATION_BITS[0] \/ ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))), state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 472:    5         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 473:    5         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 474:    5         -- let epoch_cur = $get_current_epoch(state)
 475: ####/   5     -- if ((~(((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 2))) /\ ~((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 1)))) /\ ((((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) \/ ((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2)))))
      ####          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][CURRENT_JUSTIFIED_CHECKPOINT = {EPOCH epoch_cur, ROOT $get_block_root(state, epoch_cur)}][FINALIZED_CHECKPOINT = checkpoint_prev][JUSTIFICATION_BITS = bit*{bit <- bit*}]
      rule justify_cur_finalize_neither:
 476:    5/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 477:    5/####     -- if ((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 478:    5         -- let bit*{bit <- bit*} = [true, (state.JUSTIFICATION_BITS[0] \/ ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))), state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 479:    5         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 480:    5         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 481:    5         -- let epoch_cur = $get_current_epoch(state)
 482:    5/####     -- if (((~(((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 2))) /\ ~((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 1)))) /\ ~(((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3)))) /\ ~((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
         5          state TOTAL balance_total_active PREV balance_prev_epoch_target CUR balance_current_epoch_target ~> state[PREVIOUS_JUSTIFIED_CHECKPOINT = checkpoint_cur][CURRENT_JUSTIFIED_CHECKPOINT = {EPOCH epoch_cur, ROOT $get_block_root(state, epoch_cur)}][JUSTIFICATION_BITS = bit*{bit <- bit*}]

relation ProcessEligibilityAndEjection:
      rule eligible_and_eject:
 483: 11632         -- let validator = state.VALIDATORS[vid]
 484: 11632         -- let epoch_cur = $get_current_epoch(state)
 485:  112/11520     -- if $is_eligible_for_activation_queue(validator)
 486: ####/ 112     -- if ($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
 487: ####/####     -- rel InitiateValidatorExit: state[VALIDATORS[vid].ACTIVATION_ELIGIBILITY_EPOCH = (epoch_cur + 1)] .VALIDATORS[ vid ]~> state_updated
      ####          state .VALIDATORS[ vid ]~> state_updated
      rule eligible:
 488: 11632         -- let validator = state.VALIDATORS[vid]
 489: 11632         -- let epoch_cur = $get_current_epoch(state)
 490:  112/11520     -- if $is_eligible_for_activation_queue(validator)
 491:  112/####     -- if ~($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
       112          state .VALIDATORS[ vid ]~> state[VALIDATORS[vid].ACTIVATION_ELIGIBILITY_EPOCH = (epoch_cur + 1)]
      rule eject:
 492: 11520         -- let validator = state.VALIDATORS[vid]
 493: 11520         -- let epoch_cur = $get_current_epoch(state)
 494: 11520/####     -- if ~$is_eligible_for_activation_queue(validator)
 495:  101/11419     -- if ($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
 496:  101/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid ]~> state_updated
       101          state .VALIDATORS[ vid ]~> state_updated
      rule skip:
 497: 11419         -- let validator = state.VALIDATORS[vid]
 498: 11419         -- let epoch_cur = $get_current_epoch(state)
 499: 11419/####     -- if ~$is_eligible_for_activation_queue(validator)
 500: 11419/####     -- if ~($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
      11419          state .VALIDATORS[ vid ]~> state

relation ProcessEligibilityAndEjection_over_vids:
      rule nil:
 501:   36/11632     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        36          state .VALIDATORS[ validatorIndex*{validatorIndex <- validatorIndex*} ]~> state
      rule cons:
 502: 11632/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 503: 11632         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 504: 11632/####     -- rel ProcessEligibilityAndEjection: state .VALIDATORS[ vid_h ]~> state_h
 505: 11632/####     -- rel ProcessEligibilityAndEjection_over_vids: state_h .VALIDATORS[ vid_t*{vid_t <- vid_t*} ]~> state_out
      11632          state .VALIDATORS[ validatorIndex*{validatorIndex <- validatorIndex*} ]~> state_out

def $is_eligible_activation_en:
      clause 0:

relation ApplyActivation:
      rule :

relation ApplyActivations:
      rule nil:
 506:   36/  36     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        36          state .VALIDATORS[ validatorIndex*{validatorIndex <- validatorIndex*} ].ACTIVATION_EPOCH~>* state
      rule cons:
 507:   36/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 508:   36         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 509:   36/####     -- rel ApplyActivation: state .VALIDATORS[ vid_h ].ACTIVATION_EPOCH~> state_h
 510:   36/####     -- rel ApplyActivations: state_h .VALIDATORS[ vid_t*{vid_t <- vid_t*} ].ACTIVATION_EPOCH~>* state_out
        36          state .VALIDATORS[ validatorIndex*{validatorIndex <- validatorIndex*} ].ACTIVATION_EPOCH~>* state_out

relation ProcessSlot:
      rule header_root_empty:
 511: 1185         -- let i = (state.SLOT \ $SLOTS_PER_HISTORICAL_ROOT)
 512:  557/ 628     -- if (state.LATEST_BLOCK_HEADER.STATE_ROOT = $ZERO_ROOT)
 513:  557         -- let root_state_prev = $hash_tree_root_beaconState(state)
 514:  557         -- let beaconBlockHeader = state.LATEST_BLOCK_HEADER[STATE_ROOT = $hash_tree_root_beaconState(state)]
 515:  557         -- let root_block_prev = $hash_tree_root_beaconBlockHeader(beaconBlockHeader)
       557          state ~> state[STATE_ROOTS[i] = root_state_prev][LATEST_BLOCK_HEADER = beaconBlockHeader][BLOCK_ROOTS[i] = root_block_prev]
      rule header_root_empty:
 516:  628         -- let i = (state.SLOT \ $SLOTS_PER_HISTORICAL_ROOT)
 517:  628/####     -- if (state.LATEST_BLOCK_HEADER.STATE_ROOT =/= $ZERO_ROOT)
 518:  628         -- let root_state_prev = $hash_tree_root_beaconState(state)
 519:  628         -- let root_block_prev = $hash_tree_root_beaconBlockHeader(state.LATEST_BLOCK_HEADER)
       628          state ~> state[STATE_ROOTS[i] = root_state_prev][BLOCK_ROOTS[i] = root_block_prev]

relation ProcessJustificationAndFinalization:
      rule skip:
 520:   36         -- let epoch_cur = $get_current_epoch(state)
 521:    9/  27     -- if (epoch_cur <= ($GENESIS_EPOCH + 1))
         9          state ~> state
      rule main:
 522:   27         -- let vid_prev*{vid_prev <- vid_prev*} = $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 523:   27         -- let vid_cur*{vid_cur <- vid_cur*} = $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_current_epoch(state))
 524:   27         -- let balance_total_active = $get_total_active_balance(state)
 525:   27         -- let balance_prev_target = $get_total_balance(state, vid_prev*{vid_prev <- vid_prev*})
 526:   27         -- let balance_cur_target = $get_total_balance(state, vid_cur*{vid_cur <- vid_cur*})
 527:   27/####     -- rel WeighJustificationAndFinalization: state TOTAL balance_total_active PREV balance_prev_target CUR balance_cur_target ~> state_updated
        27          state ~> state_updated

relation ProcessInactivityUpdates:
      rule skip:
 528:    6/  30     -- if ($get_current_epoch(state) = $GENESIS_EPOCH)
         6          state ~> state
      rule main:
 529:   30         -- otherwise
 530:   30         -- let vid_eligible*{vid_eligible <- vid_eligible*} = $get_eligible_validator_indices(state)
 531:   30/####     -- rel ApplyInactivity_over_vids: state .INACTIVITY_SCORES[ vid_eligible*{vid_eligible <- vid_eligible*} ]~>* state_updated
        30          state ~> state_updated

relation ProcessRewardsAndPenalties:
      rule skip:
 532:    6/  30     -- if ($get_current_epoch(state) = $GENESIS_EPOCH)
         6          state ~> state
      rule main:
 533:   30         -- otherwise
 534:   30         -- let vid_flag*{vid_flag <- vid_flag*} = $range(|$PARTICIPATION_FLAG_WEIGHTS|)
 535: ####         -- (let (reward_flag*{reward_flag <- reward_flag*}, penalty_flag*{penalty_flag <- penalty_flag*}) = $get_flag_index_deltas(state, vid_flag))*{penalty_flag* <- penalty_flag**, reward_flag* <- reward_flag**, vid_flag <- vid_flag*}
 536:   30         -- let (reward_inactivity*{reward_inactivity <- reward_inactivity*}, penalty_inactivity*{penalty_inactivity <- penalty_inactivity*}) = $get_inactivity_penalty_deltas(state)
 537:   30         -- let reward*{reward <- reward*}*{reward* <- reward**} = reward_flag*{reward_flag <- reward_flag*}*{reward_flag* <- reward_flag**} ++ [reward_inactivity*{reward_inactivity <- reward_inactivity*}]
 538:   30         -- let penalty*{penalty <- penalty*}*{penalty* <- penalty**} = penalty_flag*{penalty_flag <- penalty_flag*}*{penalty_flag* <- penalty_flag**} ++ [penalty_inactivity*{penalty_inactivity <- penalty_inactivity*}]
 539:   30         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 540:   30/####     -- rel ApplyRewardsAndPenalties_over_deltas: state .BALANCES[ vid*{vid <- vid*} ]+ reward*{reward <- reward*}*{reward* <- reward**} - penalty*{penalty <- penalty*}*{penalty* <- penalty**} ~>* state_out
        30          state ~> state_out

relation ProcessRegistryUpdates:
      rule :
 541:   36         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 542:   36/####     -- rel ProcessEligibilityAndEjection_over_vids: state .VALIDATORS[ vid*{vid <- vid*} ]~> state_1
 543:   36         -- let (vid_1, validator)*{validator <- validator*, vid_1 <- vid_1*} = $enumerate_<validator>(state_1.VALIDATORS)
 544:   36         -- let (vid_elg, validator_elg)*{validator_elg <- validator_elg*, vid_elg <- vid_elg*} = $filter_list_2_<(nat, validator), beaconState>((vid_1, validator)*{validator <- validator*, vid_1 <- vid_1*}, state_1, $is_eligible_activation_en)
 545: ####         -- (let epoch_elg = validator_elg.ACTIVATION_ELIGIBILITY_EPOCH)*{epoch_elg <- epoch_elg*, validator_elg <- validator_elg*}
 546:   36         -- let (_epoch, vid_activation_queue)*{_epoch <- _epoch*, vid_activation_queue <- vid_activation_queue*} = $sort_<(epoch, validatorIndex)>((epoch_elg, vid_elg)*{epoch_elg <- epoch_elg*, vid_elg <- vid_elg*})
 547:   36         -- let vid_dequeued*{vid_dequeued <- vid_dequeued*} = vid_activation_queue*{vid_activation_queue <- vid_activation_queue*}[0 : $get_validator_activation_churn_limit(state_1)]
 548:   36/####     -- rel ApplyActivations: state_1 .VALIDATORS[ vid_dequeued*{vid_dequeued <- vid_dequeued*} ].ACTIVATION_EPOCH~>* state_out
        36          state ~> state_out

relation ProcessSlashings:
      rule :
 549:   36         -- let epoch_cur = $get_current_epoch(state)
 550:   36         -- let balance_total = $get_total_active_balance(state)
 551:   36         -- let balance_adjusted_total_slashing = $min_uint64(($sum_<gwei>(state.SLASHINGS) * $PROPORTIONAL_SLASHING_MULTIPLIER_BELLATRIX), balance_total)
 552:   36         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 553:   36         -- let (vid_due, validator_due)*{validator_due <- validator_due*, vid_due <- vid_due*} = $filter_list_2_<(validatorIndex, validator), epoch>((vid, validator)*{validator <- validator*, vid <- vid*}, epoch_cur, $is_due_slashing_en)
 554:   36         -- let gwei_increment = $EFFECTIVE_BALANCE_INCREMENT
 555: ####         -- (let penalty_numerator = ((validator_due.EFFECTIVE_BALANCE / gwei_increment) * balance_adjusted_total_slashing))*{penalty_numerator <- penalty_numerator*, validator_due <- validator_due*}
 556: ####         -- (let penalty = ((penalty_numerator / balance_total) * gwei_increment))*{penalty <- penalty*, penalty_numerator <- penalty_numerator*}
 557:   36/####     -- rel DecreaseBalances: state .BALANCES[ vid_due*{vid_due <- vid_due*} ]- penalty*{penalty <- penalty*} ~> state_out
        36          state ~> state_out

relation ProcessEth1DataReset:
      rule skip:
 558:   36         -- let epoch_next = ($get_current_epoch(state) + 1)
 559:   35/   1     -- if ((epoch_next \ $EPOCHS_PER_ETH1_VOTING_PERIOD) =/= 0)
        35          state ~> state
      rule main:
 560:    1         -- otherwise
         1          state ~> state[ETH1_DATA_VOTES = []]

relation ProcessEffectiveBalanceUpdates:
      rule :
 561:   36         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 562: ####         -- (let balance = state.BALANCES[vid])*{balance <- balance*, vid <- vid*}
 563: ####/####     -- (rel ApplyEffectiveBalance: validator .EFFECTIVE_BALANCE balance ~> validator_updated)*{balance <- balance*, validator <- validator*, validator_updated <- validator_updated*}
      ####          state ~> state[VALIDATORS = validator_updated*{validator_updated <- validator_updated*}]

relation ProcessSlashingsReset:
      rule :
 564:   36         -- let epoch_next = ($get_current_epoch(state) + 1)
 565:   36         -- let i = (epoch_next \ $EPOCHS_PER_SLASHINGS_VECTOR)
 566:   36/####     -- if (i < |state.SLASHINGS|)
        36          state ~> state[SLASHINGS[i] = 0]

relation ProcessRandaoMixesReset:
      rule :
 567:   36         -- let epoch_cur = $get_current_epoch(state)
 568:   36         -- let epoch_next = (epoch_cur + 1)
 569:   36         -- let i = (epoch_next \ $EPOCHS_PER_HISTORICAL_VECTOR)
 570:   36/####     -- if (i < |state.RANDAO_MIXES|)
        36          state ~> state[RANDAO_MIXES[i] = $get_randao_mix(state, epoch_cur)]

relation ProcessHistoricalSummariesUpdate:
      rule skip:
 571:   36         -- let epoch_next = ($get_current_epoch(state) + 1)
 572:   35/   1     -- if ((epoch_next \ ($SLOTS_PER_HISTORICAL_ROOT / $SLOTS_PER_EPOCH)) =/= 0)
        35          state ~> state
      rule main:
 573:    1         -- otherwise
 574:    1         -- let root_block_summary = $hash_tree_root_roots(state.BLOCK_ROOTS)
 575:    1         -- let root_state_summary = $hash_tree_root_roots(state.STATE_ROOTS)
 576:    1         -- let historicalSummary = {BLOCK_SUMMARY_ROOT root_block_summary, STATE_SUMMARY_ROOT root_state_summary}
         1          state ~> state[HISTORICAL_SUMMARIES = state.HISTORICAL_SUMMARIES ++ [historicalSummary]]

relation ProcessParticipationFlagUpdates:
      rule :
 577:   36         -- let participationFlags_zero*{participationFlags_zero <- participationFlags_zero*} = $repeat_<participationFlags>(0, |state.VALIDATORS|)
        36          state ~> state[PREVIOUS_EPOCH_PARTICIPATION = state.CURRENT_EPOCH_PARTICIPATION][CURRENT_EPOCH_PARTICIPATION = participationFlags_zero*{participationFlags_zero <- participationFlags_zero*}]

relation ProcessSyncCommitteeUpdates:
      rule skip:
 578:   36         -- let epoch_next = ($get_current_epoch(state) + 1)
 579:   35/   1     -- if ((epoch_next \ $EPOCHS_PER_SYNC_COMMITTEE_PERIOD) =/= 0)
        35          state ~> state
      rule main:
 580:    1         -- otherwise
         1          state ~> state[CURRENT_SYNC_COMMITTEE = state.NEXT_SYNC_COMMITTEE][NEXT_SYNC_COMMITTEE = $get_next_sync_committee(state)]

relation ProcessEpoch:
      rule :
 581:   36/####     -- rel ProcessJustificationAndFinalization: state ~> state_0
 582:   36/####     -- rel ProcessInactivityUpdates: state_0 ~> state_1
 583:   36/####     -- rel ProcessRewardsAndPenalties: state_1 ~> state_2
 584:   36/####     -- rel ProcessRegistryUpdates: state_2 ~> state_3
 585:   36/####     -- rel ProcessSlashings: state_3 ~> state_4
 586:   36/####     -- rel ProcessEth1DataReset: state_4 ~> state_5
 587:   36/####     -- rel ProcessEffectiveBalanceUpdates: state_5 ~> state_6
 588:   36/####     -- rel ProcessSlashingsReset: state_6 ~> state_7
 589:   36/####     -- rel ProcessRandaoMixesReset: state_7 ~> state_8
 590:   36/####     -- rel ProcessHistoricalSummariesUpdate: state_8 ~> state_9
 591:   36/####     -- rel ProcessParticipationFlagUpdates: state_9 ~> state_10
 592:   36/####     -- rel ProcessSyncCommitteeUpdates: state_10 ~> state_11
        36          state ~> state_11

relation ProcessSlots_loop:
      rule base:
 593:  569/1185     -- if (state.SLOT >= slot_target)
       569          state slot_target ~> state
      rule step-no-epoch:
 594: 1185/####     -- if (state.SLOT < slot_target)
 595: 1185/####     -- rel ProcessSlot: state ~> state_after_slot
 596: 1185         -- let slot_cur = state_after_slot.SLOT
 597: 1185         -- let slot_next = (slot_cur + 1)
 598: 1149/  36     -- if ((slot_next \ $SLOTS_PER_EPOCH) =/= 0)
 599: 1149         -- let state_next = state_after_slot[SLOT = slot_next]
 600: 1149/####     -- rel ProcessSlots_loop: state_next slot_target ~> state_out
      1149          state slot_target ~> state_out
      rule step-epoch:
 601:   36/####     -- if (state.SLOT < slot_target)
 602:   36/####     -- rel ProcessSlot: state ~> state_after_slot
 603:   36         -- let slot_cur = state_after_slot.SLOT
 604:   36         -- let slot_next = (slot_cur + 1)
 605:   36/####     -- if ((slot_next \ $SLOTS_PER_EPOCH) = 0)
 606:   36/####     -- rel ProcessEpoch: state_after_slot ~> state_after_epoch
 607:   36         -- let state_next = state_after_epoch[SLOT = slot_next]
 608:   36/####     -- rel ProcessSlots_loop: state_next slot_target ~> state_out
        36          state slot_target ~> state_out

relation ProcessSlots:
      rule :
 609:  569/   3     -- if (state.SLOT < slot_target)
 610:  569/####     -- rel ProcessSlots_loop: state slot_target ~> state_out
       569          state slot_target ~> state_out

def $expected_withdrawals_loop:
      clause 0:
 611:    3/148591     -- if (|withdrawal_acc*{withdrawal_acc <- withdrawal_acc*}| = $MAX_WITHDRAWALS_PER_PAYLOAD)
         3          withdrawal_acc*{withdrawal_acc <- withdrawal_acc*}
      clause 1:
 612:  563/148028     -- if (i >= uint64_bound)
       563          withdrawal_acc*{withdrawal_acc <- withdrawal_acc*}
      clause 2:
 613: 148028/####     -- if (vid < |state.VALIDATORS|)
 614: 148028         -- let validator = state.VALIDATORS[vid]
 615: 148028         -- let balance = state.BALANCES[vid]
 616:   28/148000     -- if ($is_fully_withdrawable_validator(validator, balance, epoch) = true)
 617:   28         -- let withdrawal = {INDEX withdrawalIndex, VALIDATOR_INDEX vid, ADDRESS $extract_execution_address(validator.WITHDRAWAL_CREDENTIALS), AMOUNT balance}
        28          $expected_withdrawals_loop(state, epoch, (withdrawalIndex + 1), ((vid + 1) \ |state.VALIDATORS|), uint64_bound, (i + 1), withdrawal_acc*{withdrawal_acc <- withdrawal_acc*} ++ [withdrawal])
      clause 3:
 618: 148000/####     -- if (vid < |state.VALIDATORS|)
 619: 148000         -- let validator = state.VALIDATORS[vid]
 620: 148000         -- let balance = state.BALANCES[vid]
 621: 148000/####     -- if ($is_fully_withdrawable_validator(validator, balance, epoch) = false)
 622:   78/147922     -- if ($is_partially_withdrawable_validator(validator, balance) = true)
 623:   78         -- let withdrawal = {INDEX withdrawalIndex, VALIDATOR_INDEX vid, ADDRESS $extract_execution_address(validator.WITHDRAWAL_CREDENTIALS), AMOUNT (balance - $MAX_EFFECTIVE_BALANCE)}
        78          $expected_withdrawals_loop(state, epoch, (withdrawalIndex + 1), ((vid + 1) \ |state.VALIDATORS|), uint64_bound, (i + 1), withdrawal_acc*{withdrawal_acc <- withdrawal_acc*} ++ [withdrawal])
      clause 4:
 624: 147922/####     -- if (vid < |state.VALIDATORS|)
 625: 147922         -- let validator = state.VALIDATORS[vid]
 626: 147922         -- let balance = state.BALANCES[vid]
 627: 147922/####     -- if ~$is_fully_withdrawable_validator(validator, balance, epoch)
 628: 147922/####     -- if ~$is_partially_withdrawable_validator(validator, balance)
      147922          $expected_withdrawals_loop(state, epoch, withdrawalIndex, ((vid + 1) \ |state.VALIDATORS|), uint64_bound, (i + 1), withdrawal_acc*{withdrawal_acc <- withdrawal_acc*})

def $get_expected_withdrawals:
      clause 0:
 629:  566         -- let epoch_cur = $get_current_epoch(state)
 630:  566         -- let withdrawalIndex_start = state.NEXT_WITHDRAWAL_INDEX
 631:  566         -- let validatorIndex_start = state.NEXT_WITHDRAWAL_VALIDATOR_INDEX
 632:  566         -- let uint64_bound = $min_uint64(|state.VALIDATORS|, $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP)
       566          $expected_withdrawals_loop(state, epoch_cur, withdrawalIndex_start, validatorIndex_start, uint64_bound, 0, [])

relation ApplyWithdrawal:
      rule main:
 633:  106/####     -- rel DecreaseBalance: state .BALANCES[ withdrawal.VALIDATOR_INDEX ]- withdrawal.AMOUNT ~> state_out
       106          state .BALANCES{ withdrawal }~> state_out

relation ApplyWithdrawals:
      rule nil:
 634:  566/ 106     -- if withdrawal*{withdrawal <- withdrawal*} matches []
       566          state .BALANCES{ withdrawal*{withdrawal <- withdrawal*} }~>* state
      rule cons:
 635:  106/####     -- if withdrawal*{withdrawal <- withdrawal*} matches _ :: _
 636:  106         -- let withdrawal_h :: withdrawal_t*{withdrawal_t <- withdrawal_t*} = withdrawal*{withdrawal <- withdrawal*}
 637:  106/####     -- rel ApplyWithdrawal: state .BALANCES{ withdrawal_h }~> state_1
 638:  106/####     -- rel ApplyWithdrawals: state_1 .BALANCES{ withdrawal_t*{withdrawal_t <- withdrawal_t*} }~>* state_out
       106          state .BALANCES{ withdrawal*{withdrawal <- withdrawal*} }~>* state_out

relation AddFlag:
      rule main:
 639: 11191/8775     -- if ~$has_flag(participationFlags, vid_flag)
      11191          participationFlags [ vid_attesting ]++{ vid_flag }# state #SUM reward ~> $add_flag(participationFlags, vid_flag) #SUM (reward + ($get_base_reward(state, vid_attesting) * $PARTICIPATION_FLAG_WEIGHTS[vid_flag]))
      rule skip:
 640: 8775         -- otherwise
      8775          participationFlags [ _validatorIndex ]++{ vid_flag }# _beaconState #SUM reward ~> participationFlags #SUM reward

relation AddFlags:
      rule nil:
 641: 14474/19966     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
      14474          participationFlags [ _validatorIndex ]++{ validatorIndex*{validatorIndex <- validatorIndex*} }# _beaconState #SUM reward ~> participationFlags #SUM reward
      rule cons:
 642: 19966/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 643: 19966         -- let vid_flag_h :: vid_flag_t*{vid_flag_t <- vid_flag_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 644: 19966/####     -- rel AddFlag: participationFlags [ vid_attesting ]++{ vid_flag_h }# state #SUM reward ~> participationFlags_h #SUM reward_h
 645: 19966/####     -- rel AddFlags: participationFlags_h [ vid_attesting ]++{ vid_flag_t*{vid_flag_t <- vid_flag_t*} }# state #SUM reward_h ~> participationFlags_t #SUM reward_t
      19966          participationFlags [ vid_attesting ]++{ validatorIndex*{validatorIndex <- validatorIndex*} }# state #SUM reward ~> participationFlags_t #SUM reward_t

relation ProcessFlags:
      rule main:
 646: 14575/####     -- if (vid_attesting < |participationFlags*{participationFlags <- participationFlags*}|)
 647: 14575/####     -- rel AddFlags: participationFlags*{participationFlags <- participationFlags*}[vid_attesting] [ vid_attesting ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM 0 ~> participationFlags_updated #SUM reward_increment
      14575          participationFlags*{participationFlags <- participationFlags*} [ vid_attesting ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM reward ~> participationFlags*{participationFlags <- participationFlags*}[[vid_attesting] = participationFlags_updated] #SUM (reward + reward_increment)

relation ProcessFlags_over_attesting:
      rule nil:
 648: 1032/14575     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
      1032          participationFlags*{participationFlags <- participationFlags*} [ validatorIndex*{validatorIndex <- validatorIndex*} ]++{ _validatorIndex*{_validatorIndex <- _validatorIndex*} }# _beaconState #SUM reward ~> participationFlags*{participationFlags <- participationFlags*} #SUM reward
      rule cons:
 649: 14575/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 650: 14575         -- let vid_attesting_h :: vid_attesting_t*{vid_attesting_t <- vid_attesting_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 651: 14575/####     -- rel ProcessFlags: participationFlags*{participationFlags <- participationFlags*} [ vid_attesting_h ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM reward ~> participationFlags_1*{participationFlags_1 <- participationFlags_1*} #SUM reward_1
 652: 14575/####     -- rel ProcessFlags_over_attesting: participationFlags_1*{participationFlags_1 <- participationFlags_1*} [ vid_attesting_t*{vid_attesting_t <- vid_attesting_t*} ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM reward_1 ~> participationFlags_2*{participationFlags_2 <- participationFlags_2*} #SUM reward_2
      14575          participationFlags*{participationFlags <- participationFlags*} [ validatorIndex*{validatorIndex <- validatorIndex*} ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM reward ~> participationFlags_2*{participationFlags_2 <- participationFlags_2*} #SUM reward_2

def $bit_is_one_en_blsPubkey:
      clause 0:
 653: 558848/####     -- if (i < |boolean_bits*{boolean_bits <- boolean_bits*}|)
 654: 7424/551424     -- if (boolean_bits*{boolean_bits <- boolean_bits*}[i] = true)
      7424          true
      clause 1:
 655: 551424         -- otherwise
      551424          false

relation ApplyReward:
      rule participation:
 656: 7424/275712     -- if (bool = true)
 657: 7424/####     -- rel IncreaseBalance: state .BALANCES[ vid_participant ]+ reward_participant ~> state_participant
 658: 7424/####     -- rel IncreaseBalance: state_participant .BALANCES[ vid_proposer ]+ reward_proposer ~> state_out
      7424          state PARTICIPANT bool [ vid_participant ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_out
      rule no_participation:
 659: 275712/####     -- if (bool = false)
 660: 275712/####     -- rel DecreaseBalance: state .BALANCES[ vid_participant ]- reward_participant ~> state_out
      275712          state PARTICIPANT bool [ vid_participant ]+ reward_participant PROPOSER[ _validatorIndex ]+ _gwei ~> state_out

relation ApplyReward_over_participants:
      rule nil:
 661:  553/283136     -- if boolean*{boolean <- boolean*} matches []
 662:  553/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
       553          state PARTICIPANT boolean*{boolean <- boolean*} [ validatorIndex*{validatorIndex <- validatorIndex*} ]+ _gwei PROPOSER[ _validatorIndex ]+ _gwei' ~> state
      rule cons:
 663: 283136/####     -- if boolean*{boolean <- boolean*} matches _ :: _
 664: 283136         -- let boolean_h :: boolean_t*{boolean_t <- boolean_t*} = boolean*{boolean <- boolean*}
 665: 283136/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 666: 283136         -- let vid_participant_h :: vid_participant_t*{vid_participant_t <- vid_participant_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 667: 283136/####     -- rel ApplyReward: state PARTICIPANT boolean_h [ vid_participant_h ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_1
 668: 283136/####     -- rel ApplyReward_over_participants: state_1 PARTICIPANT boolean_t*{boolean_t <- boolean_t*} [ vid_participant_t*{vid_participant_t <- vid_participant_t*} ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_out
      283136          state PARTICIPANT boolean*{boolean <- boolean*} [ validatorIndex*{validatorIndex <- validatorIndex*} ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_out

relation ProcessProposerSlashing:
      rule :
 669:  150         -- let beaconBlockHeader_1 = proposerSlashing.SIGNED_HEADER_1.MESSAGE
 670:  150         -- let beaconBlockHeader_2 = proposerSlashing.SIGNED_HEADER_2.MESSAGE
 671:  150/####     -- if (beaconBlockHeader_1.SLOT = beaconBlockHeader_2.SLOT)
 672:  150/####     -- if (beaconBlockHeader_1.PROPOSER_INDEX = beaconBlockHeader_2.PROPOSER_INDEX)
 673:  150/####     -- if (beaconBlockHeader_1 =/= beaconBlockHeader_2)
 674:  150         -- let vid_proposer = beaconBlockHeader_1.PROPOSER_INDEX
 675:  150/####     -- if (vid_proposer < |state.VALIDATORS|)
 676:  150         -- let validator_proposer = state.VALIDATORS[vid_proposer]
 677:  148/   2     -- if $is_slashable_validator(validator_proposer, $get_current_epoch(state))
 678:  148         -- let signedBeaconBlockHeader_1 = proposerSlashing.SIGNED_HEADER_1
 679:  148         -- let epoch_h1 = $compute_epoch_at_slot(signedBeaconBlockHeader_1.MESSAGE.SLOT)
 680:  148         -- let domain_h1 = $get_domain(state, $DOMAIN_BEACON_PROPOSER, ?(epoch_h1))
 681:  148         -- let root_sign_h1 = $compute_signing_root_beaconBlockHeader(signedBeaconBlockHeader_1.MESSAGE, domain_h1)
 682:  148/####     -- if $bls_verify(validator_proposer.PUBKEY, root_sign_h1, signedBeaconBlockHeader_1.SIGNATURE)
 683:  148         -- let signedBeaconBlockHeader_2 = proposerSlashing.SIGNED_HEADER_2
 684:  148         -- let epoch_h2 = $compute_epoch_at_slot(signedBeaconBlockHeader_2.MESSAGE.SLOT)
 685:  148         -- let domain_h2 = $get_domain(state, $DOMAIN_BEACON_PROPOSER, ?(epoch_h2))
 686:  148         -- let root_sign_h2 = $compute_signing_root_beaconBlockHeader(signedBeaconBlockHeader_2.MESSAGE, domain_h2)
 687:  148/####     -- if $bls_verify(validator_proposer.PUBKEY, root_sign_h2, signedBeaconBlockHeader_2.SIGNATURE)
 688:  148/####     -- rel SlashValidator: state SLASH[ vid_proposer ]BY ?() ~> state_slashed
       148          state proposerSlashing ~> state_slashed

relation ProcessProposerSlashings:
      rule nil:
 689:  558/ 150     -- if proposerSlashing*{proposerSlashing <- proposerSlashing*} matches []
       558          state proposerSlashing*{proposerSlashing <- proposerSlashing*} ~> state
      rule cons:
 690:  150/####     -- if proposerSlashing*{proposerSlashing <- proposerSlashing*} matches _ :: _
 691:  150         -- let proposerSlashing_h :: proposerSlashing_t*{proposerSlashing_t <- proposerSlashing_t*} = proposerSlashing*{proposerSlashing <- proposerSlashing*}
 692:  148/   2     -- rel ProcessProposerSlashing: state proposerSlashing_h ~> state_mid
 693:  146/   2     -- rel ProcessProposerSlashings: state_mid proposerSlashing_t*{proposerSlashing_t <- proposerSlashing_t*} ~> state_out
       146          state proposerSlashing*{proposerSlashing <- proposerSlashing*} ~> state_out

relation ProcessAttesterSlashing:
      rule :
 694:   25         -- let indexedAttestation_1 = attesterSlashing.ATTESTATION_1
 695:   25         -- let indexedAttestation_2 = attesterSlashing.ATTESTATION_2
 696:   25/####     -- if ($is_slashable_attestation_data(indexedAttestation_1.DATA, indexedAttestation_2.DATA) = true)
 697:   25/####     -- if ($is_valid_indexed_attestation(state, indexedAttestation_1) = true)
 698:   25/####     -- if ($is_valid_indexed_attestation(state, indexedAttestation_2) = true)
 699:   25         -- let vid_intersection*{vid_intersection <- vid_intersection*} = $set_intersection_<validatorIndex>($to_set_<validatorIndex>(indexedAttestation_1.ATTESTING_INDICES), $to_set_<validatorIndex>(indexedAttestation_2.ATTESTING_INDICES))
 700:   25         -- let vid_sorted*{vid_sorted <- vid_sorted*} = $sort_<validatorIndex>(vid_intersection*{vid_intersection <- vid_intersection*})
 701:   25/####     -- rel SlashIfEligible_over_validators: state SLASH?[ vid_sorted*{vid_sorted <- vid_sorted*} ]~> state_slashed #SLASHED bool_slashed_any
 702:   24/   1     -- if bool_slashed_any
        24          state attesterSlashing ~> state_slashed

relation ProcessAttesterSlashings:
      rule nil:
 703:  557/  25     -- if attesterSlashing*{attesterSlashing <- attesterSlashing*} matches []
       557          state attesterSlashing*{attesterSlashing <- attesterSlashing*} ~> state
      rule cons:
 704:   25/####     -- if attesterSlashing*{attesterSlashing <- attesterSlashing*} matches _ :: _
 705:   25         -- let attesterSlashing_h :: attesterSlashing_t*{attesterSlashing_t <- attesterSlashing_t*} = attesterSlashing*{attesterSlashing <- attesterSlashing*}
 706:   24/   1     -- rel ProcessAttesterSlashing: state attesterSlashing_h ~> state_mid
 707:   23/   1     -- rel ProcessAttesterSlashings: state_mid attesterSlashing_t*{attesterSlashing_t <- attesterSlashing_t*} ~> state_out
        23          state attesterSlashing*{attesterSlashing <- attesterSlashing*} ~> state_out

relation ProcessAttestation:
      rule current_epoch:
 708: 1406         -- let attestationData = attestation.DATA
 709:  523/ 883     -- if (attestationData.TARGET.EPOCH = $get_current_epoch(state))
 710:  523/####     -- if (attestationData.TARGET.EPOCH = $compute_epoch_at_slot(attestationData.SLOT))
 711:  523/####     -- if ((attestationData.SLOT + $MIN_ATTESTATION_INCLUSION_DELAY) <= state.SLOT)
 712:  523/####     -- if (attestationData.INDEX < $get_committee_count_per_slot(state, attestationData.TARGET.EPOCH))
 713:  523         -- let vid_committee*{vid_committee <- vid_committee*} = $get_beacon_committee(state, attestationData.SLOT, attestationData.INDEX)
 714:  523/####     -- if (|attestation.AGGREGATION_BITS| = |vid_committee*{vid_committee <- vid_committee*}|)
 715:  523         -- let vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} = $get_attestation_participation_flag_indices(state, attestationData, (state.SLOT - attestationData.SLOT))
 716:  523/####     -- if $is_valid_indexed_attestation(state, $get_indexed_attestation(state, attestation))
 717:  523/####     -- rel ProcessFlags_over_attesting: state.CURRENT_EPOCH_PARTICIPATION [ $get_attesting_indices(state, attestation) ]++{ vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} }# state #SUM 0 ~> participationFlags_updated*{participationFlags_updated <- participationFlags_updated*} #SUM reward_proposer_numerator
 718:  523         -- let state_1 = state[CURRENT_EPOCH_PARTICIPATION = participationFlags_updated*{participationFlags_updated <- participationFlags_updated*}]
 719:  523         -- let reward_proposer_denominator = ((($WEIGHT_DENOMINATOR - $PROPOSER_WEIGHT) * $WEIGHT_DENOMINATOR) / $PROPOSER_WEIGHT)
 720:  523         -- let reward_proposer = (reward_proposer_numerator / reward_proposer_denominator)
 721:  523/####     -- rel IncreaseBalance: state_1 .BALANCES[ $get_beacon_proposer_index(state_1) ]+ reward_proposer ~> state_out
       523          state attestation ~> state_out
      rule previous_epoch:
 722:  883         -- let attestationData = attestation.DATA
 723:  883/####     -- if (attestationData.TARGET.EPOCH = $get_previous_epoch(state))
 724:  883/####     -- if (attestationData.TARGET.EPOCH = $compute_epoch_at_slot(attestationData.SLOT))
 725:  883/####     -- if ((attestationData.SLOT + $MIN_ATTESTATION_INCLUSION_DELAY) <= state.SLOT)
 726:  883/####     -- if (attestationData.INDEX < $get_committee_count_per_slot(state, attestationData.TARGET.EPOCH))
 727:  883         -- let vid_committee*{vid_committee <- vid_committee*} = $get_beacon_committee(state, attestationData.SLOT, attestationData.INDEX)
 728:  883/####     -- if (|attestation.AGGREGATION_BITS| = |vid_committee*{vid_committee <- vid_committee*}|)
 729:  883         -- let vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} = $get_attestation_participation_flag_indices(state, attestationData, (state.SLOT - attestationData.SLOT))
 730:  883/####     -- if $is_valid_indexed_attestation(state, $get_indexed_attestation(state, attestation))
 731:  883/####     -- rel ProcessFlags_over_attesting: state.PREVIOUS_EPOCH_PARTICIPATION [ $get_attesting_indices(state, attestation) ]++{ vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} }# state #SUM 0 ~> participationFlags_updated*{participationFlags_updated <- participationFlags_updated*} #SUM reward_proposer_numerator
 732:  883         -- let state_1 = state[PREVIOUS_EPOCH_PARTICIPATION = participationFlags_updated*{participationFlags_updated <- participationFlags_updated*}]
 733:  883         -- let reward_proposer_denominator = ((($WEIGHT_DENOMINATOR - $PROPOSER_WEIGHT) * $WEIGHT_DENOMINATOR) / $PROPOSER_WEIGHT)
 734:  883         -- let reward_proposer = (reward_proposer_numerator / reward_proposer_denominator)
 735:  883/####     -- rel IncreaseBalance: state_1 .BALANCES[ $get_beacon_proposer_index(state_1) ]+ reward_proposer ~> state_out
       883          state attestation ~> state_out

relation ProcessAttestations:
      rule nil:
 736:  557/1540     -- if attestation*{attestation <- attestation*} matches []
       557          state attestation*{attestation <- attestation*} ~> state
      rule cons:
 737: 1540/####     -- if attestation*{attestation <- attestation*} matches _ :: _
 738: 1540         -- let attestation_h :: attestation_t*{attestation_t <- attestation_t*} = attestation*{attestation <- attestation*}
 739: 1540/####     -- rel ProcessAttestation: state attestation_h ~> state_h
 740: 1540/####     -- rel ProcessAttestations: state_h attestation_t*{attestation_t <- attestation_t*} ~> state_t
      1540          state attestation*{attestation <- attestation*} ~> state_t

relation ProcessDeposit:
      rule main:
 741:  209/####     -- if $is_valid_merkle_branch($hash_tree_root_depositData(deposit.DATA), deposit.PROOF, ($DEPOSIT_CONTRACT_TREE_DEPTH + 1), state.ETH1_DEPOSIT_INDEX, state.ETH1_DATA.DEPOSIT_ROOT)
 742:  209         -- let state_inc = state[ETH1_DEPOSIT_INDEX = (state.ETH1_DEPOSIT_INDEX + 1)]
 743:  209/####     -- rel ApplyDeposit: state_inc .VALIDATORS++{ deposit.DATA.PUBKEY deposit.DATA.WITHDRAWAL_CREDENTIALS deposit.DATA.AMOUNT }SIGNED deposit.DATA.SIGNATURE ~> state_applied
       209          state deposit ~> state_applied

relation ProcessDeposits:
      rule nil:
 744:  557/ 209     -- if deposit*{deposit <- deposit*} matches []
       557          state deposit*{deposit <- deposit*} ~> state
      rule cons:
 745:  209/####     -- if deposit*{deposit <- deposit*} matches _ :: _
 746:  209         -- let deposit_h :: deposit_t*{deposit_t <- deposit_t*} = deposit*{deposit <- deposit*}
 747:  209/####     -- rel ProcessDeposit: state deposit_h ~> state_h
 748:  209/####     -- rel ProcessDeposits: state_h deposit_t*{deposit_t <- deposit_t*} ~> state_t
       209          state deposit*{deposit <- deposit*} ~> state_t

relation ProcessVoluntaryExit:
      rule main:
 749:  139         -- let voluntaryExit = signedVoluntaryExit.MESSAGE
 750:  139         -- let vid_exit = voluntaryExit.VALIDATOR_INDEX
 751:  139/####     -- if (vid_exit < |state.VALIDATORS|)
 752:  139         -- let validator = state.VALIDATORS[vid_exit]
 753:  139         -- let epoch_cur = $get_current_epoch(state)
 754:  139/####     -- if ($is_active_validator(validator, epoch_cur) = true)
 755:  137/   2     -- if (validator.EXIT_EPOCH = $FAR_FUTURE_EPOCH)
 756:  137/####     -- if (epoch_cur >= voluntaryExit.EPOCH)
 757:  137/####     -- if (validator.ACTIVATION_EPOCH <= ($UINT64_MAX - $SHARD_COMMITTEE_PERIOD))
 758:  137/####     -- if (epoch_cur >= (validator.ACTIVATION_EPOCH + $SHARD_COMMITTEE_PERIOD))
 759:  137         -- let domain_exit = $compute_domain($DOMAIN_VOLUNTARY_EXIT, ?($CAPELLA_FORK_VERSION), ?(state.GENESIS_VALIDATORS_ROOT))
 760:  137         -- let root_vol_exit_signing_root = $compute_signing_root_voluntary_exit(voluntaryExit, domain_exit)
 761:  137/####     -- if ($bls_verify(validator.PUBKEY, root_vol_exit_signing_root, signedVoluntaryExit.SIGNATURE) = true)
 762:  137/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid_exit ]~> state_exit
       137          state signedVoluntaryExit ~> state_exit

relation ProcessVoluntaryExits:
      rule nil:
 763:  555/ 139     -- if signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*} matches []
       555          state signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*} ~> state
      rule cons:
 764:  139/####     -- if signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*} matches _ :: _
 765:  139         -- let signedVoluntaryExit_h :: signedVoluntaryExit_t*{signedVoluntaryExit_t <- signedVoluntaryExit_t*} = signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*}
 766:  137/   2     -- rel ProcessVoluntaryExit: state signedVoluntaryExit_h ~> state_h
 767:  136/   1     -- rel ProcessVoluntaryExits: state_h signedVoluntaryExit_t*{signedVoluntaryExit_t <- signedVoluntaryExit_t*} ~> state_t
       136          state signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*} ~> state_t

relation ProcessBlsToExecutionChange:
      rule main:
 768:  117         -- let blsToExecutionChange = signedBlsToExecutionChange.MESSAGE
 769:  117         -- let vid_change = blsToExecutionChange.VALIDATOR_INDEX
 770:  117/####     -- if (vid_change < |state.VALIDATORS|)
 771:  117         -- let validator = state.VALIDATORS[vid_change]
 772:  115/   2     -- if ($get_first_byte(validator.WITHDRAWAL_CREDENTIALS) = $BLS_WITHDRAWAL_PREFIX)
 773:  115/####     -- if ($strip_first_byte(validator.WITHDRAWAL_CREDENTIALS) = $strip_first_byte($hash_<blsPubkey>(blsToExecutionChange.FROM_BLS_PUBKEY)))
 774:  115         -- let domain = $compute_domain($DOMAIN_BLS_TO_EXECUTION_CHANGE, ?(), ?(state.GENESIS_VALIDATORS_ROOT))
 775:  115         -- let root_sign = $compute_signing_root_bls_to_execution_change(blsToExecutionChange, domain)
 776:  115/####     -- if ($bls_verify(blsToExecutionChange.FROM_BLS_PUBKEY, root_sign, signedBlsToExecutionChange.SIGNATURE) = true)
 777:  115         -- let bytes32_new_wc = $make_withdrawal_credentials_eth1(blsToExecutionChange.TO_EXECUTION_ADDRESS)
       115          state signedBlsToExecutionChange ~> state[VALIDATORS[vid_change].WITHDRAWAL_CREDENTIALS = bytes32_new_wc]

relation ProcessBlsToExecutionChanges:
      rule nil:
 778:  553/ 117     -- if signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*} matches []
       553          state signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*} ~> state
      rule cons:
 779:  117/####     -- if signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*} matches _ :: _
 780:  117         -- let signedBlsToExecutionChange_h :: signedBlsToExecutionChange_t*{signedBlsToExecutionChange_t <- signedBlsToExecutionChange_t*} = signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*}
 781:  115/   2     -- rel ProcessBlsToExecutionChange: state signedBlsToExecutionChange_h ~> state_h
 782:  113/   2     -- rel ProcessBlsToExecutionChanges: state_h signedBlsToExecutionChange_t*{signedBlsToExecutionChange_t <- signedBlsToExecutionChange_t*} ~> state_t
       113          state signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*} ~> state_t

relation ProcessBlockHeader:
      rule :
 783:  569/####     -- if (block.SLOT = state.SLOT)
 784:  569/####     -- if (state.LATEST_BLOCK_HEADER.SLOT < block.SLOT)
 785:  567/   2     -- if ($get_beacon_proposer_index(state) = block.PROPOSER_INDEX)
 786:  566/   1     -- if (block.PARENT_ROOT = $hash_tree_root_beaconBlockHeader(state.LATEST_BLOCK_HEADER))
 787:  566         -- let beaconBlockHeader_new = {SLOT block.SLOT, PROPOSER_INDEX block.PROPOSER_INDEX, PARENT_ROOT block.PARENT_ROOT, STATE_ROOT $ZERO_ROOT, BODY_ROOT $hash_tree_root_beaconBlockBody(block.BODY)}
 788:  566/####     -- if (block.PROPOSER_INDEX < |state.VALIDATORS|)
 789:  566/####     -- if ~state.VALIDATORS[block.PROPOSER_INDEX].SLASHED
       566          state block ~> state[LATEST_BLOCK_HEADER = beaconBlockHeader_new]

relation ProcessWithdrawals:
      rule len0:
 790:  566         -- let withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} = $get_expected_withdrawals(state)
 791:  566/####     -- if (executionPayload.WITHDRAWALS = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*})
 792:  566/####     -- rel ApplyWithdrawals: state .BALANCES{ withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} }~>* state_after_debits
 793:  551/  15     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| = 0)
 794:  551         -- let vid_next = ((state_after_debits.NEXT_WITHDRAWAL_VALIDATOR_INDEX + $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP) \ |state_after_debits.VALIDATORS|)
       551          state executionPayload ~> state_after_debits[NEXT_WITHDRAWAL_VALIDATOR_INDEX = vid_next]
      rule lenMax:
 795:   15         -- let withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} = $get_expected_withdrawals(state)
 796:   15/####     -- if (executionPayload.WITHDRAWALS = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*})
 797:   15/####     -- rel ApplyWithdrawals: state .BALANCES{ withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} }~>* state_after_debits
 798:    3/  12     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| = $MAX_WITHDRAWALS_PER_PAYLOAD)
 799:    3         -- let withdrawal_latest = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}[(|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| - 1)]
 800:    3         -- let withdrawalIndex_next = (withdrawal_latest.INDEX + 1)
 801:    3         -- let vid_next = (withdrawal_latest.VALIDATOR_INDEX + (1 \ |state_after_debits.VALIDATORS|))
         3          state executionPayload ~> state_after_debits[NEXT_WITHDRAWAL_INDEX = withdrawalIndex_next][NEXT_WITHDRAWAL_VALIDATOR_INDEX = vid_next]
      rule lenPartial:
 802:   12         -- let withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} = $get_expected_withdrawals(state)
 803:   12/####     -- if (executionPayload.WITHDRAWALS = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*})
 804:   12/####     -- rel ApplyWithdrawals: state .BALANCES{ withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} }~>* state_after_debits
 805:   12/####     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| =/= 0)
 806:   12/####     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| =/= $MAX_WITHDRAWALS_PER_PAYLOAD)
 807:   12         -- let withdrawal_latest = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}[(|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| - 1)]
 808:   12         -- let withdrawalIndex_next = (withdrawal_latest.INDEX + 1)
 809:   12         -- let vid_next = ((state_after_debits.NEXT_WITHDRAWAL_VALIDATOR_INDEX + $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP) \ |state_after_debits.VALIDATORS|)
        12          state executionPayload ~> state_after_debits[NEXT_WITHDRAWAL_INDEX = withdrawalIndex_next][NEXT_WITHDRAWAL_VALIDATOR_INDEX = vid_next]

relation ProcessExecutionPayload:
      rule main:
 810:  566         -- let executionPayload = body.EXECUTION_PAYLOAD
 811:  566/####     -- if (executionPayload.PARENT_HASH = state.LATEST_EXECUTION_PAYLOAD_HEADER.BLOCK_HASH)
 812:  565/   1     -- if (executionPayload.PREV_RANDAO = $get_randao_mix(state, $get_current_epoch(state)))
 813:  565/####     -- if (executionPayload.TIMESTAMP = $compute_time_at_slot(state, state.SLOT))
 814:  562/   3     -- if (|body.BLOB_KZG_COMMITMENTS| <= $MAX_BLOBS_PER_BLOCK)
 815:  562/####     -- if $ee_verify_and_notify_new_payload(executionPayload)
 816:  562         -- let executionPayloadHeader = {PARENT_HASH executionPayload.PARENT_HASH, FEE_RECIPIENT executionPayload.FEE_RECIPIENT, STATE_ROOT executionPayload.STATE_ROOT, RECEIPTS_ROOT executionPayload.RECEIPTS_ROOT, LOGS_BLOOM executionPayload.LOGS_BLOOM, PREV_RANDAO executionPayload.PREV_RANDAO, BLOCK_NUMBER executionPayload.BLOCK_NUMBER, GAS_LIMIT executionPayload.GAS_LIMIT, GAS_USED executionPayload.GAS_USED, TIMESTAMP executionPayload.TIMESTAMP, EXTRA_DATA executionPayload.EXTRA_DATA, BASE_FEE_PER_GAS executionPayload.BASE_FEE_PER_GAS, BLOCK_HASH executionPayload.BLOCK_HASH, TRANSACTIONS_ROOT $hash_tree_root_tx(executionPayload.TRANSACTIONS), WITHDRAWALS_ROOT $hash_tree_root_withdrawals(executionPayload.WITHDRAWALS), BLOB_GAS_USED executionPayload.BLOB_GAS_USED, EXCESS_BLOB_GAS executionPayload.EXCESS_BLOB_GAS}
       562          state body ~> state[LATEST_EXECUTION_PAYLOAD_HEADER = executionPayloadHeader]

relation ProcessRandao:
      rule :
 817:  562         -- let epoch = $get_current_epoch(state)
 818:  562         -- let vid_proposer = $get_beacon_proposer_index(state)
 819:  562/####     -- if (vid_proposer < |state.VALIDATORS|)
 820:  562         -- let validator_proposer = state.VALIDATORS[vid_proposer]
 821:  562         -- let domain_randao = $get_domain(state, $DOMAIN_RANDAO, ?())
 822:  562         -- let root_signing = $compute_signing_root_epoch(epoch, domain_randao)
 823:  562/####     -- if $bls_verify(validator_proposer.PUBKEY, root_signing, body.RANDAO_REVEAL)
 824:  562         -- let bytes32_mix_old = $get_randao_mix(state, epoch)
 825:  562         -- let bytes32_hash_reveal = $hash_<blsSignature>(body.RANDAO_REVEAL)
 826:  562         -- let bytes32_mix_new = $xor(bytes32_mix_old, bytes32_hash_reveal)
 827:  562         -- let uint64_n = (epoch \ $EPOCHS_PER_HISTORICAL_VECTOR)
       562          state body ~> state[RANDAO_MIXES[uint64_n] = bytes32_mix_new]

relation ProcessEth1Data:
      rule majority:
 828:  562         -- let eth1Data_update*{eth1Data_update <- eth1Data_update*} = state.ETH1_DATA_VOTES ++ [body.ETH1_DATA]
 829:  562         -- let n_occ = $count_occurrences_<eth1Data>(eth1Data_update*{eth1Data_update <- eth1Data_update*}, body.ETH1_DATA)
 830: ####/ 562     -- if ((n_occ * 2) > ($EPOCHS_PER_ETH1_VOTING_PERIOD * $SLOTS_PER_EPOCH))
      ####          state body ~> state[ETH1_DATA_VOTES = eth1Data_update*{eth1Data_update <- eth1Data_update*}][ETH1_DATA = body.ETH1_DATA]
      rule accumulate:
 831:  562         -- let eth1Data_update*{eth1Data_update <- eth1Data_update*} = state.ETH1_DATA_VOTES ++ [body.ETH1_DATA]
 832:  562         -- let n_occ = $count_occurrences_<eth1Data>(eth1Data_update*{eth1Data_update <- eth1Data_update*}, body.ETH1_DATA)
 833:  562/####     -- if ((n_occ * 2) <= ($EPOCHS_PER_ETH1_VOTING_PERIOD * $SLOTS_PER_EPOCH))
       562          state body ~> state[ETH1_DATA_VOTES = eth1Data_update*{eth1Data_update <- eth1Data_update*}]

relation ProcessOperations:
      rule :
 834:  560/   2     -- if (|body.DEPOSITS| = $min_uint64($MAX_DEPOSITS, (state.ETH1_DATA.DEPOSIT_COUNT - state.ETH1_DEPOSIT_INDEX)))
 835:  558/   2     -- rel ProcessProposerSlashings: state body.PROPOSER_SLASHINGS ~> state_ps
 836:  557/   1     -- rel ProcessAttesterSlashings: state_ps body.ATTESTER_SLASHINGS ~> state_as
 837:  557/####     -- rel ProcessAttestations: state_as body.ATTESTATIONS ~> state_att
 838:  557/####     -- rel ProcessDeposits: state_att body.DEPOSITS ~> state_dep
 839:  555/   2     -- rel ProcessVoluntaryExits: state_dep body.VOLUNTARY_EXITS ~> state_ve
 840:  553/   2     -- rel ProcessBlsToExecutionChanges: state_ve body.BLS_TO_EXECUTION_CHANGES ~> state_out
       553          state body ~> state_out

relation ProcessSyncAggregate:
      rule :
 841:  553         -- let blsPubkey_committee*{blsPubkey_committee <- blsPubkey_committee*} = state.CURRENT_SYNC_COMMITTEE.PUBKEYS
 842:  553         -- let (i_committee, blsPubkey)*{blsPubkey <- blsPubkey*, i_committee <- i_committee*} = $enumerate_<blsPubkey>(blsPubkey_committee*{blsPubkey_committee <- blsPubkey_committee*})
 843: ####/####     -- (if (blsPubkey = blsPubkey_committee))*{blsPubkey <- blsPubkey*, blsPubkey_committee <- blsPubkey_committee*}
 844:  553         -- let (_nat, blsPubkey_participants)*{_nat <- _nat*, blsPubkey_participants <- blsPubkey_participants*} = $filter_list_2_<(nat, blsPubkey), boolean*>((i_committee, blsPubkey_committee)*{blsPubkey_committee <- blsPubkey_committee*, i_committee <- i_committee*}, syncAggregate.SYNC_COMMITTEE_BITS, $bit_is_one_en_blsPubkey)
 845:  553         -- let slot_prev = $get_prev_slot(state.SLOT)
 846:  553         -- let domain = $get_domain(state, $DOMAIN_SYNC_COMMITTEE, ?($compute_epoch_at_slot(slot_prev)))
 847:  553         -- let root_sign = $compute_signing_root_block_root($get_block_root_at_slot(state, slot_prev), domain)
 848:  553/####     -- if $eth_fast_aggregate_verify(blsPubkey_participants*{blsPubkey_participants <- blsPubkey_participants*}, root_sign, syncAggregate.SYNC_COMMITTEE_SIGNATURE)
 849:  553         -- let uint64_total_incs = ($get_total_active_balance(state) / $EFFECTIVE_BALANCE_INCREMENT)
 850:  553         -- let reward_total_base = ($get_base_reward_per_increment(state) * uint64_total_incs)
 851:  553         -- let reward_max_participant = (((reward_total_base * $SYNC_REWARD_WEIGHT) / $WEIGHT_DENOMINATOR) / $SLOTS_PER_EPOCH)
 852:  553         -- let reward_participant = (reward_max_participant / $SYNC_COMMITTEE_SIZE)
 853:  553         -- let reward_proposer = ((reward_participant * $PROPOSER_WEIGHT) / ($WEIGHT_DENOMINATOR - $PROPOSER_WEIGHT))
 854:  553         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 855: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 856:  553/####     -- if (state.CURRENT_SYNC_COMMITTEE.PUBKEYS = blsPubkey_committee*{blsPubkey_committee <- blsPubkey_committee*})
 857: ####         -- (let vid_committee = $index_of_pubkey((vid, blsPubkey_existing)*{blsPubkey_existing <- blsPubkey_existing*, vid <- vid*}, blsPubkey_committee))*{blsPubkey_committee <- blsPubkey_committee*, vid_committee <- vid_committee*}
 858:  553         -- let boolean_participation*{boolean_participation <- boolean_participation*} = syncAggregate.SYNC_COMMITTEE_BITS
 859:  553         -- let vid_proposer = $get_beacon_proposer_index(state)
 860:  553/####     -- rel ApplyReward_over_participants: state PARTICIPANT boolean_participation*{boolean_participation <- boolean_participation*} [ vid_committee*{vid_committee <- vid_committee*} ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_updated
       553          state syncAggregate ~> state_updated

relation ProcessBlock:
      rule :
 861:  566/   3     -- rel ProcessBlockHeader: state block ~> state_0
 862:  566/####     -- rel ProcessWithdrawals: state_0 block.BODY.EXECUTION_PAYLOAD ~> state_1
 863:  562/   4     -- rel ProcessExecutionPayload: state_1 block.BODY ~> state_2
 864:  562/####     -- rel ProcessRandao: state_2 block.BODY ~> state_3
 865:  562/####     -- rel ProcessEth1Data: state_3 block.BODY ~> state_4
 866:  553/   9     -- rel ProcessOperations: state_4 block.BODY ~> state_5
 867:  553/####     -- rel ProcessSyncAggregate: state_5 block.BODY.SYNC_AGGREGATE ~> state_6
       553          state block ~>* state_6

relation State_transition:
      rule validate:
 868: ####/ 572     -- if (bool = true)
 869: ####         -- let beaconBlock = signedBeaconBlock.MESSAGE
 870: ####/####     -- rel ProcessSlots: state beaconBlock.SLOT ~> state_cur
 871: ####/####     -- if $verify_block_signature(state_cur, signedBeaconBlock)
 872: ####/####     -- rel ProcessBlock: state_cur beaconBlock ~>* state_next
 873: ####/####     -- if (beaconBlock.STATE_ROOT = $hash_tree_root_beaconState(state_next))
      ####          state signedBeaconBlock bool ~>* state_next
      rule skip-validate:
 874:  572/####     -- if (bool = false)
 875:  572         -- let beaconBlock = signedBeaconBlock.MESSAGE
 876:  569/   3     -- rel ProcessSlots: state beaconBlock.SLOT ~> state_cur
 877:  553/  16     -- rel ProcessBlock: state_cur beaconBlock ~>* state_next
       553          state signedBeaconBlock bool ~>* state_next
