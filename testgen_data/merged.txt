
=== IL Node Coverage ===

IL Premises: 830/873 attempted (95.07%), 813/873 succeeded (93.13%)
108 rule premises
317 if-premises : 9 never attempted, 176 attempted but never failed, 132 attempted and failed

def $UINT64_MAX:
      clause 0:

def $UINT64_MAX_SQRT:
      clause 0:

def $GENESIS_SLOT:
      clause 0:

def $GENESIS_EPOCH:
      clause 0:

def $FAR_FUTURE_EPOCH:
      clause 0:

def $BASE_REWARDS_PER_EPOCH:
      clause 0:

def $DEPOSIT_CONTRACT_TREE_DEPTH:
      clause 0:

def $JUSTIFICATION_BITS_LENGTH:
      clause 0:

def $BLS_WITHDRAWAL_PREFIX:
      clause 0:

def $ETH1_ADDRESS_WITHDRAWAL_PREFIX:
      clause 0:

def $DOMAIN_BEACON_PROPOSER:
      clause 0:

def $DOMAIN_BEACON_ATTESTER:
      clause 0:

def $DOMAIN_RANDAO:
      clause 0:

def $DOMAIN_DEPOSIT:
      clause 0:

def $DOMAIN_VOLUNTARY_EXIT:
      clause 0:

def $DOMAIN_SELECTION_PROOF:
      clause 0:

def $DOMAIN_AGGREGATE_AND_PROOF:
      clause 0:

def $DOMAIN_APPLICATION_MASK:
      clause 0:

def $INTERVALS_PER_SLOT:
      clause 0:

def $BASIS_POINTS:
      clause 0:

def $NODE_ID_BITS:
      clause 0:

def $MAX_CONCURRENT_REQUESTS:
      clause 0:

def $TARGET_AGGREGATORS_PER_COMMITTEE_CONST:
      clause 0:

def $ETH_TO_GWEI:
      clause 0:

def $SAFETY_DECAY:
      clause 0:

def $TIMELY_SOURCE_FLAG_INDEX:
      clause 0:

def $TIMELY_TARGET_FLAG_INDEX:
      clause 0:

def $TIMELY_HEAD_FLAG_INDEX:
      clause 0:

def $TIMELY_SOURCE_WEIGHT:
      clause 0:

def $TIMELY_TARGET_WEIGHT:
      clause 0:

def $TIMELY_HEAD_WEIGHT:
      clause 0:

def $SYNC_REWARD_WEIGHT:
      clause 0:

def $PROPOSER_WEIGHT:
      clause 0:

def $WEIGHT_DENOMINATOR:
      clause 0:

def $DOMAIN_SYNC_COMMITTEE:
      clause 0:

def $DOMAIN_SYNC_COMMITTEE_SELECTION_PROOF:
      clause 0:

def $DOMAIN_CONTRIBUTION_AND_PROOF:
      clause 0:

def $PARTICIPATION_FLAG_WEIGHTS:
      clause 0:

def $G2_POINT_AT_INFINITY:
      clause 0:

def $TARGET_AGGREGATORS_PER_SYNC_SUBCOMMITTEE:
      clause 0:

def $SYNC_COMMITTEE_SUBNET_COUNT:
      clause 0:

def $MAX_REQUEST_LIGHT_CLIENT_UPDATES:
      clause 0:

def $SAFE_SLOTS_TO_IMPORT_OPTIMISTICALLY:
      clause 0:

def $DOMAIN_BLS_TO_EXECUTION_CHANGE:
      clause 0:

def $MAX_COMMITTEES_PER_SLOT:
      clause 0:

def $TARGET_COMMITTEE_SIZE:
      clause 0:

def $MAX_VALIDATORS_PER_COMMITTEE:
      clause 0:

def $SHUFFLE_ROUND_COUNT:
      clause 0:

def $HYSTERESIS_QUOTIENT:
      clause 0:

def $HYSTERESIS_DOWNWARD_MULTIPLIER:
      clause 0:

def $HYSTERESIS_UPWARD_MULTIPLIER:
      clause 0:

def $MIN_DEPOSIT_AMOUNT:
      clause 0:

def $MAX_EFFECTIVE_BALANCE:
      clause 0:

def $EFFECTIVE_BALANCE_INCREMENT:
      clause 0:

def $MIN_ATTESTATION_INCLUSION_DELAY:
      clause 0:

def $SLOTS_PER_EPOCH:
      clause 0:

def $MIN_SEED_LOOKAHEAD:
      clause 0:

def $MAX_SEED_LOOKAHEAD:
      clause 0:

def $MIN_EPOCHS_TO_INACTIVITY_PENALTY:
      clause 0:

def $EPOCHS_PER_ETH1_VOTING_PERIOD:
      clause 0:

def $SLOTS_PER_HISTORICAL_ROOT:
      clause 0:

def $EPOCHS_PER_HISTORICAL_VECTOR:
      clause 0:

def $EPOCHS_PER_SLASHINGS_VECTOR:
      clause 0:

def $HISTORICAL_ROOTS_LIMIT:
      clause 0:

def $VALIDATOR_REGISTRY_LIMIT:
      clause 0:

def $BASE_REWARD_FACTOR:
      clause 0:

def $WHISTLEBLOWER_REWARD_QUOTIENT:
      clause 0:

def $PROPOSER_REWARD_QUOTIENT:
      clause 0:

def $INACTIVITY_PENALTY_QUOTIENT:
      clause 0:

def $MIN_SLASHING_PENALTY_QUOTIENT:
      clause 0:

def $PROPORTIONAL_SLASHING_MULTIPLIER:
      clause 0:

def $MAX_PROPOSER_SLASHINGS:
      clause 0:

def $MAX_ATTESTER_SLASHINGS:
      clause 0:

def $MAX_ATTESTATIONS:
      clause 0:

def $MAX_DEPOSITS:
      clause 0:

def $MAX_VOLUNTARY_EXITS:
      clause 0:

def $INACTIVITY_PENALTY_QUOTIENT_ALTAIR:
      clause 0:

def $MIN_SLASHING_PENALTY_QUOTIENT_ALTAIR:
      clause 0:

def $PROPORTIONAL_SLASHING_MULTIPLIER_ALTAIR:
      clause 0:

def $SYNC_COMMITTEE_SIZE:
      clause 0:

def $EPOCHS_PER_SYNC_COMMITTEE_PERIOD:
      clause 0:

def $MIN_SYNC_COMMITTEE_PARTICIPANTS:
      clause 0:

def $UPDATE_TIMEOUT:
      clause 0:

def $INACTIVITY_PENALTY_QUOTIENT_BELLATRIX:
      clause 0:

def $MIN_SLASHING_PENALTY_QUOTIENT_BELLATRIX:
      clause 0:

def $PROPORTIONAL_SLASHING_MULTIPLIER_BELLATRIX:
      clause 0:

def $MAX_BYTES_PER_TRANSACTION:
      clause 0:

def $MAX_TRANSACTIONS_PER_PAYLOAD:
      clause 0:

def $BYTES_PER_LOGS_BLOOM:
      clause 0:

def $MAX_EXTRA_DATA_BYTES:
      clause 0:

def $MAX_BLS_TO_EXECUTION_CHANGES:
      clause 0:

def $MAX_WITHDRAWALS_PER_PAYLOAD:
      clause 0:

def $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP:
      clause 0:

def $MIN_GENESIS_ACTIVE_VALIDATOR_COUNT:
      clause 0:

def $MIN_GENESIS_TIME:
      clause 0:

def $GENESIS_FORK_VERSION:
      clause 0:

def $GENESIS_DELAY:
      clause 0:

def $SECONDS_PER_SLOT:
      clause 0:

def $SLOT_DURATION_MS:
      clause 0:

def $SECONDS_PER_ETH1_BLOCK:
      clause 0:

def $MIN_VALIDATOR_WITHDRAWABILITY_DELAY:
      clause 0:

def $SHARD_COMMITTEE_PERIOD:
      clause 0:

def $ETH1_FOLLOW_DISTANCE:
      clause 0:

def $EJECTION_BALANCE:
      clause 0:

def $MIN_PER_EPOCH_CHURN_LIMIT:
      clause 0:

def $CHURN_LIMIT_QUOTIENT:
      clause 0:

def $PROPOSER_SCORE_BOOST:
      clause 0:

def $REORG_HEAD_WEIGHT_THRESHOLD:
      clause 0:

def $REORG_PARENT_WEIGHT_THRESHOLD:
      clause 0:

def $REORG_MAX_EPOCHS_SINCE_FINALIZATION:
      clause 0:

def $PROPOSER_REORG_CUTOFF_BPS:
      clause 0:

def $MAX_PAYLOAD_SIZE:
      clause 0:

def $MAX_REQUEST_BLOCKS:
      clause 0:

def $EPOCHS_PER_SUBNET_SUBSCRIPTION:
      clause 0:

def $MIN_EPOCHS_FOR_BLOCK_REQUESTS:
      clause 0:

def $ATTESTATION_PROPAGATION_SLOT_RANGE:
      clause 0:

def $MAXIMUM_GOSSIP_CLOCK_DISPARITY:
      clause 0:

def $MESSAGE_DOMAIN_INVALID_SNAPPY:
      clause 0:

def $MESSAGE_DOMAIN_VALID_SNAPPY:
      clause 0:

def $SUBNETS_PER_NODE:
      clause 0:

def $ATTESTATION_SUBNET_COUNT:
      clause 0:

def $ATTESTATION_SUBNET_EXTRA_BITS:
      clause 0:

def $ATTESTATION_SUBNET_PREFIX_BITS:
      clause 0:

def $ATTESTATION_DUE_BPS:
      clause 0:

def $AGGREGATE_DUE_BPS:
      clause 0:

def $INACTIVITY_SCORE_BIAS:
      clause 0:

def $INACTIVITY_SCORE_RECOVERY_RATE:
      clause 0:

def $ALTAIR_FORK_VERSION:
      clause 0:

def $ALTAIR_FORK_EPOCH:
      clause 0:

def $SYNC_MESSAGE_DUE_BPS:
      clause 0:

def $CONTRIBUTION_DUE_BPS:
      clause 0:

def $TERMINAL_TOTAL_DIFFICULTY:
      clause 0:

def $TERMINAL_BLOCK_HASH:
      clause 0:

def $TERMINAL_BLOCK_HASH_ACTIVATION_EPOCH:
      clause 0:

def $BELLATRIX_FORK_VERSION:
      clause 0:

def $BELLATRIX_FORK_EPOCH:
      clause 0:

def $CAPELLA_FORK_VERSION:
      clause 0:

def $CAPELLA_FORK_EPOCH:
      clause 0:

def $ZERO_ROOT:
      clause 0:

def $MAX_RANDOM_BYTE:
      clause 0:

def $bls_verify:

def $bls_fast_aggregate_verify:

def $eth_aggregate_pubkeys:

def $hash_:

def $debug_print_:

def $debug_print_label_:

def $hash_tree_root_beaconBlockHeader:

def $hash_tree_root_beaconBlockBody:

def $hash_tree_root_depositData:

def $hash_tree_root_forkdata:

def $hash_tree_root_beaconState:

def $hash_tree_root_withdrawals:

def $hash_tree_root_beaconBlock:

def $hash_tree_root_roots:

def $hash_tree_root_tx:

def $compute_signing_root_epoch:

def $compute_signing_root_voluntary_exit:

def $compute_signing_root_bls_to_execution_change:

def $compute_signing_root_beaconBlockHeader:

def $compute_signing_root_attestationData:

def $compute_signing_root_depositMessage:

def $compute_signing_root_block_root:

def $compute_signing_root_beaconBlock:

def $is_valid_merkle_branch:

def $integer_square_root:

def $count_occurrences_:

def $pow2:

def $to_set_:

def $sum_:

def $repeat_:

def $sort_:

def $set_intersection_:

def $set_or_append_list_:

def $range:

def $enumerate_:

def $bytes_to_uint64:

def $uint_to_bytes:

def $xor:

def $first_28_bytes:

def $get_first_byte:

def $strip_first_byte:

def $concat_domain:

def $make_withdrawal_credentials_eth1:

def $uint8_to_bytes_le:

def $uint32_to_bytes_le:

def $uint64_to_bytes_le:

def $bytes32_prefix8:

def $bytes8_to_uint64_le:

def $shr_uint64:

def $and_uint64:

def $bytes32_to_bytes1_list:

def $bytes1_to_uint64:

def $concat_bytes:

def $bytes32_to_bytes:

def $bytes4_to_bytes:

def $extract_execution_address:

def $filter_list_:
      clause 0:
   0:  715/342983     -- if X*{X <- X*} matches []
       715          SUCCESS
      clause 1:
   1: 342983/####     -- if X*{X <- X*} matches _ :: _
   2: 342983         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
   3: 68391/274592     -- if ($cond(X_h) = true)
      68391          SUCCESS
      clause 2:
   4: 274592/####     -- if X*{X <- X*} matches _ :: _
   5: 274592         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
   6: 274592/####     -- if ($cond(X_h) = false)
      274592          SUCCESS

def $filter_list_2_:
      clause 0:
   7: 381921/137674537     -- if X*{X <- X*} matches []
      381921          SUCCESS
      clause 1:
   8: 137674537/####     -- if X*{X <- X*} matches _ :: _
   9: 137674537         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
  10: 94679200/42995337     -- if ($cond(X_h, Y) = true)
      94679200          SUCCESS
      clause 2:
  11: 42995337/####     -- if X*{X <- X*} matches _ :: _
  12: 42995337         -- let X_h :: X_t*{X_t <- X_t*} = X*{X <- X*}
  13: 42995337/####     -- if ($cond(X_h, Y) = false)
      42995337          SUCCESS

def $fold_:
      clause 0:
  14: 102379/9218866     -- if Y*{Y <- Y*} matches []
      102379          SUCCESS
      clause 1:
  15: 9218866/####     -- if Y*{Y <- Y*} matches _ :: _
  16: 9218866         -- let Y_h :: Y_t*{Y_t <- Y_t*} = Y*{Y <- Y*}
      9218866          SUCCESS

def $has_flag:
      clause 0:
  17: ####/56997904     -- if (n >= 8)
      ####          SUCCESS
      clause 1:
  18: 8914599/48083305     -- if (((participationFlags / $pow2(n)) \ 2) = 1)
      8914599          SUCCESS
      clause 2:
  19: 48083305         -- otherwise
      48083305          SUCCESS

def $add_flag:
      clause 0:
  20: ####/17623     -- if (n >= 8)
      ####          SUCCESS
      clause 1:
  21: ####/17623     -- if (((participationFlags / $pow2(n)) \ 2) = 1)
      ####          SUCCESS
      clause 2:
  22: 17623         -- otherwise
      17623          SUCCESS

def $max_uint64:
      clause 0:
  23: 4692227/4668739     -- if (x >= y)
      4692227          SUCCESS
      clause 1:
  24: 4668739         -- otherwise
      4668739          SUCCESS

def $min_uint64:
      clause 0:
  25: 1237/28553     -- if (x <= y)
      1237          SUCCESS
      clause 1:
  26: 28553         -- otherwise
      28553          SUCCESS

def $eth_fast_aggregate_verify:
      clause 0:
  27:  527/  36     -- if (|blsPubkey*{blsPubkey <- blsPubkey*}| = 0)
  28:  527/####     -- if (blsSignature = $G2_POINT_AT_INFINITY)
       527          SUCCESS
      clause 1:
  29:   36         -- otherwise
        36          SUCCESS

def $ee_verify_and_notify_new_payload:

def $index_of_pubkey:
      clause 0:
  30: 39046469/####     -- if (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*} matches _ :: _
  31: 39046469         -- let (vid_h, blsPubkey_h) :: (vid_t, blsPubkey_t)*{blsPubkey_t <- blsPubkey_t*, vid_t <- vid_t*} = (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*}
  32: 288259/38758210     -- if (blsPubkey_h = blsPubkey_committee)
      288259          SUCCESS
      clause 1:
  33: 38758210/####     -- if (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*} matches _ :: _
  34: 38758210         -- let (vid_h, blsPubkey_h) :: (vid_t, blsPubkey_t)*{blsPubkey_t <- blsPubkey_t*, vid_t <- vid_t*} = (validatorIndex, blsPubkey)*{(validatorIndex, blsPubkey) <- (validatorIndex, blsPubkey)*}
  35: 38758210         -- otherwise
      38758210          SUCCESS

def $compute_epoch_at_slot:
      clause 0:

def $compute_start_slot_at_epoch:
      clause 0:

def $get_current_epoch:
      clause 0:

def $get_previous_epoch:
      clause 0:
  36: ####/319329     -- if ($get_current_epoch(state) = $GENESIS_EPOCH)
      ####          SUCCESS
      clause 1:
  37: 319329         -- otherwise
      319329          SUCCESS

def $compute_time_at_slot:
      clause 0:

def $get_prev_slot:
      clause 0:
  38: ####/ 563     -- if (slot = 0)
      ####          SUCCESS
      clause 1:
  39:  563         -- otherwise
       563          SUCCESS

def $compute_fork_data_root:
      clause 0:

def $compute_domain:
      clause 0:
  40: 5482/ 711     -- if version'?{version' <- version'?} matches (_)
  41: 5482         -- let ?(version) = version'?{version' <- version'?}
  42: 5482/####     -- if root'?{root' <- root'?} matches (_)
  43: 5482         -- let ?(root) = root'?{root' <- root'?}
      5482          SUCCESS
      clause 1:
  44:  711/####     -- if version?{version <- version?} matches ()
  45:  458/ 253     -- if root?{root <- root?} matches ()
       458          SUCCESS
      clause 2:
  46: ####/ 253     -- if version'?{version' <- version'?} matches (_)
  47: ####         -- let ?(version) = version'?{version' <- version'?}
  48: ####/####     -- if root?{root <- root?} matches ()
      ####          SUCCESS
      clause 3:
  49:  253/####     -- if version?{version <- version?} matches ()
  50:  253/####     -- if root'?{root' <- root'?} matches (_)
  51:  253         -- let ?(root) = root'?{root' <- root'?}
       253          SUCCESS

def $get_domain:
      clause 0:
  52: 1150/4332     -- if epoch?{epoch <- epoch?} matches ()
  53: 1150         -- let epoch_cur = $get_current_epoch(state)
  54: ####/1150     -- if (epoch_cur < state.FORK.EPOCH)
      ####          SUCCESS
      clause 1:
  55: 1150/4332     -- if epoch?{epoch <- epoch?} matches ()
  56: 1150         -- let epoch_cur = $get_current_epoch(state)
  57: 1150         -- otherwise
      1150          SUCCESS
      clause 2:
  58: 4332/####     -- if epoch'?{epoch' <- epoch'?} matches (_)
  59: 4332         -- let ?(epoch) = epoch'?{epoch' <- epoch'?}
  60: ####/4332     -- if (epoch < state.FORK.EPOCH)
      ####          SUCCESS
      clause 3:
  61: 4332/####     -- if epoch'?{epoch' <- epoch'?} matches (_)
  62: 4332         -- let ?(epoch) = epoch'?{epoch' <- epoch'?}
  63: 4332         -- otherwise
      4332          SUCCESS

def $verify_block_signature:
      clause 0:
  64:  578         -- let validator_proposer = state.VALIDATORS[signedBeaconBlock.MESSAGE.PROPOSER_INDEX]
  65:  578         -- let root_signing = $compute_signing_root_beaconBlock(signedBeaconBlock.MESSAGE, $get_domain(state, $DOMAIN_BEACON_PROPOSER, ?()))
  66:  575/   3     -- if $bls_verify(validator_proposer.PUBKEY, root_signing, signedBeaconBlock.SIGNATURE)
       575          SUCCESS
      clause 1:
  67:    3         -- otherwise
         3          SUCCESS

def $compute_activation_exit_epoch:
      clause 0:

def $is_active_validator:
      clause 0:
  68: 94605995/18458486     -- if (validator.ACTIVATION_EPOCH <= epoch)
  69: 77787935/16818060     -- if (epoch < validator.EXIT_EPOCH)
      77787935          SUCCESS
      clause 1:
  70: 35276546         -- otherwise
      35276546          SUCCESS

def $is_eligible_for_activation_queue:
      clause 0:
  71:  416/168334     -- if (validator.ACTIVATION_ELIGIBILITY_EPOCH = $FAR_FUTURE_EPOCH)
  72:  416/####     -- if (validator.EFFECTIVE_BALANCE = $MAX_EFFECTIVE_BALANCE)
       416          SUCCESS
      clause 1:
  73: 168334         -- otherwise
      168334          SUCCESS

def $is_eligible_for_activation:
      clause 0:
  74: 81487/ 480     -- if (validator.ACTIVATION_ELIGIBILITY_EPOCH <= state.FINALIZED_CHECKPOINT.EPOCH)
  75: 2993/78494     -- if (validator.ACTIVATION_EPOCH = $FAR_FUTURE_EPOCH)
      2993          SUCCESS
      clause 1:
  76: 78974         -- otherwise
      78974          SUCCESS

def $is_slashable_validator:
      clause 0:
  77:  398/  22     -- if ~validator.SLASHED
  78:  398/####     -- if (validator.ACTIVATION_EPOCH <= epoch)
  79:  398/####     -- if (epoch < validator.WITHDRAWABLE_EPOCH)
       398          SUCCESS
      clause 1:
  80:   22         -- otherwise
        22          SUCCESS

def $has_eth1_withdrawal_credential:
      clause 0:

def $is_fully_withdrawable_validator:
      clause 0:
  81: 1070/517839     -- if ($has_eth1_withdrawal_credential(validator) = true)
  82:   82/ 988     -- if (validator.WITHDRAWABLE_EPOCH <= epoch)
  83:   79/   3     -- if (balance > 0)
        79          SUCCESS
      clause 1:
  84: 518830         -- otherwise
      518830          SUCCESS

def $is_partially_withdrawable_validator:
      clause 0:
  85:  585/345226     -- if ($has_eth1_withdrawal_credential(validator) = true)
  86:  229/ 356     -- if (validator.EFFECTIVE_BALANCE = $MAX_EFFECTIVE_BALANCE)
  87:  227/   2     -- if (balance > $MAX_EFFECTIVE_BALANCE)
       227          SUCCESS
      clause 1:
  88: 345584         -- otherwise
      345584          SUCCESS

def $is_active_validator_enumerated:
      clause 0:

def $get_active_validator_indices:
      clause 0:
  89: 204845         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
  90: 204845         -- let (vid_active, _validator)*{_validator <- _validator*, vid_active <- vid_active*} = $filter_list_2_<(validatorIndex, validator), epoch>((vid, validator)*{validator <- validator*, vid <- vid*}, epoch, $is_active_validator_enumerated)
      204845          SUCCESS

def $get_validator_churn_limit:
      clause 0:
  91:  811         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, $get_current_epoch(state))
       811          SUCCESS

def $get_validator_from_deposit:
      clause 0:
  92:  458         -- let balance_effective = $min_uint64((uint64_amount - (uint64_amount \ $EFFECTIVE_BALANCE_INCREMENT)), $MAX_EFFECTIVE_BALANCE)
       458          SUCCESS

relation AddValidatorToRegistry:
      rule :
  93:  458         -- let vid_new = |state.VALIDATORS|
  94:  458         -- let validator = $get_validator_from_deposit(blsPubkey, bytes32_wc, uint64_amount)
  95:  458         -- let validator_updated*{validator_updated <- validator_updated*} = $set_or_append_list_<validator>(state.VALIDATORS, vid_new, validator)
  96:  458         -- let balance_updated*{balance_updated <- balance_updated*} = $set_or_append_list_<gwei>(state.BALANCES, vid_new, uint64_amount)
  97:  458         -- let participationFlags_prev_updated*{participationFlags_prev_updated <- participationFlags_prev_updated*} = $set_or_append_list_<participationFlags>(state.PREVIOUS_EPOCH_PARTICIPATION, vid_new, 0)
  98:  458         -- let participationFlags_cur_updated*{participationFlags_cur_updated <- participationFlags_cur_updated*} = $set_or_append_list_<participationFlags>(state.CURRENT_EPOCH_PARTICIPATION, vid_new, 0)
  99:  458         -- let uint64_inactivity_updated*{uint64_inactivity_updated <- uint64_inactivity_updated*} = $set_or_append_list_<uint64>(state.INACTIVITY_SCORES, vid_new, 0)
       458          SUCCESS

def $is_eligible_validator_enumerated:
      clause 0:

def $get_eligible_validator_indices:
      clause 0:
 100:  450         -- let epoch_prev = $get_previous_epoch(state)
 101:  450         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 102:  450         -- let (vid_eligible, _validator)*{_validator <- _validator*, vid_eligible <- vid_eligible*} = $filter_list_2_<(validatorIndex, validator), epoch>((vid, validator)*{validator <- validator*, vid <- vid*}, epoch_prev, $is_eligible_validator_enumerated)
       450          SUCCESS

def $has_flag_for_index:
      clause 0:

def $is_unslashed:
      clause 0:
 103: 9805171/####     -- if (vid < |state.VALIDATORS|)
 104: 9805171         -- let validator = state.VALIDATORS[vid]
 105: 7979949/1825222     -- if (validator.SLASHED = false)
      7979949          SUCCESS
      clause 1:
 106: 1825222         -- otherwise
      1825222          SUCCESS

def $get_unslashed_participating_indices:
      clause 0:
 107:   87/84703     -- if ($get_current_epoch(state) = epoch)
 108:   87         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch)
 109:   87         -- let vid_participating*{vid_participating <- vid_participating*} = $filter_list_2_<validatorIndex, (participationFlags*, validatorIndex)>(vid_active*{vid_active <- vid_active*}, (state.CURRENT_EPOCH_PARTICIPATION, vid_flag), $has_flag_for_index)
 110:   87         -- let vid_unslashed*{vid_unslashed <- vid_unslashed*} = $filter_list_2_<validatorIndex, beaconState>(vid_participating*{vid_participating <- vid_participating*}, state, $is_unslashed)
        87          SUCCESS
      clause 1:
 111: 84703/####     -- if ($get_previous_epoch(state) = epoch)
 112: 84703         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch)
 113: 84703         -- let vid_participating*{vid_participating <- vid_participating*} = $filter_list_2_<validatorIndex, (participationFlags*, nat)>(vid_active*{vid_active <- vid_active*}, (state.PREVIOUS_EPOCH_PARTICIPATION, vid_flag), $has_flag_for_index)
 114: 84703         -- let vid_unslashed*{vid_unslashed <- vid_unslashed*} = $filter_list_2_<validatorIndex, beaconState>(vid_participating*{vid_participating <- vid_participating*}, state, $is_unslashed)
      84703          SUCCESS

def $get_block_root_at_slot:
      clause 0:
 115: 6186/####     -- if (slot < state.SLOT)
 116: 6186/####     -- if (state.SLOT <= (slot + $SLOTS_PER_HISTORICAL_ROOT))
 117: 6186         -- let root = state.BLOCK_ROOTS[(slot \ $SLOTS_PER_HISTORICAL_ROOT)]
      6186          SUCCESS

def $get_block_root:
      clause 0:

def $get_randao_mix:
      clause 0:

def $get_finality_delay:
      clause 0:
 118: 73883/####     -- if ($get_previous_epoch(state) >= state.FINALIZED_CHECKPOINT.EPOCH)
      73883          SUCCESS

def $is_in_inactivity_leak:
      clause 0:

def $choose_index:
      clause 0:
 119: 4589589/4560171     -- if (nat_bit = 1)
      4589589          SUCCESS
      clause 1:
 120: 4560171         -- otherwise
      4560171          SUCCESS

def $apply_shuffle_round:
      clause 0:
 121: 9149760         -- let bytes_seed = $bytes32_to_bytes(seed)
 122: 9149760         -- let bytes_round8 = $uint8_to_bytes_le((uint64_round \ 256))
 123: 9149760         -- let bytes32_round_hash = $hash_<bytes>($concat_bytes(bytes_seed, bytes_round8))
 124: 9149760         -- let bytes8_prefix = $bytes32_prefix8(bytes32_round_hash)
 125: 9149760         -- let uint64_pivot_raw = $bytes8_to_uint64_le(bytes8_prefix)
 126: 9149760         -- let uint64_pivot = (uint64_pivot_raw \ i_count)
 127: 9149760         -- let uint64_flip = (((uint64_pivot + i_count) - i) \ i_count)
 128: 9149760         -- let uint64_position = $max_uint64(i, uint64_flip)
 129: 9149760         -- let bytes_bucket = $uint32_to_bytes_le((uint64_position / 256))
 130: 9149760         -- let bytes32_source = $hash_<bytes>($concat_bytes($concat_bytes(bytes_seed, bytes_round8), bytes_bucket))
 131: 9149760         -- let uint64_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_source)[((uint64_position \ 256) / 8)])
 132: 9149760         -- let nat_bit = ($and_uint64($shr_uint64(uint64_byte, (uint64_position \ 8)), 1) \ 2)
 133: 9149760         -- let i_out = $choose_index(i, uint64_flip, nat_bit)
      9149760          SUCCESS

def $compute_shuffled_index:
      clause 0:
 134: 101664         -- let uint64_round*{uint64_round <- uint64_round*} = $range($SHUFFLE_ROUND_COUNT)
 135: 101664         -- let uint64_round_first = uint64_round*{uint64_round <- uint64_round*}[0]
 136: 101664         -- let uint64_round_last = uint64_round*{uint64_round <- uint64_round*}[($SHUFFLE_ROUND_COUNT - 1)]
 137: 101664         -- let i_shuffled = $fold_<uint64, (bytes32, uint64, uint64)>($apply_shuffle_round, i, (seed, i_count, uint64_round)*{uint64_round <- uint64_round*})
      101664          SUCCESS

def $compute_proposer_index_loop:
      clause 0:
 138: 5338         -- let vid_candidate = vid*{vid <- vid*}[$compute_shuffled_index((i \ uint64_total), uint64_total, seed)]
 139: 5338         -- let bytes32_h = $hash_<bytes>($concat_bytes($bytes32_to_bytes(seed), $uint64_to_bytes_le((i / 32))))
 140: 5338         -- let uint64_random_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_h)[(i \ 32)])
 141: 5338         -- let balance_effective = state.VALIDATORS[vid_candidate].EFFECTIVE_BALANCE
 142: 5338/####     -- if (balance_effective <= ($UINT64_MAX / $MAX_RANDOM_BYTE))
 143: 4896/ 442     -- if ((balance_effective * $MAX_RANDOM_BYTE) >= ($MAX_EFFECTIVE_BALANCE * uint64_random_byte))
      4896          SUCCESS
      clause 1:
 144:  442         -- otherwise
       442          SUCCESS

def $compute_proposer_index:
      clause 0:
 145: 4896/####     -- if (|vid*{vid <- vid*}| > 0)
      4896          SUCCESS

def $get_seed:
      clause 0:
 146: 13261         -- let bytes32_mix = $get_randao_mix(state, (((epoch + $EPOCHS_PER_HISTORICAL_VECTOR) - $MIN_SEED_LOOKAHEAD) - 1))
      13261          SUCCESS

def $get_beacon_proposer_index:
      clause 0:
 147: 4896         -- let epoch_cur = $get_current_epoch(state)
 148: 4896         -- let seed = $hash_<bytes>($concat_bytes($bytes32_to_bytes($get_seed(state, epoch_cur, $DOMAIN_BEACON_PROPOSER)), $uint64_to_bytes_le(state.SLOT)))
 149: 4896         -- let vid*{vid <- vid*} = $get_active_validator_indices(state, epoch_cur)
      4896          SUCCESS

relation IncreaseBalance:
      rule :
 150: 199107/####     -- if (vid < |state.BALANCES|)
 151: 199107         -- let balance = state.BALANCES[vid]
      199107          SUCCESS

relation DecreaseBalance:
      rule zero:
 152: 436864/####     -- if (vid < |state.BALANCES|)
 153: 436864         -- let balance = state.BALANCES[vid]
 154:   59/436805     -- if (delta > balance)
        59          SUCCESS
      rule remains:
 155: 436805/####     -- if (vid < |state.BALANCES|)
 156: 436805         -- let balance = state.BALANCES[vid]
 157: 436805/####     -- if (delta <= balance)
      436805          SUCCESS

def $exit_epoch_not_far_future:
      clause 0:
 158: 68391/549184     -- if (validator.EXIT_EPOCH =/= $FAR_FUTURE_EPOCH)
      68391          SUCCESS
      clause 1:
 159: 549184         -- otherwise
      549184          SUCCESS

def $exit_epoch_eq:
      clause 0:
 160: 1718/133346     -- if (validator.EXIT_EPOCH = epoch)
      1718          SUCCESS
      clause 1:
 161: 133346         -- otherwise
      133346          SUCCESS

def $fold_max_epoch:
      clause 0:

def $select_epoch_exit_queue:
      clause 0:
 162:  167/ 548     -- if (uint64_churn >= uint64_limit)
       167          SUCCESS
      clause 1:
 163:  548         -- otherwise
       548          SUCCESS

relation InitiateValidatorExit:
      rule skip:
 164: 1471/####     -- if (vid < |state.VALIDATORS|)
 165: 1471         -- let validator = state.VALIDATORS[vid]
 166:  756/ 715     -- if (validator.EXIT_EPOCH =/= $FAR_FUTURE_EPOCH)
       756          SUCCESS
      rule main:
 167:  715/####     -- if (vid < |state.VALIDATORS|)
 168:  715         -- let validator = state.VALIDATORS[vid]
 169:  715/####     -- if (validator.EXIT_EPOCH = $FAR_FUTURE_EPOCH)
 170:  715         -- let validator_exit*{validator_exit <- validator_exit*} = $filter_list_<validator>(state.VALIDATORS, $exit_epoch_not_far_future)
 171: ####         -- (let epoch_exit = validator_exit.EXIT_EPOCH)*{epoch_exit <- epoch_exit*, validator_exit <- validator_exit*}
 172:  715         -- let epoch_exit_queue_0 = $fold_<epoch, epoch>($fold_max_epoch, 0, epoch_exit*{epoch_exit <- epoch_exit*} ++ [$compute_activation_exit_epoch($get_current_epoch(state))])
 173:  715         -- let validator_exit_queue*{validator_exit_queue <- validator_exit_queue*} = $filter_list_2_<validator, epoch>(validator_exit*{validator_exit <- validator_exit*}, epoch_exit_queue_0, $exit_epoch_eq)
 174:  715         -- let uint64_exit_queue_churn = |validator_exit_queue*{validator_exit_queue <- validator_exit_queue*}|
 175:  715         -- let uint64_churn_limit = $get_validator_churn_limit(state)
 176:  715         -- let epoch_exit_queue = $select_epoch_exit_queue(epoch_exit_queue_0, uint64_exit_queue_churn, uint64_churn_limit)
 177:  715/####     -- if (epoch_exit_queue <= ($UINT64_MAX - $MIN_VALIDATOR_WITHDRAWABILITY_DELAY))
 178:  715         -- let epoch_withdrawable = (epoch_exit_queue + $MIN_VALIDATOR_WITHDRAWABILITY_DELAY)
       715          SUCCESS

relation SlashValidator:
      rule no_whistleblower:
 179:  398/####     -- if validatorIndex?{validatorIndex <- validatorIndex?} matches ()
 180:  398/####     -- if (vid_slashed < |state.VALIDATORS|)
 181:  398         -- let epoch = $get_current_epoch(state)
 182:  398/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid_slashed ]~> state_init
 183:  398         -- let validator = state_init.VALIDATORS[vid_slashed]
 184:  398         -- let epoch_withdrawable = $max_uint64(validator.WITHDRAWABLE_EPOCH, (epoch + $EPOCHS_PER_SLASHINGS_VECTOR))
 185:  398         -- let state_1 = state_init[VALIDATORS[vid_slashed] = validator[SLASHED = true][WITHDRAWABLE_EPOCH = epoch_withdrawable]]
 186:  398         -- let state_2 = state_1[SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] = (state_1.SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] + validator.EFFECTIVE_BALANCE)]
 187:  398         -- let penalty_slashing = (validator.EFFECTIVE_BALANCE / $MIN_SLASHING_PENALTY_QUOTIENT_BELLATRIX)
 188:  398/####     -- rel DecreaseBalance: state_2 .BALANCES[ vid_slashed ]- penalty_slashing ~> state_penalized
 189:  398         -- let vid_proposer = $get_beacon_proposer_index(state_penalized)
 190:  398         -- let reward_whistleblower = (validator.EFFECTIVE_BALANCE / $WHISTLEBLOWER_REWARD_QUOTIENT)
 191:  398         -- let gwei_prop_num = (reward_whistleblower * $PROPOSER_WEIGHT)
 192:  398         -- let reward_proposer = (gwei_prop_num / $WEIGHT_DENOMINATOR)
 193:  398/####     -- rel IncreaseBalance: state_penalized .BALANCES[ vid_proposer ]+ reward_proposer ~> state_rewarded_proposer
 194:  398         -- let reward_whistleblower_rest = (reward_whistleblower - reward_proposer)
 195:  398/####     -- rel IncreaseBalance: state_rewarded_proposer .BALANCES[ vid_proposer ]+ reward_whistleblower_rest ~> state_out
       398          SUCCESS
      rule whistleblower:
 196: ####/####     -- if validatorIndex?{validatorIndex <- validatorIndex?} matches (_)
 197: ####         -- let ?(vid_whistleblower) = validatorIndex?{validatorIndex <- validatorIndex?}
 198: ####/####     -- if (vid_slashed < |state.VALIDATORS|)
 199: ####         -- let epoch = $get_current_epoch(state)
 200: ####/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid_slashed ]~> state_init
 201: ####         -- let validator = state_init.VALIDATORS[vid_slashed]
 202: ####         -- let epoch_withdrawable = $max_uint64(validator.WITHDRAWABLE_EPOCH, (epoch + $EPOCHS_PER_SLASHINGS_VECTOR))
 203: ####         -- let state_1 = state_init[VALIDATORS[vid_slashed] = validator[SLASHED = true][WITHDRAWABLE_EPOCH = epoch_withdrawable]]
 204: ####         -- let state_2 = state_1[SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] = (state_1.SLASHINGS[(epoch \ $EPOCHS_PER_SLASHINGS_VECTOR)] + validator.EFFECTIVE_BALANCE)]
 205: ####         -- let penalty_slashing = (validator.EFFECTIVE_BALANCE / $MIN_SLASHING_PENALTY_QUOTIENT_BELLATRIX)
 206: ####/####     -- rel DecreaseBalance: state_2 .BALANCES[ vid_slashed ]- penalty_slashing ~> state_penalized
 207: ####         -- let vid_proposer = $get_beacon_proposer_index(state_penalized)
 208: ####         -- let reward_whistleblower = (validator.EFFECTIVE_BALANCE / $WHISTLEBLOWER_REWARD_QUOTIENT)
 209: ####         -- let gwei_prop_num = (reward_whistleblower * $PROPOSER_WEIGHT)
 210: ####         -- let reward_proposer = (gwei_prop_num / $WEIGHT_DENOMINATOR)
 211: ####/####     -- rel IncreaseBalance: state_penalized .BALANCES[ vid_proposer ]+ reward_proposer ~> state_rewarded_proposer
 212: ####         -- let reward_whistleblower_rest = (reward_whistleblower - reward_proposer)
 213: ####/####     -- rel IncreaseBalance: state_rewarded_proposer .BALANCES[ vid_whistleblower ]+ reward_whistleblower_rest ~> state_out
      ####          SUCCESS

relation ApplyDeposit:
      rule new_pubkey:
 214:  461         -- let validator*{validator <- validator*} = state.VALIDATORS
 215: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 216:  458/   3     -- if ~blsPubkey <- blsPubkey_existing*{blsPubkey_existing <- blsPubkey_existing*}
 217:  458         -- let depositMessage = {PUBKEY blsPubkey, WITHDRAWAL_CREDENTIALS bytes32_wc, AMOUNT gwei_amount}
 218:  458         -- let domain = $compute_domain($DOMAIN_DEPOSIT, ?(), ?())
 219:  458/####     -- if $bls_verify(blsPubkey, $compute_signing_root_depositMessage(depositMessage, domain), blsSignature)
 220:  458/####     -- rel AddValidatorToRegistry: state .VALIDATORS++{ blsPubkey bytes32_wc gwei_amount }~> state_out
       458          SUCCESS
      rule new_pubkey_invalid_sig:
 221:    3         -- let validator*{validator <- validator*} = state.VALIDATORS
 222: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 223: ####/   3     -- if ~blsPubkey <- blsPubkey_existing*{blsPubkey_existing <- blsPubkey_existing*}
 224: ####         -- let depositMessage = {PUBKEY blsPubkey, WITHDRAWAL_CREDENTIALS bytes32_wc, AMOUNT gwei_amount}
 225: ####         -- let domain = $compute_domain($DOMAIN_DEPOSIT, ?(), ?())
 226: ####/####     -- if ~$bls_verify(blsPubkey, $compute_signing_root_depositMessage(depositMessage, domain), blsSignature)
      ####          SUCCESS
      rule existing_pubkey:
 227:    3         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 228: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 229:    3/####     -- if blsPubkey <- blsPubkey_existing*{blsPubkey_existing <- blsPubkey_existing*}
 230:    3         -- let vid_found = $index_of_pubkey((vid, blsPubkey_existing)*{blsPubkey_existing <- blsPubkey_existing*, vid <- vid*}, blsPubkey)
 231:    3/####     -- rel IncreaseBalance: state .BALANCES[ vid_found ]+ gwei_amount ~> state_out
         3          SUCCESS

def $compute_committee:
      clause 0:
 232: 8364         -- let i_start = ((|vid*{vid <- vid*}| * i) / count)
 233: 8364         -- let i_end = ((|vid*{vid <- vid*}| * (i + 1)) / count)
 234: 8364         -- let n*{n <- n*} = $range((i_end - i_start))
 235: ####         -- (let i_range = (n + i_start))*{i_range <- i_range*, n <- n*}
 236: ####         -- (let i_shuffled = $compute_shuffled_index(i_range, |vid*{vid <- vid*}|, seed))*{i_range <- i_range*, i_shuffled <- i_shuffled*}
 237: ####         -- (let vid_committee = vid*{vid <- vid*}[i_shuffled])*{i_shuffled <- i_shuffled*, vid_committee <- vid_committee*}
      ####          SUCCESS

def $get_committee_count_per_slot:
      clause 0:

def $get_beacon_committee:
      clause 0:
 238: 8364         -- let epoch = $compute_epoch_at_slot(slot)
 239: 8364         -- let count_committees_per_slot = $get_committee_count_per_slot(state, epoch)
      8364          SUCCESS

def $bit_is_one_at_index:
      clause 0:
 240: 63876/####     -- if (i < |bit*{bit <- bit*}|)
      63876          SUCCESS

def $get_attesting_indices:
      clause 0:
 241: 5576         -- let vid_committee*{vid_committee <- vid_committee*} = $get_beacon_committee(state, attestation.DATA.SLOT, attestation.DATA.INDEX)
 242: 5576         -- let (i, vid)*{i <- i*, vid <- vid*} = $enumerate_<validatorIndex>(vid_committee*{vid_committee <- vid_committee*})
 243: 5576         -- let (_nat, vid_attesting)*{_nat <- _nat*, vid_attesting <- vid_attesting*} = $filter_list_2_<(nat, validatorIndex), boolean*>((i, vid)*{i <- i*, vid <- vid*}, attestation.AGGREGATION_BITS, $bit_is_one_at_index)
 244: 5576         -- let vid_set*{vid_set <- vid_set*} = $to_set_<validatorIndex>(vid_attesting*{vid_attesting <- vid_attesting*})
      5576          SUCCESS

def $is_slashable_attestation_data:
      clause 0:

def $is_valid_indexed_attestation:
      clause 0:
 245: ####/2874     -- if (|indexedAttestation.ATTESTING_INDICES| = 0)
      ####          SUCCESS
      clause 1:
 246: ####/2874     -- if ($sort_<validatorIndex>($to_set_<validatorIndex>(indexedAttestation.ATTESTING_INDICES)) =/= indexedAttestation.ATTESTING_INDICES)
      ####          SUCCESS
      clause 2:
 247: 2874         -- let vid*{vid <- vid*} = indexedAttestation.ATTESTING_INDICES
 248: 2874/####     -- if (|indexedAttestation.ATTESTING_INDICES| > 0)
 249: 2874/####     -- if ($sort_<validatorIndex>($to_set_<validatorIndex>(indexedAttestation.ATTESTING_INDICES)) = indexedAttestation.ATTESTING_INDICES)
 250: ####         -- (let blsPubkey = state.VALIDATORS[vid].PUBKEY)*{blsPubkey <- blsPubkey*, vid <- vid*}
 251: 2874         -- let domain = $get_domain(state, $DOMAIN_BEACON_ATTESTER, ?(indexedAttestation.DATA.TARGET.EPOCH))
 252: 2874         -- let root_signing = $compute_signing_root_attestationData(indexedAttestation.DATA, domain)
      2874          SUCCESS

relation SlashIfEligible:
      rule true:
 253:  120/####     -- if (vid < |state.VALIDATORS|)
 254:  110/  10     -- if $is_slashable_validator(state.VALIDATORS[vid], $get_current_epoch(state))
 255:  110/####     -- rel SlashValidator: state SLASH[ vid ]BY ?() ~> state_slashed
       110          SUCCESS
      rule false:
 256:   10/####     -- if (vid < |state.VALIDATORS|)
 257:   10/####     -- if ~$is_slashable_validator(state.VALIDATORS[vid], $get_current_epoch(state))
        10          SUCCESS

relation SlashIfEligible_over_validators:
      rule nil:
 258:   43/ 120     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        43          SUCCESS
      rule cons:
 259:  120/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 260:  120         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 261:  120/####     -- rel SlashIfEligible: state SLASH?[ vid_h ]~> state_h #SLASHED bool_h
 262:  120/####     -- rel SlashIfEligible_over_validators: state_h SLASH?[ vid_t*{vid_t <- vid_t*} ]~> state_t #SLASHED bool_t
       120          SUCCESS

def $source_flag_list:
      clause 0:
 263:  557/2231     -- if (uint64_inclusion_delay <= $integer_square_root($SLOTS_PER_EPOCH))
       557          SUCCESS
      clause 1:
 264: 2231         -- otherwise
      2231          SUCCESS

def $target_flag_list:
      clause 0:
 265: 2788/####     -- if (uint64_inclusion_delay <= $SLOTS_PER_EPOCH)
      2788          SUCCESS
      clause 1:
 266: ####         -- otherwise
      ####          SUCCESS

def $head_flag_list:
      clause 0:
 267:  258/2530     -- if (uint64_inclusion_delay = $MIN_ATTESTATION_INCLUSION_DELAY)
       258          SUCCESS
      clause 1:
 268: 2530         -- otherwise
      2530          SUCCESS

def $get_attestation_participation_flag_indices:
      clause 0:
 269: 1110/1678     -- if (attestationData.TARGET.EPOCH = $get_current_epoch(state))
 270: 1110         -- let checkpoint_justified = state.CURRENT_JUSTIFIED_CHECKPOINT
 271: 1110/####     -- if (attestationData.SOURCE.EPOCH = checkpoint_justified.EPOCH)
 272: 1110/####     -- if (attestationData.SOURCE.ROOT = checkpoint_justified.ROOT)
 273: 1110         -- let root_target = $get_block_root(state, attestationData.TARGET.EPOCH)
 274: 1110         -- let bool_target_root_matches = (attestationData.TARGET.ROOT = root_target)
 275: 1110         -- let root_head = $get_block_root_at_slot(state, attestationData.SLOT)
 276: 1110         -- let bool_head_root_matches = (attestationData.BEACON_BLOCK_ROOT = root_head)
 277: 1110         -- let flag_source*{flag_source <- flag_source*} = $source_flag_list(uint64_inclusion_delay)
 278: 1110         -- let flag_target*{flag_target <- flag_target*} = $target_flag_list(bool_target_root_matches, uint64_inclusion_delay)
 279: 1110         -- let flag_head*{flag_head <- flag_head*} = $head_flag_list((bool_target_root_matches /\ bool_head_root_matches), uint64_inclusion_delay)
 280: 1110         -- let flag_indices*{flag_indices <- flag_indices*} = flag_source*{flag_source <- flag_source*} ++ flag_target*{flag_target <- flag_target*} ++ flag_head*{flag_head <- flag_head*}
      1110          SUCCESS
      clause 1:
 281: 1678/####     -- if (attestationData.TARGET.EPOCH =/= $get_current_epoch(state))
 282: 1678         -- let checkpoint_justified = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 283: 1678/####     -- if (attestationData.SOURCE.EPOCH = checkpoint_justified.EPOCH)
 284: 1678/####     -- if (attestationData.SOURCE.ROOT = checkpoint_justified.ROOT)
 285: 1678         -- let root_target = $get_block_root(state, attestationData.TARGET.EPOCH)
 286: 1678         -- let bool_target_root_matches = (attestationData.TARGET.ROOT = root_target)
 287: 1678         -- let root_head = $get_block_root_at_slot(state, attestationData.SLOT)
 288: 1678         -- let bool_head_root_matches = (attestationData.BEACON_BLOCK_ROOT = root_head)
 289: 1678         -- let flag_source*{flag_source <- flag_source*} = $source_flag_list(uint64_inclusion_delay)
 290: 1678         -- let flag_target*{flag_target <- flag_target*} = $target_flag_list(bool_target_root_matches, uint64_inclusion_delay)
 291: 1678         -- let flag_head*{flag_head <- flag_head*} = $head_flag_list((bool_target_root_matches /\ bool_head_root_matches), uint64_inclusion_delay)
 292: 1678         -- let flag_indices*{flag_indices <- flag_indices*} = flag_source*{flag_source <- flag_source*} ++ flag_target*{flag_target <- flag_target*} ++ flag_head*{flag_head <- flag_head*}
      1678          SUCCESS

def $next_sync_committee_indices_loop:
      clause 0:
 293:    1/ 512     -- if (|vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}| >= $SYNC_COMMITTEE_SIZE)
         1          SUCCESS
      clause 1:
 294:  512/####     -- if (|vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}| < $SYNC_COMMITTEE_SIZE)
 295:  512         -- let vid_candidate = vid_active*{vid_active <- vid_active*}[$compute_shuffled_index((vid \ count_active), count_active, seed)]
 296:  512         -- let bytes32_h = $hash_<bytes>($concat_bytes($bytes32_to_bytes(seed), $uint_to_bytes((vid / 32))))
 297:  512         -- let uint64_random_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_h)[(vid \ 32)])
 298:  512         -- let balance_effective = state.VALIDATORS[vid_candidate].EFFECTIVE_BALANCE
 299:  512/####     -- if (balance_effective <= ($UINT64_MAX / $MAX_RANDOM_BYTE))
 300:  512/####     -- if ((balance_effective * $MAX_RANDOM_BYTE) >= ($MAX_EFFECTIVE_BALANCE * uint64_random_byte))
       512          SUCCESS
      clause 2:
 301: ####/####     -- if (|vid_sync_committee*{vid_sync_committee <- vid_sync_committee*}| < $SYNC_COMMITTEE_SIZE)
 302: ####         -- let vid_candidate = vid_active*{vid_active <- vid_active*}[$compute_shuffled_index((vid \ count_active), count_active, seed)]
 303: ####         -- let bytes32_h = $hash_<bytes>($concat_bytes($bytes32_to_bytes(seed), $uint_to_bytes((vid / 32))))
 304: ####         -- let uint64_random_byte = $bytes1_to_uint64($bytes32_to_bytes1_list(bytes32_h)[(vid \ 32)])
 305: ####         -- let balance_effective = state.VALIDATORS[vid_candidate].EFFECTIVE_BALANCE
 306: ####/####     -- if (balance_effective <= ($UINT64_MAX / $MAX_RANDOM_BYTE))
 307: ####/####     -- if ((balance_effective * $MAX_RANDOM_BYTE) < ($MAX_EFFECTIVE_BALANCE * uint64_random_byte))
      ####          SUCCESS

def $get_next_sync_committee_indices:
      clause 0:
 308:    1         -- let epoch_next = ($get_current_epoch(state) + 1)
 309:    1         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch_next)
 310:    1         -- let count_active = |vid_active*{vid_active <- vid_active*}|
 311:    1         -- let seed = $get_seed(state, epoch_next, $DOMAIN_SYNC_COMMITTEE)
 312:    1         -- let vid_out*{vid_out <- vid_out*} = $next_sync_committee_indices_loop(state, vid_active*{vid_active <- vid_active*}, seed, count_active, 0, [])
         1          SUCCESS

def $get_next_sync_committee:
      clause 0:
 313:    1         -- let vid*{vid <- vid*} = $get_next_sync_committee_indices(state)
 314: ####         -- (let blsPubkey = state.VALIDATORS[vid].PUBKEY)*{blsPubkey <- blsPubkey*, vid <- vid*}
 315:    1         -- let blsPubkey_aggregate = $eth_aggregate_pubkeys(blsPubkey*{blsPubkey <- blsPubkey*})
         1          SUCCESS

def $get_indexed_attestation:
      clause 0:
 316: 2788         -- let vid_attesting*{vid_attesting <- vid_attesting*} = $get_attesting_indices(state, attestation)
      2788          SUCCESS

def $get_total_balance:
      clause 0:
 317: ####         -- (let balance = state.VALIDATORS[vid].EFFECTIVE_BALANCE)*{balance <- balance*, vid <- vid*}
      ####          SUCCESS

def $get_total_active_balance:
      clause 0:
 318: 94831         -- let epoch_cur = $get_current_epoch(state)
 319: 94831         -- let vid_active*{vid_active <- vid_active*} = $get_active_validator_indices(state, epoch_cur)
 320: 94831         -- let vid_set*{vid_set <- vid_set*} = $to_set_<validatorIndex>(vid_active*{vid_active <- vid_active*})
 321: 94831         -- let gwei_total = $get_total_balance(state, vid_set*{vid_set <- vid_set*})
      94831          SUCCESS

def $get_base_reward_per_increment:
      clause 0:

def $get_base_reward:
      clause 0:
 322: 93252/####     -- if (vid < |state.VALIDATORS|)
 323: 93252         -- let uint64_increments = (state.VALIDATORS[vid].EFFECTIVE_BALANCE / $EFFECTIVE_BALANCE_INCREMENT)
 324: 93252         -- let reward = (uint64_increments * $get_base_reward_per_increment(state))
      93252          SUCCESS

relation ApplyEffectiveBalance:
      rule main:
 325: 42480         -- let uint64_hyst_inc = ($EFFECTIVE_BALANCE_INCREMENT / $HYSTERESIS_QUOTIENT)
 326: 42480         -- let uint64_down_th = (uint64_hyst_inc * $HYSTERESIS_DOWNWARD_MULTIPLIER)
 327: 42480         -- let uint64_up_th = (uint64_hyst_inc * $HYSTERESIS_UPWARD_MULTIPLIER)
 328: 42480/####     -- if (balance <= ($UINT64_MAX - uint64_down_th))
 329: 42480/####     -- if (validator.EFFECTIVE_BALANCE <= ($UINT64_MAX - uint64_up_th))
 330: 16898/25582     -- if (((balance + uint64_down_th) < validator.EFFECTIVE_BALANCE) \/ ((validator.EFFECTIVE_BALANCE + uint64_up_th) < balance))
      16898          SUCCESS
      rule skip:
 331: 25582         -- otherwise
      25582          SUCCESS

def $inact_penalty_one:
      clause 0:
 332: 34464/6480     -- if vid <- vid_eligible*{vid_eligible <- vid_eligible*}
 333: 21298/13166     -- if ~vid <- vid_matching*{vid_matching <- vid_matching*}
 334: 21298/####     -- if (vid < |state.VALIDATORS|)
 335: 21298         -- let balance_effective = state.VALIDATORS[vid].EFFECTIVE_BALANCE
 336: 21298/####     -- if (vid < |state.INACTIVITY_SCORES|)
 337: 21298         -- let uint64_score = state.INACTIVITY_SCORES[vid]
 338: 21298         -- let penalty_numerator = (balance_effective * uint64_score)
 339: 21298/####     -- if (penalty_numerator <= $UINT64_MAX)
 340: 21298         -- let penalty_denominator = ($INACTIVITY_SCORE_BIAS * $INACTIVITY_PENALTY_QUOTIENT_BELLATRIX)
 341: 21298         -- let penalty = (penalty_numerator / penalty_denominator)
      21298          SUCCESS
      clause 1:
 342: 19646         -- otherwise
      19646          SUCCESS

def $get_inactivity_penalty_deltas:
      clause 0:
 343:   90         -- let reward*{reward <- reward*} = $repeat_<gwei>(0, |state.VALIDATORS|)
 344:   90         -- let vid_eligible*{vid_eligible <- vid_eligible*} = $get_eligible_validator_indices(state)
 345:   90         -- let epoch_prev = $get_previous_epoch(state)
 346:   90         -- let vid_matching*{vid_matching <- vid_matching*} = $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, epoch_prev)
 347:   90         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 348: ####         -- (let penalty = $inact_penalty_one(vid, state, vid_matching*{vid_matching <- vid_matching*}, vid_eligible*{vid_eligible <- vid_eligible*}))*{penalty <- penalty*, vid <- vid*}
      ####          SUCCESS

def $flag_reward_one_eligible:
      clause 0:
 349: 103392/19440     -- if vid <- vid_eligible*{vid_eligible <- vid_eligible*}
 350: 32223/71169     -- if vid <- vid_unslashed*{vid_unslashed <- vid_unslashed*}
 351: 30374/1849     -- if ~$is_in_inactivity_leak(state)
 352: 30374         -- let reward_base = $get_base_reward(state, vid)
 353: 30374         -- let reward_numerator = ((reward_base * uint64_weight) * uint64_up_incs)
 354: 30374         -- let reward = (reward_numerator / (uint64_active_incs * $WEIGHT_DENOMINATOR))
      30374          SUCCESS
      clause 1:
 355: 92458         -- otherwise
      92458          SUCCESS

def $flag_penalty_one_eligible:
      clause 0:
 356: 103392/19440     -- if vid <- vid_eligible*{vid_eligible <- vid_eligible*}
 357: 71169/32223     -- if ~vid <- vid_unslashed*{vid_unslashed <- vid_unslashed*}
 358: 45255/25914     -- if (flag_index =/= $TIMELY_HEAD_FLAG_INDEX)
 359: 45255         -- let reward_base = $get_base_reward(state, vid)
 360: 45255         -- let penalty = ((reward_base * uint64_weight) / $WEIGHT_DENOMINATOR)
      45255          SUCCESS
      clause 1:
 361: 77577         -- otherwise
      77577          SUCCESS

def $get_flag_index_deltas:
      clause 0:
 362:  270         -- let vid_eligible*{vid_eligible <- vid_eligible*} = $to_set_<validatorIndex>($get_eligible_validator_indices(state))
 363:  270         -- let epoch_prev = $get_previous_epoch(state)
 364:  270         -- let vid_unslashed*{vid_unslashed <- vid_unslashed*} = $get_unslashed_participating_indices(state, flag_index, epoch_prev)
 365:  270         -- let uint64_weight = $PARTICIPATION_FLAG_WEIGHTS[flag_index]
 366:  270         -- let balance_unslashed_participating = $get_total_balance(state, vid_unslashed*{vid_unslashed <- vid_unslashed*})
 367:  270         -- let uint64_unslashed_increments = (balance_unslashed_participating / $EFFECTIVE_BALANCE_INCREMENT)
 368:  270         -- let uint64_active_increments = ($get_total_active_balance(state) / $EFFECTIVE_BALANCE_INCREMENT)
 369:  270         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 370: ####         -- (let reward = $flag_reward_one_eligible(vid, state, vid_unslashed*{vid_unslashed <- vid_unslashed*}, vid_eligible*{vid_eligible <- vid_eligible*}, uint64_weight, uint64_unslashed_increments, uint64_active_increments))*{reward <- reward*, vid <- vid*}
 371: ####         -- (let penalty = $flag_penalty_one_eligible(vid, state, vid_unslashed*{vid_unslashed <- vid_unslashed*}, vid_eligible*{vid_eligible <- vid_eligible*}, uint64_weight, flag_index))*{penalty <- penalty*, vid <- vid*}
      ####          SUCCESS

relation ApplyRewardAndPenalty:
      rule :
 372: 163776/####     -- rel IncreaseBalance: state .BALANCES[ vid ]+ reward ~> state_r
 373: 163776/####     -- rel DecreaseBalance: state_r .BALANCES[ vid ]- penalty ~> state_out
      163776          SUCCESS

relation ApplyRewardsAndPenalties:
      rule nil:
 374:  360/163776     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
 375:  360/####     -- if gwei*{gwei <- gwei*} matches []
 376:  360/####     -- if gwei'*{gwei' <- gwei'*} matches []
       360          SUCCESS
      rule cons:
 377: 163776/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 378: 163776         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 379: 163776/####     -- if gwei*{gwei <- gwei*} matches _ :: _
 380: 163776         -- let reward_h :: reward_t*{reward_t <- reward_t*} = gwei*{gwei <- gwei*}
 381: 163776/####     -- if gwei'*{gwei' <- gwei'*} matches _ :: _
 382: 163776         -- let penalty_h :: penalty_t*{penalty_t <- penalty_t*} = gwei'*{gwei' <- gwei'*}
 383: 163776/####     -- rel ApplyRewardAndPenalty: state .BALANCES[ vid_h ]+ reward_h - penalty_h ~> state_h
 384: 163776/####     -- rel ApplyRewardsAndPenalties: state_h .BALANCES[ vid_t*{vid_t <- vid_t*} ]+ reward_t*{reward_t <- reward_t*} - penalty_t*{penalty_t <- penalty_t*} ~> state_out
      163776          SUCCESS

relation ApplyRewardsAndPenalties_over_deltas:
      rule nil:
 385:   90/ 360     -- if gwei*{gwei <- gwei*}*{gwei* <- gwei**} matches []
 386:   90/####     -- if gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**} matches []
        90          SUCCESS
      rule cons:
 387:  360/####     -- if gwei*{gwei <- gwei*}*{gwei* <- gwei**} matches _ :: _
 388:  360         -- let reward*{reward <- reward*} :: reward_t*{reward_t <- reward_t*}*{reward_t* <- reward_t**} = gwei*{gwei <- gwei*}*{gwei* <- gwei**}
 389:  360/####     -- if gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**} matches _ :: _
 390:  360         -- let penalty*{penalty <- penalty*} :: penalty_t*{penalty_t <- penalty_t*}*{penalty_t* <- penalty_t**} = gwei'*{gwei' <- gwei'*}*{gwei'* <- gwei'**}
 391:  360/####     -- rel ApplyRewardsAndPenalties: state .BALANCES[ vid*{vid <- vid*} ]+ reward*{reward <- reward*} - penalty*{penalty <- penalty*} ~> state_t
 392:  360/####     -- rel ApplyRewardsAndPenalties_over_deltas: state_t .BALANCES[ vid*{vid <- vid*} ]+ reward_t*{reward_t <- reward_t*}*{reward_t* <- reward_t**} - penalty_t*{penalty_t <- penalty_t*}*{penalty_t* <- penalty_t**} ~>* state_out
       360          SUCCESS

relation ApplyInactivity:
      rule unslashed_participating_no_leak:
 393: 13166/21298     -- if vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 394: 11946/1220     -- if ~$is_in_inactivity_leak(state)
 395: 11946/####     -- if (vid < |state.INACTIVITY_SCORES|)
      11946          SUCCESS
      rule unslashed_participating_leak:
 396: 1220/21298     -- if vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 397: 1220/####     -- if $is_in_inactivity_leak(state)
 398: 1220/####     -- if (vid < |state.INACTIVITY_SCORES|)
      1220          SUCCESS
      rule inactive_no_leak:
 399: 21298/####     -- if ~vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 400: 15322/5976     -- if ~$is_in_inactivity_leak(state)
 401: 15322         -- let n_leak_penalty = ($INACTIVITY_SCORE_BIAS - $INACTIVITY_SCORE_RECOVERY_RATE)
 402: 15322/####     -- if (vid < |state.INACTIVITY_SCORES|)
      15322          SUCCESS
      rule inactive_leak:
 403: 5976/####     -- if ~vid <- $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 404: 5976/####     -- if $is_in_inactivity_leak(state)
 405: 5976/####     -- if (vid < |state.INACTIVITY_SCORES|)
      5976          SUCCESS

relation ApplyInactivity_over_vids:
      rule nil:
 406:   90/34464     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        90          SUCCESS
      rule cons:
 407: 34464/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 408: 34464         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 409: 34464/####     -- rel ApplyInactivity: state .INACTIVITY_SCORES[ vid_h ]~> state_1
 410: 34464/####     -- rel ApplyInactivity_over_vids: state_1 .INACTIVITY_SCORES[ vid_t*{vid_t <- vid_t*} ]~>* state_updated
      34464          SUCCESS

def $is_due_slashing_en:
      clause 0:
 411: 7704/77256     -- if (validator.SLASHED = true)
 412: ####/7704     -- if ((epoch + ($EPOCHS_PER_SLASHINGS_VECTOR / 2)) = validator.WITHDRAWABLE_EPOCH)
      ####          SUCCESS
      clause 1:
 413: 84960         -- otherwise
      84960          SUCCESS

relation DecreaseBalances:
      rule nil:
 414:   96/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
 415:   96/####     -- if gwei*{gwei <- gwei*} matches []
        96          SUCCESS
      rule cons:
 416: ####/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 417: ####         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 418: ####/####     -- if gwei*{gwei <- gwei*} matches _ :: _
 419: ####         -- let penalty_h :: penalty_t*{penalty_t <- penalty_t*} = gwei*{gwei <- gwei*}
 420: ####/####     -- rel DecreaseBalance: state .BALANCES[ vid_h ]- penalty_h ~> state_h
 421: ####/####     -- rel DecreaseBalances: state_h .BALANCES[ vid_t*{vid_t <- vid_t*} ]- penalty_t*{penalty_t <- penalty_t*} ~> state_t
      ####          SUCCESS

relation WeighJustificationAndFinalization:
      rule justify_neither_finalize_prev:
 422:   87/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 423:   45/  42     -- if ~((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 424:   40/   5     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 425:   40         -- let bit*{bit <- bit*} = [false, state.JUSTIFICATION_BITS[0], state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 426:   40         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 427:   40         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 428:   40         -- let epoch_cur = $get_current_epoch(state)
 429: ####/  40     -- if ((((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) \/ ((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
      ####          SUCCESS
      rule justify_neither_finalize_neither:
 430:   87/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 431:   45/  42     -- if ~((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 432:   40/   5     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 433:   40         -- let bit*{bit <- bit*} = [false, state.JUSTIFICATION_BITS[0], state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 434:   40         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 435:   40         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 436:   40         -- let epoch_cur = $get_current_epoch(state)
 437:   40/####     -- if (~(((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) /\ ~((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
        40          SUCCESS
      rule justify_prev_finalize_prev:
 438:   47/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 439:   42/   5     -- if ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 440:   23/  19     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 441:   23         -- let bit*{bit <- bit*} = [false, true, state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 442:   23         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 443:   23         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 444:   23         -- let epoch_prev = $get_previous_epoch(state)
 445:   23         -- let epoch_cur = $get_current_epoch(state)
 446:   17/   6     -- if ((((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) \/ ((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
        17          SUCCESS
      rule justify_prev_finalize_neither:
 447:   30/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 448:   25/   5     -- if ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))
 449:    6/  19     -- if ~((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 450:    6         -- let bit*{bit <- bit*} = [false, true, state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 451:    6         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 452:    6         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 453:    6         -- let epoch_prev = $get_previous_epoch(state)
 454:    6         -- let epoch_cur = $get_current_epoch(state)
 455:    6/####     -- if (~(((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) /\ ~((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
         6          SUCCESS
      rule justify_cur_finalize_cur:
 456:   24/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 457:   24/####     -- if ((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 458:   24         -- let bit*{bit <- bit*} = [true, (state.JUSTIFICATION_BITS[0] \/ ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))), state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 459:   24         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 460:   24         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 461:   24         -- let epoch_cur = $get_current_epoch(state)
 462:   19/   5     -- if ((((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 2))) \/ ((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 1))))
        19          SUCCESS
      rule justify_cur_finalize_prev:
 463:    5/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 464:    5/####     -- if ((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 465:    5         -- let bit*{bit <- bit*} = [true, (state.JUSTIFICATION_BITS[0] \/ ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))), state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 466:    5         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 467:    5         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 468:    5         -- let epoch_cur = $get_current_epoch(state)
 469: ####/   5     -- if ((~(((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 2))) /\ ~((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 1)))) /\ ((((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3))) \/ ((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2)))))
      ####          SUCCESS
      rule justify_cur_finalize_neither:
 470:    5/####     -- if (balance_total_active <= ($UINT64_MAX / 2))
 471:    5/####     -- if ((balance_current_epoch_target * 3) >= (balance_total_active * 2))
 472:    5         -- let bit*{bit <- bit*} = [true, (state.JUSTIFICATION_BITS[0] \/ ((balance_prev_epoch_target * 3) >= (balance_total_active * 2))), state.JUSTIFICATION_BITS[1], state.JUSTIFICATION_BITS[2]]
 473:    5         -- let checkpoint_prev = state.PREVIOUS_JUSTIFIED_CHECKPOINT
 474:    5         -- let checkpoint_cur = state.CURRENT_JUSTIFIED_CHECKPOINT
 475:    5         -- let epoch_cur = $get_current_epoch(state)
 476:    5/####     -- if (((~(((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 2))) /\ ~((bit*{bit <- bit*}[0] /\ bit*{bit <- bit*}[1]) /\ (epoch_cur = (checkpoint_cur.EPOCH + 1)))) /\ ~(((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ bit*{bit <- bit*}[3]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 3)))) /\ ~((bit*{bit <- bit*}[1] /\ bit*{bit <- bit*}[2]) /\ (epoch_cur = (checkpoint_prev.EPOCH + 2))))
         5          SUCCESS

relation ProcessEligibilityAndEjection:
      rule eligible_and_eject:
 477: 42480         -- let validator = state.VALIDATORS[vid]
 478: 42480         -- let epoch_cur = $get_current_epoch(state)
 479:  208/42272     -- if $is_eligible_for_activation_queue(validator)
 480: ####/ 208     -- if ($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
 481: ####/####     -- rel InitiateValidatorExit: state[VALIDATORS[vid].ACTIVATION_ELIGIBILITY_EPOCH = (epoch_cur + 1)] .VALIDATORS[ vid ]~> state_updated
      ####          SUCCESS
      rule eligible:
 482: 42480         -- let validator = state.VALIDATORS[vid]
 483: 42480         -- let epoch_cur = $get_current_epoch(state)
 484:  208/42272     -- if $is_eligible_for_activation_queue(validator)
 485:  208/####     -- if ~($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
       208          SUCCESS
      rule eject:
 486: 42272         -- let validator = state.VALIDATORS[vid]
 487: 42272         -- let epoch_cur = $get_current_epoch(state)
 488: 42272/####     -- if ~$is_eligible_for_activation_queue(validator)
 489:  754/41518     -- if ($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
 490:  754/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid ]~> state_updated
       754          SUCCESS
      rule skip:
 491: 41518         -- let validator = state.VALIDATORS[vid]
 492: 41518         -- let epoch_cur = $get_current_epoch(state)
 493: 41518/####     -- if ~$is_eligible_for_activation_queue(validator)
 494: 41518/####     -- if ~($is_active_validator(validator, epoch_cur) /\ (validator.EFFECTIVE_BALANCE <= $EJECTION_BALANCE))
      41518          SUCCESS

relation ProcessEligibilityAndEjection_over_vids:
      rule nil:
 495:   96/42480     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        96          SUCCESS
      rule cons:
 496: 42480/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 497: 42480         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 498: 42480/####     -- rel ProcessEligibilityAndEjection: state .VALIDATORS[ vid_h ]~> state_h
 499: 42480/####     -- rel ProcessEligibilityAndEjection_over_vids: state_h .VALIDATORS[ vid_t*{vid_t <- vid_t*} ]~> state_out
      42480          SUCCESS

def $is_eligible_activation_en:
      clause 0:

relation ApplyActivation:
      rule :

relation ApplyActivations:
      rule nil:
 500:   96/ 276     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
        96          SUCCESS
      rule cons:
 501:  276/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 502:  276         -- let vid_h :: vid_t*{vid_t <- vid_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 503:  276/####     -- rel ApplyActivation: state .VALIDATORS[ vid_h ].ACTIVATION_EPOCH~> state_h
 504:  276/####     -- rel ApplyActivations: state_h .VALIDATORS[ vid_t*{vid_t <- vid_t*} ].ACTIVATION_EPOCH~>* state_out
       276          SUCCESS

relation ProcessSlot:
      rule header_root_empty:
 505: 3438         -- let i = (state.SLOT \ $SLOTS_PER_HISTORICAL_ROOT)
 506:  573/2865     -- if (state.LATEST_BLOCK_HEADER.STATE_ROOT = $ZERO_ROOT)
 507:  573         -- let root_state_prev = $hash_tree_root_beaconState(state)
 508:  573         -- let beaconBlockHeader = state.LATEST_BLOCK_HEADER[STATE_ROOT = $hash_tree_root_beaconState(state)]
 509:  573         -- let root_block_prev = $hash_tree_root_beaconBlockHeader(beaconBlockHeader)
       573          SUCCESS
      rule header_root_empty:
 510: 2865         -- let i = (state.SLOT \ $SLOTS_PER_HISTORICAL_ROOT)
 511: 2865/####     -- if (state.LATEST_BLOCK_HEADER.STATE_ROOT =/= $ZERO_ROOT)
 512: 2865         -- let root_state_prev = $hash_tree_root_beaconState(state)
 513: 2865         -- let root_block_prev = $hash_tree_root_beaconBlockHeader(state.LATEST_BLOCK_HEADER)
      2865          SUCCESS

relation ProcessJustificationAndFinalization:
      rule skip:
 514:   96         -- let epoch_cur = $get_current_epoch(state)
 515:    9/  87     -- if (epoch_cur <= ($GENESIS_EPOCH + 1))
         9          SUCCESS
      rule main:
 516:   87         -- let vid_prev*{vid_prev <- vid_prev*} = $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_previous_epoch(state))
 517:   87         -- let vid_cur*{vid_cur <- vid_cur*} = $get_unslashed_participating_indices(state, $TIMELY_TARGET_FLAG_INDEX, $get_current_epoch(state))
 518:   87         -- let balance_total_active = $get_total_active_balance(state)
 519:   87         -- let balance_prev_target = $get_total_balance(state, vid_prev*{vid_prev <- vid_prev*})
 520:   87         -- let balance_cur_target = $get_total_balance(state, vid_cur*{vid_cur <- vid_cur*})
 521:   87/####     -- rel WeighJustificationAndFinalization: state TOTAL balance_total_active PREV balance_prev_target CUR balance_cur_target ~> state_updated
        87          SUCCESS

relation ProcessInactivityUpdates:
      rule skip:
 522:    6/  90     -- if ($get_current_epoch(state) = $GENESIS_EPOCH)
         6          SUCCESS
      rule main:
 523:   90         -- otherwise
 524:   90         -- let vid_eligible*{vid_eligible <- vid_eligible*} = $get_eligible_validator_indices(state)
 525:   90/####     -- rel ApplyInactivity_over_vids: state .INACTIVITY_SCORES[ vid_eligible*{vid_eligible <- vid_eligible*} ]~>* state_updated
        90          SUCCESS

relation ProcessRewardsAndPenalties:
      rule skip:
 526:    6/  90     -- if ($get_current_epoch(state) = $GENESIS_EPOCH)
         6          SUCCESS
      rule main:
 527:   90         -- otherwise
 528:   90         -- let vid_flag*{vid_flag <- vid_flag*} = $range(|$PARTICIPATION_FLAG_WEIGHTS|)
 529: ####         -- (let (reward_flag*{reward_flag <- reward_flag*}, penalty_flag*{penalty_flag <- penalty_flag*}) = $get_flag_index_deltas(state, vid_flag))*{penalty_flag* <- penalty_flag**, reward_flag* <- reward_flag**, vid_flag <- vid_flag*}
 530:   90         -- let (reward_inactivity*{reward_inactivity <- reward_inactivity*}, penalty_inactivity*{penalty_inactivity <- penalty_inactivity*}) = $get_inactivity_penalty_deltas(state)
 531:   90         -- let reward*{reward <- reward*}*{reward* <- reward**} = reward_flag*{reward_flag <- reward_flag*}*{reward_flag* <- reward_flag**} ++ [reward_inactivity*{reward_inactivity <- reward_inactivity*}]
 532:   90         -- let penalty*{penalty <- penalty*}*{penalty* <- penalty**} = penalty_flag*{penalty_flag <- penalty_flag*}*{penalty_flag* <- penalty_flag**} ++ [penalty_inactivity*{penalty_inactivity <- penalty_inactivity*}]
 533:   90         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 534:   90/####     -- rel ApplyRewardsAndPenalties_over_deltas: state .BALANCES[ vid*{vid <- vid*} ]+ reward*{reward <- reward*}*{reward* <- reward**} - penalty*{penalty <- penalty*}*{penalty* <- penalty**} ~>* state_out
        90          SUCCESS

relation ProcessRegistryUpdates:
      rule :
 535:   96         -- let vid*{vid <- vid*} = $range(|state.VALIDATORS|)
 536:   96/####     -- rel ProcessEligibilityAndEjection_over_vids: state .VALIDATORS[ vid*{vid <- vid*} ]~> state_1
 537:   96         -- let (vid_1, validator)*{validator <- validator*, vid_1 <- vid_1*} = $enumerate_<validator>(state_1.VALIDATORS)
 538:   96         -- let (vid_elg, validator_elg)*{validator_elg <- validator_elg*, vid_elg <- vid_elg*} = $filter_list_2_<(nat, validator), beaconState>((vid_1, validator)*{validator <- validator*, vid_1 <- vid_1*}, state_1, $is_eligible_activation_en)
 539: ####         -- (let epoch_elg = validator_elg.ACTIVATION_ELIGIBILITY_EPOCH)*{epoch_elg <- epoch_elg*, validator_elg <- validator_elg*}
 540:   96         -- let (_epoch, vid_activation_queue)*{_epoch <- _epoch*, vid_activation_queue <- vid_activation_queue*} = $sort_<(epoch, validatorIndex)>((epoch_elg, vid_elg)*{epoch_elg <- epoch_elg*, vid_elg <- vid_elg*})
 541:   96         -- let vid_dequeued*{vid_dequeued <- vid_dequeued*} = vid_activation_queue*{vid_activation_queue <- vid_activation_queue*}[0 : $get_validator_churn_limit(state_1)]
 542:   96/####     -- rel ApplyActivations: state_1 .VALIDATORS[ vid_dequeued*{vid_dequeued <- vid_dequeued*} ].ACTIVATION_EPOCH~>* state_out
        96          SUCCESS

relation ProcessSlashings:
      rule :
 543:   96         -- let epoch_cur = $get_current_epoch(state)
 544:   96         -- let balance_total = $get_total_active_balance(state)
 545:   96         -- let balance_adjusted_total_slashing = $min_uint64(($sum_<gwei>(state.SLASHINGS) * $PROPORTIONAL_SLASHING_MULTIPLIER_BELLATRIX), balance_total)
 546:   96         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 547:   96         -- let (vid_due, validator_due)*{validator_due <- validator_due*, vid_due <- vid_due*} = $filter_list_2_<(validatorIndex, validator), epoch>((vid, validator)*{validator <- validator*, vid <- vid*}, epoch_cur, $is_due_slashing_en)
 548:   96         -- let gwei_increment = $EFFECTIVE_BALANCE_INCREMENT
 549: ####         -- (let penalty_numerator = ((validator_due.EFFECTIVE_BALANCE / gwei_increment) * balance_adjusted_total_slashing))*{penalty_numerator <- penalty_numerator*, validator_due <- validator_due*}
 550: ####         -- (let penalty = ((penalty_numerator / balance_total) * gwei_increment))*{penalty <- penalty*, penalty_numerator <- penalty_numerator*}
 551:   96/####     -- rel DecreaseBalances: state .BALANCES[ vid_due*{vid_due <- vid_due*} ]- penalty*{penalty <- penalty*} ~> state_out
        96          SUCCESS

relation ProcessEth1DataReset:
      rule skip:
 552:   96         -- let epoch_next = ($get_current_epoch(state) + 1)
 553:   95/   1     -- if ((epoch_next \ $EPOCHS_PER_ETH1_VOTING_PERIOD) =/= 0)
        95          SUCCESS
      rule main:
 554:    1         -- otherwise
         1          SUCCESS

relation ProcessEffectiveBalanceUpdates:
      rule :
 555:   96         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 556: ####         -- (let balance = state.BALANCES[vid])*{balance <- balance*, vid <- vid*}
 557: ####/####     -- (rel ApplyEffectiveBalance: validator .EFFECTIVE_BALANCE balance ~> validator_updated)*{balance <- balance*, validator <- validator*, validator_updated <- validator_updated*}
      ####          SUCCESS

relation ProcessSlashingsReset:
      rule :
 558:   96         -- let epoch_next = ($get_current_epoch(state) + 1)
 559:   96         -- let i = (epoch_next \ $EPOCHS_PER_SLASHINGS_VECTOR)
 560:   96/####     -- if (i < |state.SLASHINGS|)
        96          SUCCESS

relation ProcessRandaoMixesReset:
      rule :
 561:   96         -- let epoch_cur = $get_current_epoch(state)
 562:   96         -- let epoch_next = (epoch_cur + 1)
 563:   96         -- let i = (epoch_next \ $EPOCHS_PER_HISTORICAL_VECTOR)
 564:   96/####     -- if (i < |state.RANDAO_MIXES|)
        96          SUCCESS

relation ProcessHistoricalSummariesUpdate:
      rule skip:
 565:   96         -- let epoch_next = ($get_current_epoch(state) + 1)
 566:   95/   1     -- if ((epoch_next \ ($SLOTS_PER_HISTORICAL_ROOT / $SLOTS_PER_EPOCH)) =/= 0)
        95          SUCCESS
      rule main:
 567:    1         -- otherwise
 568:    1         -- let root_block_summary = $hash_tree_root_roots(state.BLOCK_ROOTS)
 569:    1         -- let root_state_summary = $hash_tree_root_roots(state.STATE_ROOTS)
 570:    1         -- let historicalSummary = {BLOCK_SUMMARY_ROOT root_block_summary, STATE_SUMMARY_ROOT root_state_summary}
         1          SUCCESS

relation ProcessParticipationFlagUpdates:
      rule :
 571:   96         -- let participationFlags_zero*{participationFlags_zero <- participationFlags_zero*} = $repeat_<participationFlags>(0, |state.VALIDATORS|)
        96          SUCCESS

relation ProcessSyncCommitteeUpdates:
      rule skip:
 572:   96         -- let epoch_next = ($get_current_epoch(state) + 1)
 573:   95/   1     -- if ((epoch_next \ $EPOCHS_PER_SYNC_COMMITTEE_PERIOD) =/= 0)
        95          SUCCESS
      rule main:
 574:    1         -- otherwise
         1          SUCCESS

relation ProcessEpoch:
      rule :
 575:   96/####     -- rel ProcessJustificationAndFinalization: state ~> state_0
 576:   96/####     -- rel ProcessInactivityUpdates: state_0 ~> state_1
 577:   96/####     -- rel ProcessRewardsAndPenalties: state_1 ~> state_2
 578:   96/####     -- rel ProcessRegistryUpdates: state_2 ~> state_3
 579:   96/####     -- rel ProcessSlashings: state_3 ~> state_4
 580:   96/####     -- rel ProcessEth1DataReset: state_4 ~> state_5
 581:   96/####     -- rel ProcessEffectiveBalanceUpdates: state_5 ~> state_6
 582:   96/####     -- rel ProcessSlashingsReset: state_6 ~> state_7
 583:   96/####     -- rel ProcessRandaoMixesReset: state_7 ~> state_8
 584:   96/####     -- rel ProcessHistoricalSummariesUpdate: state_8 ~> state_9
 585:   96/####     -- rel ProcessParticipationFlagUpdates: state_9 ~> state_10
 586:   96/####     -- rel ProcessSyncCommitteeUpdates: state_10 ~> state_11
        96          SUCCESS

relation ProcessSlots_loop:
      rule base:
 587:  578/3342     -- if (state.SLOT >= slot_target)
       578          SUCCESS
      rule step-no-epoch:
 588: 3342/####     -- if (state.SLOT < slot_target)
 589: 3342/####     -- rel ProcessSlot: state ~> state_after_slot
 590: 3342         -- let slot_cur = state_after_slot.SLOT
 591: 3342         -- let slot_next = (slot_cur + 1)
 592: 3246/  96     -- if ((slot_next \ $SLOTS_PER_EPOCH) =/= 0)
 593: 3246         -- let state_next = state_after_slot[SLOT = slot_next]
 594: 3246/####     -- rel ProcessSlots_loop: state_next slot_target ~> state_out
      3246          SUCCESS
      rule step-epoch:
 595:   96/####     -- if (state.SLOT < slot_target)
 596:   96/####     -- rel ProcessSlot: state ~> state_after_slot
 597:   96         -- let slot_cur = state_after_slot.SLOT
 598:   96         -- let slot_next = (slot_cur + 1)
 599:   96/####     -- if ((slot_next \ $SLOTS_PER_EPOCH) = 0)
 600:   96/####     -- rel ProcessEpoch: state_after_slot ~> state_after_epoch
 601:   96         -- let state_next = state_after_epoch[SLOT = slot_next]
 602:   96/####     -- rel ProcessSlots_loop: state_next slot_target ~> state_out
        96          SUCCESS

relation ProcessSlots:
      rule :
 603:  578/   3     -- if (state.SLOT < slot_target)
 604:  578/####     -- rel ProcessSlots_loop: state slot_target ~> state_out
       578          SUCCESS

def $expected_withdrawals_loop:
      clause 0:
 605:    6/173706     -- if (|withdrawal_acc*{withdrawal_acc <- withdrawal_acc*}| = $MAX_WITHDRAWALS_PER_PAYLOAD)
         6          SUCCESS
      clause 1:
 606:  608/173098     -- if (i >= uint64_bound)
       608          SUCCESS
      clause 2:
 607: 173098/####     -- if (vid < |state.VALIDATORS|)
 608: 173098         -- let validator = state.VALIDATORS[vid]
 609: 173098         -- let balance = state.BALANCES[vid]
 610:   79/173019     -- if ($is_fully_withdrawable_validator(validator, balance, epoch) = true)
 611:   79         -- let withdrawal = {INDEX withdrawalIndex, VALIDATOR_INDEX vid, ADDRESS $extract_execution_address(validator.WITHDRAWAL_CREDENTIALS), AMOUNT balance}
        79          SUCCESS
      clause 3:
 612: 173019/####     -- if (vid < |state.VALIDATORS|)
 613: 173019         -- let validator = state.VALIDATORS[vid]
 614: 173019         -- let balance = state.BALANCES[vid]
 615: 173019/####     -- if ($is_fully_withdrawable_validator(validator, balance, epoch) = false)
 616:  227/172792     -- if ($is_partially_withdrawable_validator(validator, balance) = true)
 617:  227         -- let withdrawal = {INDEX withdrawalIndex, VALIDATOR_INDEX vid, ADDRESS $extract_execution_address(validator.WITHDRAWAL_CREDENTIALS), AMOUNT (balance - $MAX_EFFECTIVE_BALANCE)}
       227          SUCCESS
      clause 4:
 618: 172792/####     -- if (vid < |state.VALIDATORS|)
 619: 172792         -- let validator = state.VALIDATORS[vid]
 620: 172792         -- let balance = state.BALANCES[vid]
 621: 172792/####     -- if ~$is_fully_withdrawable_validator(validator, balance, epoch)
 622: 172792/####     -- if ~$is_partially_withdrawable_validator(validator, balance)
      172792          SUCCESS

def $get_expected_withdrawals:
      clause 0:
 623:  614         -- let epoch_cur = $get_current_epoch(state)
 624:  614         -- let withdrawalIndex_start = state.NEXT_WITHDRAWAL_INDEX
 625:  614         -- let validatorIndex_start = state.NEXT_WITHDRAWAL_VALIDATOR_INDEX
 626:  614         -- let uint64_bound = $min_uint64(|state.VALIDATORS|, $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP)
       614          SUCCESS

relation ApplyWithdrawal:
      rule main:
 627:  306/####     -- rel DecreaseBalance: state .BALANCES[ withdrawal.VALIDATOR_INDEX ]- withdrawal.AMOUNT ~> state_out
       306          SUCCESS

relation ApplyWithdrawals:
      rule nil:
 628:  614/ 306     -- if withdrawal*{withdrawal <- withdrawal*} matches []
       614          SUCCESS
      rule cons:
 629:  306/####     -- if withdrawal*{withdrawal <- withdrawal*} matches _ :: _
 630:  306         -- let withdrawal_h :: withdrawal_t*{withdrawal_t <- withdrawal_t*} = withdrawal*{withdrawal <- withdrawal*}
 631:  306/####     -- rel ApplyWithdrawal: state .BALANCES{ withdrawal_h }~> state_1
 632:  306/####     -- rel ApplyWithdrawals: state_1 .BALANCES{ withdrawal_t*{withdrawal_t <- withdrawal_t*} }~>* state_out
       306          SUCCESS

relation AddFlag:
      rule main:
 633: 17623/22039     -- if ~$has_flag(participationFlags, vid_flag)
      17623          SUCCESS
      rule skip:
 634: 22039         -- otherwise
      22039          SUCCESS

relation AddFlags:
      rule nil:
 635: 31938/39662     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
      31938          SUCCESS
      rule cons:
 636: 39662/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 637: 39662         -- let vid_flag_h :: vid_flag_t*{vid_flag_t <- vid_flag_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 638: 39662/####     -- rel AddFlag: participationFlags [ vid_attesting ]++{ vid_flag_h }# state #SUM reward ~> participationFlags_h #SUM reward_h
 639: 39662/####     -- rel AddFlags: participationFlags_h [ vid_attesting ]++{ vid_flag_t*{vid_flag_t <- vid_flag_t*} }# state #SUM reward_h ~> participationFlags_t #SUM reward_t
      39662          SUCCESS

relation ProcessFlags:
      rule main:
 640: 31938/####     -- if (vid_attesting < |participationFlags*{participationFlags <- participationFlags*}|)
 641: 31938/####     -- rel AddFlags: participationFlags*{participationFlags <- participationFlags*}[vid_attesting] [ vid_attesting ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM 0 ~> participationFlags_updated #SUM reward_increment
      31938          SUCCESS

relation ProcessFlags_over_attesting:
      rule nil:
 642: 2788/31938     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
      2788          SUCCESS
      rule cons:
 643: 31938/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 644: 31938         -- let vid_attesting_h :: vid_attesting_t*{vid_attesting_t <- vid_attesting_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 645: 31938/####     -- rel ProcessFlags: participationFlags*{participationFlags <- participationFlags*} [ vid_attesting_h ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM reward ~> participationFlags_1*{participationFlags_1 <- participationFlags_1*} #SUM reward_1
 646: 31938/####     -- rel ProcessFlags_over_attesting: participationFlags_1*{participationFlags_1 <- participationFlags_1*} [ vid_attesting_t*{vid_attesting_t <- vid_attesting_t*} ]++{ vid_flag*{vid_flag <- vid_flag*} }# state #SUM reward_1 ~> participationFlags_2*{participationFlags_2 <- participationFlags_2*} #SUM reward_2
      31938          SUCCESS

def $bit_is_one_en_blsPubkey:
      clause 0:
 647: 560640/####     -- if (i < |boolean_bits*{boolean_bits <- boolean_bits*}|)
 648: 15872/544768     -- if (boolean_bits*{boolean_bits <- boolean_bits*}[i] = true)
      15872          SUCCESS
      clause 1:
 649: 544768         -- otherwise
      544768          SUCCESS

relation ApplyReward:
      rule participation:
 650: 15872/272384     -- if (bool = true)
 651: 15872/####     -- rel IncreaseBalance: state .BALANCES[ vid_participant ]+ reward_participant ~> state_participant
 652: 15872/####     -- rel IncreaseBalance: state_participant .BALANCES[ vid_proposer ]+ reward_proposer ~> state_out
      15872          SUCCESS
      rule no_participation:
 653: 272384/####     -- if (bool = false)
 654: 272384/####     -- rel DecreaseBalance: state .BALANCES[ vid_participant ]- reward_participant ~> state_out
      272384          SUCCESS

relation ApplyReward_over_participants:
      rule nil:
 655:  563/288256     -- if boolean*{boolean <- boolean*} matches []
 656:  563/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches []
       563          SUCCESS
      rule cons:
 657: 288256/####     -- if boolean*{boolean <- boolean*} matches _ :: _
 658: 288256         -- let boolean_h :: boolean_t*{boolean_t <- boolean_t*} = boolean*{boolean <- boolean*}
 659: 288256/####     -- if validatorIndex*{validatorIndex <- validatorIndex*} matches _ :: _
 660: 288256         -- let vid_participant_h :: vid_participant_t*{vid_participant_t <- vid_participant_t*} = validatorIndex*{validatorIndex <- validatorIndex*}
 661: 288256/####     -- rel ApplyReward: state PARTICIPANT boolean_h [ vid_participant_h ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_1
 662: 288256/####     -- rel ApplyReward_over_participants: state_1 PARTICIPANT boolean_t*{boolean_t <- boolean_t*} [ vid_participant_t*{vid_participant_t <- vid_participant_t*} ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_out
      288256          SUCCESS

relation ProcessProposerSlashing:
      rule :
 663:  290         -- let beaconBlockHeader_1 = proposerSlashing.SIGNED_HEADER_1.MESSAGE
 664:  290         -- let beaconBlockHeader_2 = proposerSlashing.SIGNED_HEADER_2.MESSAGE
 665:  290/####     -- if (beaconBlockHeader_1.SLOT = beaconBlockHeader_2.SLOT)
 666:  290/####     -- if (beaconBlockHeader_1.PROPOSER_INDEX = beaconBlockHeader_2.PROPOSER_INDEX)
 667:  290/####     -- if (beaconBlockHeader_1 =/= beaconBlockHeader_2)
 668:  290         -- let vid_proposer = beaconBlockHeader_1.PROPOSER_INDEX
 669:  290/####     -- if (vid_proposer < |state.VALIDATORS|)
 670:  290         -- let validator_proposer = state.VALIDATORS[vid_proposer]
 671:  288/   2     -- if $is_slashable_validator(validator_proposer, $get_current_epoch(state))
 672:  288         -- let signedBeaconBlockHeader_1 = proposerSlashing.SIGNED_HEADER_1
 673:  288         -- let epoch_h1 = $compute_epoch_at_slot(signedBeaconBlockHeader_1.MESSAGE.SLOT)
 674:  288         -- let domain_h1 = $get_domain(state, $DOMAIN_BEACON_PROPOSER, ?(epoch_h1))
 675:  288         -- let root_sign_h1 = $compute_signing_root_beaconBlockHeader(signedBeaconBlockHeader_1.MESSAGE, domain_h1)
 676:  288/####     -- if $bls_verify(validator_proposer.PUBKEY, root_sign_h1, signedBeaconBlockHeader_1.SIGNATURE)
 677:  288         -- let signedBeaconBlockHeader_2 = proposerSlashing.SIGNED_HEADER_2
 678:  288         -- let epoch_h2 = $compute_epoch_at_slot(signedBeaconBlockHeader_2.MESSAGE.SLOT)
 679:  288         -- let domain_h2 = $get_domain(state, $DOMAIN_BEACON_PROPOSER, ?(epoch_h2))
 680:  288         -- let root_sign_h2 = $compute_signing_root_beaconBlockHeader(signedBeaconBlockHeader_2.MESSAGE, domain_h2)
 681:  288/####     -- if $bls_verify(validator_proposer.PUBKEY, root_sign_h2, signedBeaconBlockHeader_2.SIGNATURE)
 682:  288/####     -- rel SlashValidator: state SLASH[ vid_proposer ]BY ?() ~> state_slashed
       288          SUCCESS

relation ProcessProposerSlashings:
      rule nil:
 683:  568/ 290     -- if proposerSlashing*{proposerSlashing <- proposerSlashing*} matches []
       568          SUCCESS
      rule cons:
 684:  290/####     -- if proposerSlashing*{proposerSlashing <- proposerSlashing*} matches _ :: _
 685:  290         -- let proposerSlashing_h :: proposerSlashing_t*{proposerSlashing_t <- proposerSlashing_t*} = proposerSlashing*{proposerSlashing <- proposerSlashing*}
 686:  288/   2     -- rel ProcessProposerSlashing: state proposerSlashing_h ~> state_mid
 687:  286/   2     -- rel ProcessProposerSlashings: state_mid proposerSlashing_t*{proposerSlashing_t <- proposerSlashing_t*} ~> state_out
       286          SUCCESS

relation ProcessAttesterSlashing:
      rule :
 688:   43         -- let indexedAttestation_1 = attesterSlashing.ATTESTATION_1
 689:   43         -- let indexedAttestation_2 = attesterSlashing.ATTESTATION_2
 690:   43/####     -- if ($is_slashable_attestation_data(indexedAttestation_1.DATA, indexedAttestation_2.DATA) = true)
 691:   43/####     -- if ($is_valid_indexed_attestation(state, indexedAttestation_1) = true)
 692:   43/####     -- if ($is_valid_indexed_attestation(state, indexedAttestation_2) = true)
 693:   43         -- let vid_intersection*{vid_intersection <- vid_intersection*} = $set_intersection_<validatorIndex>($to_set_<validatorIndex>(indexedAttestation_1.ATTESTING_INDICES), $to_set_<validatorIndex>(indexedAttestation_2.ATTESTING_INDICES))
 694:   43         -- let vid_sorted*{vid_sorted <- vid_sorted*} = $sort_<validatorIndex>(vid_intersection*{vid_intersection <- vid_intersection*})
 695:   43/####     -- rel SlashIfEligible_over_validators: state SLASH?[ vid_sorted*{vid_sorted <- vid_sorted*} ]~> state_slashed #SLASHED bool_slashed_any
 696:   42/   1     -- if bool_slashed_any
        42          SUCCESS

relation ProcessAttesterSlashings:
      rule nil:
 697:  567/  43     -- if attesterSlashing*{attesterSlashing <- attesterSlashing*} matches []
       567          SUCCESS
      rule cons:
 698:   43/####     -- if attesterSlashing*{attesterSlashing <- attesterSlashing*} matches _ :: _
 699:   43         -- let attesterSlashing_h :: attesterSlashing_t*{attesterSlashing_t <- attesterSlashing_t*} = attesterSlashing*{attesterSlashing <- attesterSlashing*}
 700:   42/   1     -- rel ProcessAttesterSlashing: state attesterSlashing_h ~> state_mid
 701:   41/   1     -- rel ProcessAttesterSlashings: state_mid attesterSlashing_t*{attesterSlashing_t <- attesterSlashing_t*} ~> state_out
        41          SUCCESS

relation ProcessAttestation:
      rule current_epoch:
 702: 2788         -- let attestationData = attestation.DATA
 703: 1110/1678     -- if (attestationData.TARGET.EPOCH = $get_current_epoch(state))
 704: 1110/####     -- if (attestationData.TARGET.EPOCH = $compute_epoch_at_slot(attestationData.SLOT))
 705: 1110/####     -- if ((attestationData.SLOT + $MIN_ATTESTATION_INCLUSION_DELAY) <= state.SLOT)
 706: 1110/####     -- if (state.SLOT <= (attestationData.SLOT + $SLOTS_PER_EPOCH))
 707: 1110/####     -- if (attestationData.INDEX < $get_committee_count_per_slot(state, attestationData.TARGET.EPOCH))
 708: 1110         -- let vid_committee*{vid_committee <- vid_committee*} = $get_beacon_committee(state, attestationData.SLOT, attestationData.INDEX)
 709: 1110/####     -- if (|attestation.AGGREGATION_BITS| = |vid_committee*{vid_committee <- vid_committee*}|)
 710: 1110         -- let vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} = $get_attestation_participation_flag_indices(state, attestationData, (state.SLOT - attestationData.SLOT))
 711: 1110/####     -- if $is_valid_indexed_attestation(state, $get_indexed_attestation(state, attestation))
 712: 1110/####     -- rel ProcessFlags_over_attesting: state.CURRENT_EPOCH_PARTICIPATION [ $get_attesting_indices(state, attestation) ]++{ vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} }# state #SUM 0 ~> participationFlags_updated*{participationFlags_updated <- participationFlags_updated*} #SUM reward_proposer_numerator
 713: 1110         -- let state_1 = state[CURRENT_EPOCH_PARTICIPATION = participationFlags_updated*{participationFlags_updated <- participationFlags_updated*}]
 714: 1110         -- let reward_proposer_denominator = ((($WEIGHT_DENOMINATOR - $PROPOSER_WEIGHT) * $WEIGHT_DENOMINATOR) / $PROPOSER_WEIGHT)
 715: 1110         -- let reward_proposer = (reward_proposer_numerator / reward_proposer_denominator)
 716: 1110/####     -- rel IncreaseBalance: state_1 .BALANCES[ $get_beacon_proposer_index(state_1) ]+ reward_proposer ~> state_out
      1110          SUCCESS
      rule previous_epoch:
 717: 1678         -- let attestationData = attestation.DATA
 718: 1678/####     -- if (attestationData.TARGET.EPOCH = $get_previous_epoch(state))
 719: 1678/####     -- if (attestationData.TARGET.EPOCH = $compute_epoch_at_slot(attestationData.SLOT))
 720: 1678/####     -- if ((attestationData.SLOT + $MIN_ATTESTATION_INCLUSION_DELAY) <= state.SLOT)
 721: 1678/####     -- if (state.SLOT <= (attestationData.SLOT + $SLOTS_PER_EPOCH))
 722: 1678/####     -- if (attestationData.INDEX < $get_committee_count_per_slot(state, attestationData.TARGET.EPOCH))
 723: 1678         -- let vid_committee*{vid_committee <- vid_committee*} = $get_beacon_committee(state, attestationData.SLOT, attestationData.INDEX)
 724: 1678/####     -- if (|attestation.AGGREGATION_BITS| = |vid_committee*{vid_committee <- vid_committee*}|)
 725: 1678         -- let vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} = $get_attestation_participation_flag_indices(state, attestationData, (state.SLOT - attestationData.SLOT))
 726: 1678/####     -- if $is_valid_indexed_attestation(state, $get_indexed_attestation(state, attestation))
 727: 1678/####     -- rel ProcessFlags_over_attesting: state.PREVIOUS_EPOCH_PARTICIPATION [ $get_attesting_indices(state, attestation) ]++{ vid_participation_flag*{vid_participation_flag <- vid_participation_flag*} }# state #SUM 0 ~> participationFlags_updated*{participationFlags_updated <- participationFlags_updated*} #SUM reward_proposer_numerator
 728: 1678         -- let state_1 = state[PREVIOUS_EPOCH_PARTICIPATION = participationFlags_updated*{participationFlags_updated <- participationFlags_updated*}]
 729: 1678         -- let reward_proposer_denominator = ((($WEIGHT_DENOMINATOR - $PROPOSER_WEIGHT) * $WEIGHT_DENOMINATOR) / $PROPOSER_WEIGHT)
 730: 1678         -- let reward_proposer = (reward_proposer_numerator / reward_proposer_denominator)
 731: 1678/####     -- rel IncreaseBalance: state_1 .BALANCES[ $get_beacon_proposer_index(state_1) ]+ reward_proposer ~> state_out
      1678          SUCCESS

relation ProcessAttestations:
      rule nil:
 732:  567/2788     -- if attestation*{attestation <- attestation*} matches []
       567          SUCCESS
      rule cons:
 733: 2788/####     -- if attestation*{attestation <- attestation*} matches _ :: _
 734: 2788         -- let attestation_h :: attestation_t*{attestation_t <- attestation_t*} = attestation*{attestation <- attestation*}
 735: 2788/####     -- rel ProcessAttestation: state attestation_h ~> state_h
 736: 2788/####     -- rel ProcessAttestations: state_h attestation_t*{attestation_t <- attestation_t*} ~> state_t
      2788          SUCCESS

relation ProcessDeposit:
      rule main:
 737:  461/####     -- if $is_valid_merkle_branch($hash_tree_root_depositData(deposit.DATA), deposit.PROOF, ($DEPOSIT_CONTRACT_TREE_DEPTH + 1), state.ETH1_DEPOSIT_INDEX, state.ETH1_DATA.DEPOSIT_ROOT)
 738:  461         -- let state_inc = state[ETH1_DEPOSIT_INDEX = (state.ETH1_DEPOSIT_INDEX + 1)]
 739:  461/####     -- rel ApplyDeposit: state_inc .VALIDATORS++{ deposit.DATA.PUBKEY deposit.DATA.WITHDRAWAL_CREDENTIALS deposit.DATA.AMOUNT }SIGNED deposit.DATA.SIGNATURE ~> state_applied
       461          SUCCESS

relation ProcessDeposits:
      rule nil:
 740:  567/ 461     -- if deposit*{deposit <- deposit*} matches []
       567          SUCCESS
      rule cons:
 741:  461/####     -- if deposit*{deposit <- deposit*} matches _ :: _
 742:  461         -- let deposit_h :: deposit_t*{deposit_t <- deposit_t*} = deposit*{deposit <- deposit*}
 743:  461/####     -- rel ProcessDeposit: state deposit_h ~> state_h
 744:  461/####     -- rel ProcessDeposits: state_h deposit_t*{deposit_t <- deposit_t*} ~> state_t
       461          SUCCESS

relation ProcessVoluntaryExit:
      rule main:
 745:  321         -- let voluntaryExit = signedVoluntaryExit.MESSAGE
 746:  321         -- let vid_exit = voluntaryExit.VALIDATOR_INDEX
 747:  321/####     -- if (vid_exit < |state.VALIDATORS|)
 748:  321         -- let validator = state.VALIDATORS[vid_exit]
 749:  321         -- let epoch_cur = $get_current_epoch(state)
 750:  321/####     -- if ($is_active_validator(validator, epoch_cur) = true)
 751:  319/   2     -- if (validator.EXIT_EPOCH = $FAR_FUTURE_EPOCH)
 752:  319/####     -- if (epoch_cur >= voluntaryExit.EPOCH)
 753:  319/####     -- if (validator.ACTIVATION_EPOCH <= ($UINT64_MAX - $SHARD_COMMITTEE_PERIOD))
 754:  319/####     -- if (epoch_cur >= (validator.ACTIVATION_EPOCH + $SHARD_COMMITTEE_PERIOD))
 755:  319         -- let domain_exit = $get_domain(state, $DOMAIN_VOLUNTARY_EXIT, ?(voluntaryExit.EPOCH))
 756:  319         -- let root_vol_exit_signing_root = $compute_signing_root_voluntary_exit(voluntaryExit, domain_exit)
 757:  319/####     -- if ($bls_verify(validator.PUBKEY, root_vol_exit_signing_root, signedVoluntaryExit.SIGNATURE) = true)
 758:  319/####     -- rel InitiateValidatorExit: state .VALIDATORS[ vid_exit ]~> state_exit
       319          SUCCESS

relation ProcessVoluntaryExits:
      rule nil:
 759:  565/ 321     -- if signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*} matches []
       565          SUCCESS
      rule cons:
 760:  321/####     -- if signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*} matches _ :: _
 761:  321         -- let signedVoluntaryExit_h :: signedVoluntaryExit_t*{signedVoluntaryExit_t <- signedVoluntaryExit_t*} = signedVoluntaryExit*{signedVoluntaryExit <- signedVoluntaryExit*}
 762:  319/   2     -- rel ProcessVoluntaryExit: state signedVoluntaryExit_h ~> state_h
 763:  318/   1     -- rel ProcessVoluntaryExits: state_h signedVoluntaryExit_t*{signedVoluntaryExit_t <- signedVoluntaryExit_t*} ~> state_t
       318          SUCCESS

relation ProcessBlsToExecutionChange:
      rule main:
 764:  255         -- let blsToExecutionChange = signedBlsToExecutionChange.MESSAGE
 765:  255         -- let vid_change = blsToExecutionChange.VALIDATOR_INDEX
 766:  255/####     -- if (vid_change < |state.VALIDATORS|)
 767:  255         -- let validator = state.VALIDATORS[vid_change]
 768:  253/   2     -- if ($get_first_byte(validator.WITHDRAWAL_CREDENTIALS) = $BLS_WITHDRAWAL_PREFIX)
 769:  253/####     -- if ($strip_first_byte(validator.WITHDRAWAL_CREDENTIALS) = $strip_first_byte($hash_<blsPubkey>(blsToExecutionChange.FROM_BLS_PUBKEY)))
 770:  253         -- let domain = $compute_domain($DOMAIN_BLS_TO_EXECUTION_CHANGE, ?(), ?(state.GENESIS_VALIDATORS_ROOT))
 771:  253         -- let root_sign = $compute_signing_root_bls_to_execution_change(blsToExecutionChange, domain)
 772:  253/####     -- if ($bls_verify(blsToExecutionChange.FROM_BLS_PUBKEY, root_sign, signedBlsToExecutionChange.SIGNATURE) = true)
 773:  253         -- let bytes32_new_wc = $make_withdrawal_credentials_eth1(blsToExecutionChange.TO_EXECUTION_ADDRESS)
       253          SUCCESS

relation ProcessBlsToExecutionChanges:
      rule nil:
 774:  563/ 255     -- if signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*} matches []
       563          SUCCESS
      rule cons:
 775:  255/####     -- if signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*} matches _ :: _
 776:  255         -- let signedBlsToExecutionChange_h :: signedBlsToExecutionChange_t*{signedBlsToExecutionChange_t <- signedBlsToExecutionChange_t*} = signedBlsToExecutionChange*{signedBlsToExecutionChange <- signedBlsToExecutionChange*}
 777:  253/   2     -- rel ProcessBlsToExecutionChange: state signedBlsToExecutionChange_h ~> state_h
 778:  251/   2     -- rel ProcessBlsToExecutionChanges: state_h signedBlsToExecutionChange_t*{signedBlsToExecutionChange_t <- signedBlsToExecutionChange_t*} ~> state_t
       251          SUCCESS

relation ProcessBlockHeader:
      rule :
 779:  575/####     -- if (block.SLOT = state.SLOT)
 780:  575/####     -- if (state.LATEST_BLOCK_HEADER.SLOT < block.SLOT)
 781:  574/   1     -- if ($get_beacon_proposer_index(state) = block.PROPOSER_INDEX)
 782:  573/   1     -- if (block.PARENT_ROOT = $hash_tree_root_beaconBlockHeader(state.LATEST_BLOCK_HEADER))
 783:  573         -- let beaconBlockHeader_new = {SLOT block.SLOT, PROPOSER_INDEX block.PROPOSER_INDEX, PARENT_ROOT block.PARENT_ROOT, STATE_ROOT $ZERO_ROOT, BODY_ROOT $hash_tree_root_beaconBlockBody(block.BODY)}
 784:  573/####     -- if (block.PROPOSER_INDEX < |state.VALIDATORS|)
 785:  573/####     -- if ~state.VALIDATORS[block.PROPOSER_INDEX].SLASHED
       573          SUCCESS

relation ProcessWithdrawals:
      rule len0:
 786:  573         -- let withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} = $get_expected_withdrawals(state)
 787:  573/####     -- if (executionPayload.WITHDRAWALS = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*})
 788:  573/####     -- rel ApplyWithdrawals: state .BALANCES{ withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} }~>* state_after_debits
 789:  551/  22     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| = 0)
 790:  551         -- let vid_next = ((state_after_debits.NEXT_WITHDRAWAL_VALIDATOR_INDEX + $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP) \ |state_after_debits.VALIDATORS|)
       551          SUCCESS
      rule lenMax:
 791:   22         -- let withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} = $get_expected_withdrawals(state)
 792:   22/####     -- if (executionPayload.WITHDRAWALS = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*})
 793:   22/####     -- rel ApplyWithdrawals: state .BALANCES{ withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} }~>* state_after_debits
 794:    3/  19     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| = $MAX_WITHDRAWALS_PER_PAYLOAD)
 795:    3         -- let withdrawal_latest = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}[(|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| - 1)]
 796:    3         -- let withdrawalIndex_next = (withdrawal_latest.INDEX + 1)
 797:    3         -- let vid_next = (withdrawal_latest.VALIDATOR_INDEX + (1 \ |state_after_debits.VALIDATORS|))
         3          SUCCESS
      rule lenPartial:
 798:   19         -- let withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} = $get_expected_withdrawals(state)
 799:   19/####     -- if (executionPayload.WITHDRAWALS = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*})
 800:   19/####     -- rel ApplyWithdrawals: state .BALANCES{ withdrawal_expected*{withdrawal_expected <- withdrawal_expected*} }~>* state_after_debits
 801:   19/####     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| =/= 0)
 802:   19/####     -- if (|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| =/= $MAX_WITHDRAWALS_PER_PAYLOAD)
 803:   19         -- let withdrawal_latest = withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}[(|withdrawal_expected*{withdrawal_expected <- withdrawal_expected*}| - 1)]
 804:   19         -- let withdrawalIndex_next = (withdrawal_latest.INDEX + 1)
 805:   19         -- let vid_next = ((state_after_debits.NEXT_WITHDRAWAL_VALIDATOR_INDEX + $MAX_VALIDATORS_PER_WITHDRAWALS_SWEEP) \ |state_after_debits.VALIDATORS|)
        19          SUCCESS

relation ProcessExecutionPayload:
      rule main:
 806:  573         -- let executionPayload = body.EXECUTION_PAYLOAD
 807:  573/####     -- if (executionPayload.PARENT_HASH = state.LATEST_EXECUTION_PAYLOAD_HEADER.BLOCK_HASH)
 808:  572/   1     -- if (executionPayload.PREV_RANDAO = $get_randao_mix(state, $get_current_epoch(state)))
 809:  572/####     -- if (executionPayload.TIMESTAMP = $compute_time_at_slot(state, state.SLOT))
 810:  572/####     -- if $ee_verify_and_notify_new_payload(executionPayload)
 811:  572         -- let executionPayloadHeader = {PARENT_HASH executionPayload.PARENT_HASH, FEE_RECIPIENT executionPayload.FEE_RECIPIENT, STATE_ROOT executionPayload.STATE_ROOT, RECEIPTS_ROOT executionPayload.RECEIPTS_ROOT, LOGS_BLOOM executionPayload.LOGS_BLOOM, PREV_RANDAO executionPayload.PREV_RANDAO, BLOCK_NUMBER executionPayload.BLOCK_NUMBER, GAS_LIMIT executionPayload.GAS_LIMIT, GAS_USED executionPayload.GAS_USED, TIMESTAMP executionPayload.TIMESTAMP, EXTRA_DATA executionPayload.EXTRA_DATA, BASE_FEE_PER_GAS executionPayload.BASE_FEE_PER_GAS, BLOCK_HASH executionPayload.BLOCK_HASH, TRANSACTIONS_ROOT $hash_tree_root_tx(executionPayload.TRANSACTIONS), WITHDRAWALS_ROOT $hash_tree_root_withdrawals(executionPayload.WITHDRAWALS)}
       572          SUCCESS

relation ProcessRandao:
      rule :
 812:  572         -- let epoch = $get_current_epoch(state)
 813:  572         -- let vid_proposer = $get_beacon_proposer_index(state)
 814:  572/####     -- if (vid_proposer < |state.VALIDATORS|)
 815:  572         -- let validator_proposer = state.VALIDATORS[vid_proposer]
 816:  572         -- let domain_randao = $get_domain(state, $DOMAIN_RANDAO, ?())
 817:  572         -- let root_signing = $compute_signing_root_epoch(epoch, domain_randao)
 818:  572/####     -- if $bls_verify(validator_proposer.PUBKEY, root_signing, body.RANDAO_REVEAL)
 819:  572         -- let bytes32_mix_old = $get_randao_mix(state, epoch)
 820:  572         -- let bytes32_hash_reveal = $hash_<blsSignature>(body.RANDAO_REVEAL)
 821:  572         -- let bytes32_mix_new = $xor(bytes32_mix_old, bytes32_hash_reveal)
 822:  572         -- let uint64_n = (epoch \ $EPOCHS_PER_HISTORICAL_VECTOR)
       572          SUCCESS

relation ProcessEth1Data:
      rule majority:
 823:  572         -- let eth1Data_update*{eth1Data_update <- eth1Data_update*} = state.ETH1_DATA_VOTES ++ [body.ETH1_DATA]
 824:  572         -- let n_occ = $count_occurrences_<eth1Data>(eth1Data_update*{eth1Data_update <- eth1Data_update*}, body.ETH1_DATA)
 825: ####/ 572     -- if ((n_occ * 2) > ($EPOCHS_PER_ETH1_VOTING_PERIOD * $SLOTS_PER_EPOCH))
      ####          SUCCESS
      rule accumulate:
 826:  572         -- let eth1Data_update*{eth1Data_update <- eth1Data_update*} = state.ETH1_DATA_VOTES ++ [body.ETH1_DATA]
 827:  572         -- let n_occ = $count_occurrences_<eth1Data>(eth1Data_update*{eth1Data_update <- eth1Data_update*}, body.ETH1_DATA)
 828:  572/####     -- if ((n_occ * 2) <= ($EPOCHS_PER_ETH1_VOTING_PERIOD * $SLOTS_PER_EPOCH))
       572          SUCCESS

relation ProcessOperations:
      rule :
 829:  570/   2     -- if (|body.DEPOSITS| = $min_uint64($MAX_DEPOSITS, (state.ETH1_DATA.DEPOSIT_COUNT - state.ETH1_DEPOSIT_INDEX)))
 830:  568/   2     -- rel ProcessProposerSlashings: state body.PROPOSER_SLASHINGS ~> state_ps
 831:  567/   1     -- rel ProcessAttesterSlashings: state_ps body.ATTESTER_SLASHINGS ~> state_as
 832:  567/####     -- rel ProcessAttestations: state_as body.ATTESTATIONS ~> state_att
 833:  567/####     -- rel ProcessDeposits: state_att body.DEPOSITS ~> state_dep
 834:  565/   2     -- rel ProcessVoluntaryExits: state_dep body.VOLUNTARY_EXITS ~> state_ve
 835:  563/   2     -- rel ProcessBlsToExecutionChanges: state_ve body.BLS_TO_EXECUTION_CHANGES ~> state_out
       563          SUCCESS

relation ProcessSyncAggregate:
      rule :
 836:  563         -- let blsPubkey_committee*{blsPubkey_committee <- blsPubkey_committee*} = state.CURRENT_SYNC_COMMITTEE.PUBKEYS
 837:  563         -- let (i_committee, blsPubkey)*{blsPubkey <- blsPubkey*, i_committee <- i_committee*} = $enumerate_<blsPubkey>(blsPubkey_committee*{blsPubkey_committee <- blsPubkey_committee*})
 838: ####/####     -- (if (blsPubkey = blsPubkey_committee))*{blsPubkey <- blsPubkey*, blsPubkey_committee <- blsPubkey_committee*}
 839:  563         -- let (_nat, blsPubkey_participants)*{_nat <- _nat*, blsPubkey_participants <- blsPubkey_participants*} = $filter_list_2_<(nat, blsPubkey), boolean*>((i_committee, blsPubkey_committee)*{blsPubkey_committee <- blsPubkey_committee*, i_committee <- i_committee*}, syncAggregate.SYNC_COMMITTEE_BITS, $bit_is_one_en_blsPubkey)
 840:  563         -- let slot_prev = $get_prev_slot(state.SLOT)
 841:  563         -- let domain = $get_domain(state, $DOMAIN_SYNC_COMMITTEE, ?($compute_epoch_at_slot(slot_prev)))
 842:  563         -- let root_sign = $compute_signing_root_block_root($get_block_root_at_slot(state, slot_prev), domain)
 843:  563/####     -- if $eth_fast_aggregate_verify(blsPubkey_participants*{blsPubkey_participants <- blsPubkey_participants*}, root_sign, syncAggregate.SYNC_COMMITTEE_SIGNATURE)
 844:  563         -- let uint64_total_incs = ($get_total_active_balance(state) / $EFFECTIVE_BALANCE_INCREMENT)
 845:  563         -- let reward_total_base = ($get_base_reward_per_increment(state) * uint64_total_incs)
 846:  563         -- let reward_max_participant = (((reward_total_base * $SYNC_REWARD_WEIGHT) / $WEIGHT_DENOMINATOR) / $SLOTS_PER_EPOCH)
 847:  563         -- let reward_participant = (reward_max_participant / $SYNC_COMMITTEE_SIZE)
 848:  563         -- let reward_proposer = ((reward_participant * $PROPOSER_WEIGHT) / ($WEIGHT_DENOMINATOR - $PROPOSER_WEIGHT))
 849:  563         -- let (vid, validator)*{validator <- validator*, vid <- vid*} = $enumerate_<validator>(state.VALIDATORS)
 850: ####         -- (let blsPubkey_existing = validator.PUBKEY)*{blsPubkey_existing <- blsPubkey_existing*, validator <- validator*}
 851:  563/####     -- if (state.CURRENT_SYNC_COMMITTEE.PUBKEYS = blsPubkey_committee*{blsPubkey_committee <- blsPubkey_committee*})
 852: ####         -- (let vid_committee = $index_of_pubkey((vid, blsPubkey_existing)*{blsPubkey_existing <- blsPubkey_existing*, vid <- vid*}, blsPubkey_committee))*{blsPubkey_committee <- blsPubkey_committee*, vid_committee <- vid_committee*}
 853:  563         -- let boolean_participation*{boolean_participation <- boolean_participation*} = syncAggregate.SYNC_COMMITTEE_BITS
 854:  563         -- let vid_proposer = $get_beacon_proposer_index(state)
 855:  563/####     -- rel ApplyReward_over_participants: state PARTICIPANT boolean_participation*{boolean_participation <- boolean_participation*} [ vid_committee*{vid_committee <- vid_committee*} ]+ reward_participant PROPOSER[ vid_proposer ]+ reward_proposer ~> state_updated
       563          SUCCESS

relation ProcessBlock:
      rule :
 856:  573/   2     -- rel ProcessBlockHeader: state block ~> state_0
 857:  573/####     -- rel ProcessWithdrawals: state_0 block.BODY.EXECUTION_PAYLOAD ~> state_1
 858:  572/   1     -- rel ProcessExecutionPayload: state_1 block.BODY ~> state_2
 859:  572/####     -- rel ProcessRandao: state_2 block.BODY ~> state_3
 860:  572/####     -- rel ProcessEth1Data: state_3 block.BODY ~> state_4
 861:  563/   9     -- rel ProcessOperations: state_4 block.BODY ~> state_5
 862:  563/####     -- rel ProcessSyncAggregate: state_5 block.BODY.SYNC_AGGREGATE ~> state_6
       563          SUCCESS

relation State_transition:
      rule validate:
 863:  581/####     -- if (bool = true)
 864:  581         -- let beaconBlock = signedBeaconBlock.MESSAGE
 865:  578/   3     -- rel ProcessSlots: state beaconBlock.SLOT ~> state_cur
 866:  575/   3     -- if $verify_block_signature(state_cur, signedBeaconBlock)
 867:  563/  12     -- rel ProcessBlock: state_cur beaconBlock ~>* state_next
 868:  562/   1     -- if (beaconBlock.STATE_ROOT = $hash_tree_root_beaconState(state_next))
       562          SUCCESS
      rule skip-validate:
 869: ####/  19     -- if (bool = false)
 870: ####         -- let beaconBlock = signedBeaconBlock.MESSAGE
 871: ####/####     -- rel ProcessSlots: state beaconBlock.SLOT ~> state_cur
 872: ####/####     -- rel ProcessBlock: state_cur beaconBlock ~>* state_next
      ####          SUCCESS
